# ADOR OS — Session Handoff / Project Context

This file is auto-loaded by Claude Code at the start of every session in this directory. It exists so a fresh session can continue this project without replaying the conversation that produced it. Keep it current — update it at the end of any session that changes architecture, status, or plans.

## What this project is

ADOR is a 3-founder strategic intelligence firm. ADOR OS is their internal operating system — invite-only, built to orient/focus/execute, not a passive dashboard. The design bar throughout has been "does this feel like Apple designed it internally" — restrained glass surfaces, no component libraries, deliberate motion, elegant empty states.

The user (Ángel) is new to both Claude Code and web development. Explain the "why" as you go; he prefers a direct recommendation over a menu of open-ended options when asked "what should we do."

## Current status

See `PROJECT_STATE.md` for the up-to-date checklist of what's built vs. pending — that file is the living status snapshot and gets updated more often than this one. Short version (as of 2026-09-16): Phases 1–2 (shell + Home) and five Phase 3 modules (Clientes, Finanzas, Workspace, Objetivos, ADOR IA) are done and live; Firestore/Auth/deployment are all set up. ADOR IA responds via a local, zero-cost rule-based engine by deliberate user choice, not Gemini — see §17 (§16 describes the Gemini path, which exists in the codebase but isn't what's running). The app now has its real logo (§18), and Workspace now opens on a "Hoy" landing view (quick capture + Vencidas + Para hoy + Mis Pendientes) instead of the full team board, with a labeled Hoy/Lista/Kanban/Timeline view switcher — the earlier standalone "Notas" tab was folded into Hoy the same day it shipped (§19). Assigning a task to someone other than yourself now requires their confirmation via a blocking popup (`AssignmentConfirmGate.jsx`) before it counts as theirs anywhere in the app (§20). Conocimiento (a Markdown wiki) and News (with Comunidad as a tab inside it) shipped on 2026-09-22 — Conocimiento is for documents/knowledge, not general notes (§18). Directorio (§30) is done and live as of 2026-09-18: Personas (Dirección cards + Equipo table + person detail panel), Organigrama, Equipos, and Roles, all built on two new collections (`directoryPeople`/`directoryTeams`) decoupled from real login accounts. **Update 2026-09-24:** hosting moved to Cloudflare Workers (`ador-os.adorfirm.workers.dev`, free, §36); files and backups live in Google Drive (§35/§37); global search, error log, roles + Administración (§37); installable on phone/iPad with a mobile layout (§38). Earlier, 2026-09-23: every module has shipped; Chat was rebuilt into **Comunicación** (§34) — DMs, groups, channels with permissions, threads, mentions, Google Meet calls with ringing (one-click confirmed live), presence/statuses, search, a graphite-and-gold look, and a free 90-day retention policy for conversation files (text kept forever).

## Architecture decisions worth knowing before touching this code

### 1. Portal-based floating UI is load-bearing — don't regress it

Sidebar tooltips, the profile hover-pill, the notification dropdown, and the profile dropdown all render via `createPortal(..., document.body)` with `position: fixed` and coordinates computed from the trigger's `getBoundingClientRect()`.

This isn't decoration — it fixes a **confirmed, reproduced bug**: the sidebar and top-bar pill-nav are `rounded-full` capsules sized by shrink-to-fit (`width: auto`). When a hover tooltip/dropdown was nested *inside* one of those capsules as a normal `position: absolute` child, Chromium let the tooltip's width leak into the capsule's own auto-width calculation — hovering a sidebar icon made the whole capsule balloon from 62px to 150px wide. Verified numerically via `getBoundingClientRect()` before/after; fixed by moving all four to portals.

**Rule for any new floating UI (new dropdown, new tooltip, new popover) added later: never nest it inside a shrink-wrapped flex/glass container. Portal it to `document.body` and position it via a measured rect, following the existing pattern in `TopBar.jsx` (`ProfileTrigger`) and `Sidebar.jsx` (`NavButton`).**

Related gotcha already fixed once: if the anchor rect is measured only on mount (not on window resize), resizing the browser window desyncs the portaled element's position. `ProfileTrigger` in `TopBar.jsx` has a `resize` listener for this — copy that pattern, don't cache a rect without it.

### 2. Shell navigation is deliberately split across two surfaces

- **Top bar** (`TopBar.jsx`): a centered pill-tab bar (3-column CSS grid: logo / tabs / icons) holding the "primary" modules — Inicio, Workspace, Objetivos, Clientes, Finanzas. No background/border of its own; it blends into the page (per explicit user feedback — an earlier version had its own glass background and the user said it looked like "a rectangle" separating from the page).
- **Sidebar** (`Sidebar.jsx`): a floating rounded capsule (not a full-height panel) holding the remaining modules — Calendario, Conocimiento, Comunidad, Chat, News, Directorio — plus ADOR IA below a divider. Calendario moved here from the top bar on 2026-08-14 (user's own call, made by pointing at the sidebar in a screenshot) — no other reasoning recorded beyond that preference.

This split came from direct user reference images (a pill-tab nav bar + a separate floating icon capsule), not from the original written spec, which originally put all 10 modules in one full-height sidebar. If asked to add a new module, ask which surface it belongs on rather than assuming.

### 3. Design tokens live in `src/index.css`, reused everywhere

- `.ador-glass` — the standard card/surface recipe: `background: rgba(255,255,255,0.05)`, `border: 1px solid rgba(255,255,255,0.1)`, `backdrop-filter: blur(24px) saturate(160%)`, `box-shadow: 0 4px 24px rgba(0,0,0,0.3)`. Apply to every new card/dropdown/panel.
- `.ador-grain` — a 0.03-opacity SVG-noise `::after` overlay, paired with `.ador-glass` on almost everything.
- `.ador-skeleton` — a shimmer-sweep gradient (not a static dash) for values with no data yet. Added specifically because plain "—" placeholders made the Home screen feel dead; the shimmer reads as "the system is tracking this, just waiting on data."
- Keyframes: `ador-drift` (slow background gradient drift, Welcome screen), `ador-pulse` (opacity breathe, used for live-status dots and breathing icons), `ador-spin-dot` (6-dot loading ring), `ador-shimmer` (skeleton sweep).
- Color roles: bg `#0A0A0A`, text `#F5F5F5` / `#888888` / `#444444`, accent blue `#1E5FAD` (primary/brand), accent gold `#B8860B` (used sparingly — decisions/checkmarks only).

### 4. Firebase is guarded against missing config

`src/firebase.js` exports `isFirebaseConfigured` and only calls `initializeApp`/`getAuth` if `VITE_FIREBASE_API_KEY` is set. `useAuth.js` checks `auth` for null before every call. This was deliberate: the Firebase SDK throws synchronously on an invalid API key, which would otherwise white-screen the whole app if `.env` is ever missing. Preserve this pattern if Firestore gets wired in — `db` in `lib/firestore.js` already follows the same guard.

### 5. The `?preview=1` URL param is a dev-only test harness — not for shipping

`App.jsx` has a `PREVIEW_MOCK_USER` that bypasses Splash/Login/Welcome and injects a fake authenticated user (with a fabricated `displayName` and `lastSignInTime`) when the URL has `?preview=1`. This exists purely so Claude's own screenshot-based preview tooling could visually verify the authenticated shell without doing a real Google OAuth round-trip each time. It's harmless (real users won't stumble onto it), but don't rely on it as a real auth bypass, and consider removing it before any real deployment.

### 6. Welcome screen logic (`useWelcomeScreen.js`)

Shows on: first login of the day, or returning after 13:00 when the last login was before 13:00 same day. Tracked via `localStorage` keys `ador_last_login_at` and `ador_welcome_shown` (keyed by date + time-block so it never repeats within the same block). If this logic ever needs to change, the block boundaries (`morning` 6–13, `lunch` 13–15, `afternoon` 15–19, `evening` 19–6) are in that file.

### 7. Finance lives on Home now, Finanzas module later — deliberately not merged into Clientes

User asked (2026-08-14) where a financial overview for founders should live, and specifically floated combining it with Clientes. Decision: keep them separate *as modules/UI*. Clientes is a CRM/relationship model (client status, intervention progress); finance is a money-flow model (revenue, eventually expenses/runway) — different mental models that get harder to scan together as both grow.

However, they share one *data* source of truth on purpose (revised same day once Clientes actually got built — see §8): `FinanceBlock.jsx` on Home computes its monthly revenue, % change, and sparkline by reading `pago1`/`pago2` fields off every `clients/{id}` doc, not a separately-entered collection. An earlier version of this used a standalone hand-entered `revenue` collection — that was retired specifically to avoid a dual-source-of-truth problem (see §8's "why payments live on the client doc" for the reasoning). When a full Finanzas module gets built, it should read the same `pago1`/`pago2` fields for quarterly/annual rollups — never re-introduce a parallel manually-entered ledger.

### 8. Clientes module (2026-08-14) — SPC→SP pipeline CRM

Built from a detailed user-supplied spec (ADOR's internal vocabulary: SPC = Strategic Partner Candidate, SP = Strategic Partner once "Intervención Activa" starts; never "cliente"/"prospecto" in UI copy — see `lib/clientStages.js` for the full pipeline stage list). Lives at `src/components/clientes/`, wired into `AppShell.jsx` for the `clientes` nav item.

**Data model decision — payments live on the client doc, not a separate ledger.** Each `clients/{id}` doc holds its own `pago1`/`pago2` fields (`{ amount, status: 'Pendiente'|'Recibido', date: 'YYYY-MM-DD' }`, fixed 60/40 split — constants in `clientStages.js`). This was a deliberate choice over a separate `payments`/`invoices` collection: with only ~10s of clients ever, per-client payment state is simple enough to live inline, and it means the Pagos tab (Clientes) and the Home Finanzas block are reading the exact same fields — no sync step, no drift. If per-client payments ever need to support more than 2 fixed installments, *that's* the trigger to extract a real `payments` subcollection; don't do it preemptively.

**Data model decision — intervention progress lives on the client doc too.** `interventionWeek`/`interventionTotalWeeks` fields on the client, active once `stage === 'intervencion_activa'`. Originally (earlier the same day) there was a separate `interventions` collection referencing `clientId` — retired once it became clear "the intervention" and "the SP's current stage" are the same concept in ADOR's actual mental model, not two things to keep in sync.

**Payment dates are plain strings, not Firestore Timestamps.** `pago1.date`/`pago2.date` are `'YYYY-MM-DD'` strings (set via `<input type="date">`), not `serverTimestamp()` sentinels — these are calendar dates the team enters/edits by hand, not exact server-clock events. `useHomeData.js`'s revenue bucketing reads `payment.date.slice(0, 7)` for the month key accordingly. Don't "fix" this back to Timestamps without checking that call site.

**No Firebase Admin SDK, so no way to list every Auth user from the client** — "Asociado responsable" pickers need a directory. Solved with self-registration: `App.jsx` writes `users/{uid}` (displayName, email) via `saveUserProfile` on every real login (skipped for the `?preview=1` mock user — check `user.uid !== 'preview'` before any write that touches shared collections like `users`, or you'll pollute the real Firestore project with a fake "preview" directory entry, which happened once during testing and had to be manually cleaned up). `subscribeUsers()` in `lib/firestore.js` powers the dropdown.

**Kanban drag-and-drop is hand-built with Framer Motion, no DnD library.** Each `ClientCard` uses `drag` + `dragSnapToOrigin` + `onDragEnd`; `KanbanBoard.jsx` keeps a ref map of each column's DOM node and hit-tests the drop point (`info.point`) against `getBoundingClientRect()` on drop — same pattern already used for portaled dropdown positioning elsewhere in the shell. The SPC→SP conversion glow-pulse fires by diffing each client's stage against a `prevStagesRef` snapshot on every Firestore update, not from the drag handler directly — so it also fires correctly when a stage change happens from the List view's quick-action arrow, not just from a Kanban drag.

**Documentos tab is metadata-only — Firebase Storage isn't enabled.** File name/type/size get written to `clients/{id}/documents`, but no actual file bytes are stored anywhere yet (no Storage bucket configured, unlike Firestore/Auth which went through the same enable-in-console flow earlier this project). The download button is present but disabled with a tooltip explaining why. Enabling Storage (a console step, same pattern as Firestore setup) is the next infrastructure prerequisite before Documentos can be made real — walk the user through it the same way Firestore was set up (see git history / earlier CLAUDE.md revisions for that flow).

**Testing note for future sessions:** simulating real user interaction (hover, blur, drag) via raw `dispatchEvent` calls in this environment is unreliable — React's synthetic event system needs proper `relatedTarget`/focus state that bare `new MouseEvent(...)` often doesn't replicate, and a `.blur()` fired in the same synchronous tick as a preceding `input` event can race React's state update. When verifying forms/interactions here, prefer calling the exported `lib/firestore.js` functions directly via `import('/src/lib/firestore.js')` in the browser console to isolate "does the write work" from "did my simulated event fire" — this saved significant time chasing false-positive bugs during this build.

### 9. Finanzas module (2026-08-14) — asymmetric dashboard, no charting library

Built from a detailed user-supplied UI spec. Lives at `src/components/finanzas/`, wired into `AppShell.jsx` for the `finanzas` nav item — added as a 6th tab on the **top bar** pill-nav (`TopBar.jsx` `PRIMARY_MODULES`), alongside Clientes, not on the sidebar. Layout is a 68%/32% two-column split (`FinanzasModule.jsx`), a deliberate asymmetric composition per the user's spec rather than the shell's usual centered/symmetric card grids.

**Chart is hand-drawn SVG, not Recharts/Chart.js.** The user's spec offered either library, but the project's existing convention (see `FinanceBlock.jsx`'s Sparkline, and README's "No UI component libraries — every control is hand-built") already solved this exact problem without a dependency. `FinanceChart.jsx` follows that precedent: a line+area SVG path animated via Framer Motion's `pathLength`, hover tooltip via mouse-tracked SVG rects, no new package added to `package.json`.

**Ingresos are never a hand-entered monthly total — same source-of-truth rule as Home/Clientes (§7/§8).** `useFinanceData.js` derives "Últimos Ingresos" and the hero "Ingresos del mes" from `clients/{id}.pago1`/`pago2` records marked `Recibido`, merged with a new `incomes` collection that holds *only* manual entries (income not tied to an SP payment — e.g. a one-off consulting fee). Don't reintroduce a parallel collection for SP-payment income; if it doesn't come from a client's pago1/pago2, it's manual and belongs in `incomes`.

**New Firestore collections:** `expenses` (category, description, amount, date, optional `receipt` metadata, notes), `incomes` (manual-only, as above), and a single shared doc `settings/finanzas` holding `quarterlyTarget` (one target at a time, edited inline via the pencil icon on the Meta del Trimestre card — no historical target tracking yet, that's the trigger to add one if it's ever needed).

**Comprobante (expense receipt) is metadata-only, same reason as Clientes → Documentos (§8).** Firebase Storage isn't enabled, so `AddExpenseModal.jsx` stores `{name, type, size}` only — no file bytes. When Storage gets enabled, wire both Documentos and Comprobante uploads together, not separately.

**"Ver todos" on Últimos Ingresos/Gastos expands in place rather than linking anywhere.** There's no dedicated full-ledger page yet; a dead link would violate the spec's own "if it doesn't inform a decision, remove it" instruction. If a full transactions view becomes a real need, that's a separate module/route, not a retrofit of this link.

**Redesigned 2026-08-14 (same day) to match a fintech-dashboard reference image the user shared.** Kept ADOR's own tokens (blue/gold accents, `.ador-glass`, no orange) but adopted the reference's *shapes*: metric numbers became small glass cards with a % badge (`MetricCards.jsx`, replacing the original card-less typography-only hero), the line chart became a bar chart with a highlighted active bar and a floating value bubble (`FinanceChart.jsx`), the two side-by-side 5-row income/expense lists became one searchable, scrollable transactions table with colored status pills (`MovimientosTable.jsx`, replacing `MovimientosBlock.jsx`), and the flat category list became a segmented stacked bar + legend, echoing the reference's "Smart Spending Limits" widget (`CategoryBreakdownCard.jsx`). Two elements from the reference were deliberately **not** ported: the credit-card visual and the "Upgrade Pro" ad card — neither maps to anything ADOR actually needs. The ad card's spot was replaced with `NextPaymentCard.jsx` ("Próximo Cobro" — nearest pending SP payment), which actually informs a decision (who to follow up with) instead of selling an upgrade.

**Firestore rules were not touched by this build — verify them.** This repo has no local `firestore.rules` file (rules are edited live in Firebase Console, per how Firestore was originally set up — see Infrastructure status in `PROJECT_STATE.md`). The `expenses`/`incomes`/`settings` collections were added to `lib/firestore.js` assuming the existing rule (`request.auth != null` AND `allowedEmails/{email}` exists) is a blanket rule covering all collections under `/databases/(default)/documents/{document=**}`. If it's instead scoped per-collection, these three need to be added explicitly in the console before real (non-preview) writes will succeed.

### 10. Workspace module (2026-08-14) — the operational brain, built from a detailed spec

Lives at `src/components/workspace/`, wired into `AppShell.jsx` for the `workspace` nav item. Built from a ClickUp/Monday-style spec the user provided, adapted with judgment calls documented below.

**Intervenciones are never stored — they're derived live from `clients`, same rule as Finanzas/Home (§7/§8/§9).** The spec described "Intervenciones" as one work-group per active SP with its own 8-week duration and 7-layer progress. Rather than create a parallel `interventions`/`workstreams` collection (which CLAUDE.md §8 already documents as tried-and-retired once for this exact concept), `useWorkspaceData.js` derives one virtual workstream per client where `stage === 'intervencion_activa'`, reusing the client's existing `interventionWeek`/`interventionTotalWeeks` fields. Its id is `client:{clientId}`. The current layer (1–7) is *computed* from week/total — there's no `currentLayer` field anywhere. **Proyectos Internos are the only new collection** (`proyectosInternos`) since there's no existing concept for internal firm work; its workstream id is `proyecto:{proyectoId}`. See `lib/workspace.js` for the id convention and the 7 layer names.

**Tasks got a schema change — 4-state status, array assignees.** `tasks/{id}` now holds `workstreamId` (one of the two id shapes above), `assignedTo` (array of userIds, not a single scalar — the spec wanted up to 3 overlapping avatars per task), `priority` (alta/media/baja), and `status` using the spec's 4 states (`por_hacer`/`en_progreso`/`completado`/`bloqueado`) instead of the original 2-state (`pendiente`/`completada`). This is a breaking rename from Home's original task-status vocabulary — `TasksTodayBlock.jsx` and `useHomeData.js`'s fallback were updated in the same change. Safe to do because no real tasks existed yet in production. `subscribeTasksForUser` switched from `where('assignedTo','==',uid)` to `where('assignedTo','array-contains',uid)` to match.

**Decisions got a real write path.** `decisions` already existed (Home's "Última Decisión" read from it) but had no creation UI anywhere — `createDecision()` in `lib/firestore.js` is new. A decision can optionally link to a workstream (`clientId` for an Intervención, `proyectoId` for a Proyecto Interno). Because Home already subscribes to the same collection, decisions registered from Workspace show up in Home automatically — no extra wiring needed.

**Scope cut: Timeline view was not built.** The spec asked for three views (Lista/Kanban/Timeline) toggled top-right. Lista (the spec's own "show me this first" priority) and Kanban shipped — Kanban reuses the exact hand-built Framer Motion drag-and-drop + rect hit-testing pattern already proven in Clientes' `KanbanBoard.jsx` (see §8), just re-targeted at status columns instead of pipeline stages. Timeline (a horizontal Gantt-style week/month view with its own scroll and hit-testing) is a materially bigger, mostly-planning-oriented feature that didn't fit the same pass without either rushing it or shipping something half-built — and per this project's own working rule, a half-finished feature is worse than an absent one. The view-toggle only shows Lista/Kanban for now; add Timeline as its own follow-up rather than bolting a third tab onto the toggle first and filling it in later.

**Scope cut: row-level hover quick-actions (edit/delete icons) were dropped.** The spec wanted them on every Lista row, but clicking a row already opens the Task Detail Panel where edit and delete both live — a redundant hover-revealed icon that does the same thing violates this project's own "nothing decorative, everything earns its place" rule. Delete lives only in the Task Detail Panel, and needs a second confirming click (no native `confirm()` dialog, to stay consistent with the rest of the app's modal-based confirms) rather than firing immediately from a small row icon.

**Priority colors follow the spec's traffic-light scheme (`#EF5350`/`#FFC107`/`#4CAF50`) rather than ADOR's usual blue/gold accents** — this is the one place gold is deliberately absent, since a 3-color severity scale reads faster as red/amber/green than as a blue/gold/gray scale would, and ADOR's own token rules already reserve gold for "decisions/checkmarks only" (§3), not priority.

**Timeline view was added later the same day**, once the user shared a reference image (a Gantt-style board with rounded date-bounded pill bars, a "Now" line, and diamond milestones). `TimelineView.jsx` reuses the reference's *shapes* on ADOR's own tokens: pixel-based date-to-x mapping (not CSS grid) so bars can have exact, comparable widths with horizontal scroll for long ranges; a live "Hoy" line; and diamond markers for tasks with no `startDate` (a real duration needs two dates, and most tasks will only ever get a due date — the diamond is what a 1-day/no-duration task honestly is, not a forced zero-width bar). This requires tasks to carry an optional `startDate` alongside `dueDate` — added as a second date input in the Task Detail Panel, directly beside "Fecha límite". Tasks with no dates simply don't appear in Timeline yet; there's no forced placeholder, consistent with this project's elegant-empty-state rule. The view-toggle now shows all three views (Lista/Kanban/Timeline). The date axis (and "Hoy" line) always renders even with zero dated tasks, defaulting to a 5-week window around today — the empty-state message sits inside the board instead of replacing it, per the user's own follow-up request.

**Empty Workspace shows a real "General" table instead of a blank message.** Originally, zero Intervenciones + zero Proyectos Internos rendered a single centered sentence — the user's follow-up feedback was that this is meant to be the whole company's general workspace, so it needs to look and work like one even before anything's been created. `ListaView.jsx` now falls back to a synthetic `GENERAL_WORKSTREAM` (`{ id: null, kind: 'proyecto_interno', name: 'General' }`) that's never persisted itself — its column headers and "+ Agregar tarea" row render immediately, and the moment someone actually adds a task through it, `InlineAddTask`'s submit handler silently provisions a real "General" `proyectosInternos` doc first, then attaches the task to it. No "create a project before you can even see the board" gate.

**Each group's table got `overflow-x-auto` + a `min-w-[560px]` floor.** Without it, `TaskRow`'s fixed-width columns (avatar stack / priority / due / status) could exceed a narrow Workspace main column and overflow silently — and because an overflowing focused element can force the whole (deliberately non-scrollable, `overflow:hidden`) page to shift horizontally to bring it into view, that overflow was capable of clipping the sidebar and Decisiones panel at the edges of the screen instead of just affecting the table. Confirmed the fix holds by testing down to 1100px width — the table now gets its own local horizontal scrollbar and nothing else on the page moves.

**Lista's rows became a real `<table>` per the user's own follow-up request** ("que se vea como tablas ordenadas"), then got replaced again almost immediately by a CSS Grid version once an actual `<table>` proved unreliable at this row's column count/widths — the Estado column was clipping at moderate viewport widths, an HTML table's per-cell auto-sizing fighting the fixed pixel widths needed for alignment. `TASK_ROW_GRID` (`lib/workspace.js`) is a single `grid-template-columns` string shared by the header row and every `TaskRow.jsx`, so columns are pixel-exact and never renegotiate based on content the way table cells do. Each `WorkstreamGroup` is also now a proper `.ador-glass ador-grain` card (previously just a left-border strip with no background) with a type badge ("Intervención"/"Proyecto Interno") next to the group name — reads as an actual project-management surface instead of a bare list, per direct user feedback that the plain version "no se ve como un sistema de project management." The redundant "Sin tareas todavía" text row was dropped — an empty group with just its header badge and "+ Agregar tarea" already communicates emptiness without a second, weaker echo of the same fact.

**Lista's cells became independently editable, per direct user request ("así como hacen las excelentes plataformas de project management").** Clicking a task's title still opens the full Task Detail Panel, but Asignado/Prioridad/Vence/Estado now edit inline without leaving the row — matching how Linear/Asana/ClickUp handle list-view editing. `CellPopover.jsx` is a small reusable floating menu (portaled to `document.body`, positioned from the trigger's `getBoundingClientRect()` — the same rule as every other floating element per §1) used by `TaskRow.jsx`'s `PillCell` (Prioridad/Estado dropdowns) and `AssigneeCell` (checkbox list); `DueDateCell` just swaps to a native `<input type="date">` in place, no popover needed. Every cell's click handler calls `stopPropagation()` so editing a cell never also triggers the title's "open detail panel" click. Found and fixed a real bug while wiring this up: `CellPopover` originally wrapped its two children in `<AnimatePresence>` without keys, which Framer Motion needs when there's more than one direct child — threw a duplicate-key warning. Fixed by dropping the (unnecessary — the popover mounts/unmounts as a whole via the parent's conditional render, so `AnimatePresence`'s exit-animation coordination wasn't doing anything anyway) `AnimatePresence` wrapper entirely.

**Silent write failures got real error feedback, after the user reported "+ Agregar tarea" as simply not working.** Every create/update call in Workspace (`InlineAddTask`'s submit, and `TaskRow`'s per-cell `applyUpdate`/complete-toggle) previously called Firestore directly with no `.catch` — a failed write (wrong/no auth, rules not yet covering a collection) did nothing visible at all, which reads identically to a broken button. Two fixes, both in `lib/workspace.js`: (1) every write now shows a toast on rejection instead of failing silently: (2) `withTimeout()` races the write against an 8s timer, because a write with no valid auth token attached doesn't always reject promptly — confirmed directly (`await import('/src/lib/workspace.js')` from the console, timing a promise that never resolves) that Firestore can leave such a promise pending indefinitely rather than erroring, which is the more likely real explanation for "nothing happens" than a wiring bug — the click handlers themselves were verified correct by dispatching real DOM events at the exact button. If a write still silently does nothing after this, the next thing to check is whether the signed-in account is in `allowedEmails` and whether Firestore rules cover `proyectosInternos`/`tasks` yet (§9's flagged action item, still open).

**Lista's columns expanded to Tarea/Descripción/Asignado/Prioridad/Estimación/Estado after the user shared a ClickUp-style reference image.** Two additions, both editable inline via the same `CellPopover` pattern as everything else: `description` (new optional string field on tasks — click-to-edit textarea, also added to the Task Detail Panel for consistency) and `EstimationCell`, which replaced the old single-date "Vence" column — it shows `start – due` when both dates are set (falling back to just the due date), editable via a small two-field popover (Inicio/Fin). This reuses the exact `startDate`/`dueDate` fields Timeline already draws bars from (see the Timeline entry above) — setting a duration here is the same duration that shows as a bar there, no separate estimate field invented. `TASK_ROW_GRID` grew to 7 columns to fit.

**Cell editors were extracted into `TaskCells.jsx` and made reusable so the "+ Agregar tarea" row could get every column too, not just a title box.** The user's follow-up on the reference image was specific: the add-row should let you set Descripción/Asignado/Prioridad/Estimación *while creating* the task, matching every real cell in the reference row — not just type a title and fill everything else in after. `PillCell`/`EstimationCell`/`DescriptionCell`/`AssigneeCell` were generalized to take plain values + an `onChange` (no more `task`/`onUpdate` coupling), so `TaskRow.jsx` wires them to `applyUpdate` (writes straight to Firestore) while `ListaView.jsx`'s `InlineAddTask` wires the exact same components to local draft state, only calling `createTask` once when the title is saved. Deliberately **no `onBlur`-triggered submit** on the draft row (unlike the single-field version this replaced) — clicking into any of the other cells blurs the title input, and auto-submitting on that blur would create the task early with whatever partial title existed. Save is Enter-on-title only.

**"Mis tareas" (2026-08-14) — a cross-workstream assignee filter, separate from the workstream selector.** The sidebar previously only let you narrow Workspace to one Intervención/Proyecto at a time; there was no way to see "everything assigned to me" across all of them. `WorkspaceModule.jsx` now holds `onlyMine` alongside `selectedWorkstreamId` — the two are mutually exclusive (selecting a specific workstream turns `onlyMine` off and vice versa, via `selectWorkstream`/`toggleOnlyMine`) rather than combinable, to keep the filter model simple. All three views (Lista/Kanban/Timeline) apply the same `isMine` filter uniformly. Lista/Timeline additionally filter out any workstream left with zero matching tasks (`visibleWorkstreams`), so "Mis tareas" never shows an empty group — and it deliberately does **not** fall through to Lista's own `GENERAL_WORKSTREAM` empty-state card, since inviting "+ Nuevo Proyecto Interno" from inside a personal filter view would be misleading; a dedicated "todo al día" empty state renders instead when the count is zero. The sidebar button shows a live count of open (non-`completado`) tasks assigned to the signed-in user.

**Overdue/due-today tasks now surface in the top bar bell (2026-08-14), the second of three follow-up asks from the same conversation ("Mis tareas" was the first).** `useTaskNotifications.js` follows the exact pattern `useClientNotifications.js` already established (live outside any one module since the bell needs it everywhere, not just while Workspace is open) — subscribes to the signed-in user's own tasks via the existing `subscribeTasksForUser`, flags anything overdue or due today via the same `isOverdue`/`isDueToday` helpers `TaskRow`/`TimelineView` already use, and resolves each task's workstream name by separately subscribing `clients` + `proyectosInternos` and rebuilding the same `client:{id}`/`proyecto:{id}` key shape `lib/workspace.js`'s `workstreamId()` produces (cheaper than pulling in all of `useWorkspaceData`, which also derives Kanban/Timeline-specific groupings this hook doesn't need). `TopBar.jsx` concatenates `useTaskNotifications(user.uid)` ahead of `useClientNotifications()` into one bell list — task deadlines are the more time-sensitive of the two, so they lead.

**Task history (2026-08-14) — the third of the three same-conversation follow-ups.** New `tasks/{id}/history` subcollection, same shape as Clientes' `clients/{id}/history` (§8): `subscribeTaskHistory`/`addTaskHistoryEvent` in `lib/firestore.js` mirror `subscribeClientHistory`/`addHistoryEvent` exactly. Unlike Clientes' Historial tab, there's no manual "log an interaction" form — every entry is auto-generated, because task edits already happen through a small number of known write paths (unlike client interactions, which are real-world events with no natural trigger). The single funnel for that auto-logging is `applyTaskUpdate(taskId, data, actorName)`, new in `lib/firestore.js` — it calls `updateTask` then writes a history line built by `describeTaskChange(data)` (`lib/workspace.js`), which turns a raw patch into a readable summary ("Estado → Completado", "Prioridad → Alta", etc.) by checking which key is present. `createTask` and `toggleTaskComplete` log their own history lines directly rather than going through `applyTaskUpdate`, since "created"/"completed"/"reopened" aren't describable from a data patch alone. **Every task-editing surface now routes through one of these three** — `TaskRow.jsx`'s cells, `TaskDetailPanel.jsx`'s fields, and `KanbanView.jsx`'s drag-and-drop all needed `actorName` threaded in from `WorkspaceModule.jsx` to make this possible, since a history line without an author isn't useful. The Task Detail Panel gained a read-only "Historial" section (`subscribeTaskHistory`) below Asignado, reverse-chronological, matching Clientes' HistorialTab layout minus the input form.

**Zoom levels (Día/Semana/Mes/Trimestre/Año) added right after, same day.** Each level in `TimelineView.jsx`'s `RANGES` map controls three independent things: `pxPerDay` (how zoomed the pixel scale is), `padBefore`/`padAfter` (the default window around today when there's no real task data to size against), and a `ticks()` function — Día/Semana step by calendar day, Mes/Trimestre/Año step by calendar-unit boundary (`startOfMonth`/`startOfQuarter`, stepped via `addMonths`) so labels land on real month/quarter/year lines instead of arbitrary N-day offsets. Real task dates always widen the computed window beyond the granularity's default padding — a task due in 4 months still shows up in Día view, just far down the (scrollable) track, never silently clipped. The board auto-scrolls to center "Hoy" whenever the range or track width changes (a `useEffect` on `scrollRef`), since a track stretched to fit a far-future task would otherwise leave the viewport parked at the far-left edge showing mostly empty space.

**Bug found and fixed while building this: `.ador-grain` was silently breaking every `fixed`-positioned panel that also used it.** `index.css`'s `.ador-grain { position: relative; }` was *unlayered* author CSS, and Tailwind v4's own utilities (`.fixed`, `.absolute`, etc.) live inside a CSS cascade layer — per the Cascade Layers spec, **unlayered rules always beat layered ones**, regardless of source order or specificity. So any element combining `ador-grain` with `fixed` (Task Detail Panel, and the pre-existing Clientes → Ficha `ClientDetailPanel`) was silently forced to `position: relative`, which — for a `fixed inset-0`-backdrop-adjacent slide-in panel sized by `top-0 right-0` — computed to `position: relative` at whatever normal-flow position it fell in, rendering the panel completely off-screen below the viewport instead of sliding in from the right. Confirmed via `getComputedStyle` while wiring up the new Timeline's click-to-open-panel flow. Fixed by wrapping just the `.ador-grain` position rule in `@layer components` so Tailwind's own layered utilities win as expected — see the comment left in `index.css` at that rule. This was a latent, previously-unnoticed bug in `ClientDetailPanel` too, not something introduced by Workspace; worth a quick manual check that Clientes → Ficha still slides in correctly next time it's touched.

### 11. Bug found and fixed across the whole shell (2026-08-15): Chromium drops `backdrop-filter` blur when the same element also has a `transform`

Started as "the notification dropdown looks too transparent" — traced to the *opposite* problem: `NotificationCenter.jsx`'s `motion.div` combined `.ador-glass`'s `backdrop-filter: blur(24px)` with Framer Motion's `animate={{ opacity, y, scale }}`. Chromium has a known bug where an element carrying both `transform` and `backdrop-filter` can stop compositing the blur entirely — confirmed by comparing `getComputedStyle` (backdrop-filter correctly set) against the actual rendered screenshot (background behind it was sharp, not blurred). Critically, this isn't just a mid-animation glitch: Framer Motion leaves an inline `transform: translateY(0px) scale(1)` on the element even at rest once you use explicit `animate={{x/y/scale}}` props (unlike the `layout` prop, which cleans up its transform after a FLIP animation settles) — so the bug persisted indefinitely, not just during the 150ms transition.

**Fix pattern, applied to all 13 affected files:** never combine a transform-animated `motion.div` with a `.ador-glass`/`.ador-modal-surface` class on the *same* element. Split into two nested elements — an outer `motion.div` that owns only the transform/opacity animation, and an inner plain `div` that owns the glass classes and visual content. Affected: `NotificationCenter`, `ProfileMenu`, Sidebar's tooltip, `CellPopover`, all 7 centered modals (`SettingsModal`, `ProfileModal`, `AddExpenseModal`, `AddIncomeModal`, `RegisterDecisionModal`, `NewProyectoModal`, `NewClientModal`), both slide-in panels (`ClientDetailPanel`, `TaskDetailPanel`), and the toast stack (`useToast.jsx`). As a side effect this also fully sidesteps the `.ador-grain`/`.fixed` cascade-layer bug from §10 for the two slide-in panels, since `.ador-grain` no longer shares an element with `.fixed`.

**Overcorrection made and reverted in the same session:** while fixing this, dropdowns/popovers/tooltips were briefly switched from `.ador-glass` to `.ador-modal-surface` (thinking the original "too transparent" complaint meant they needed more opacity). That was wrong — once the blur bug was actually fixed, `.ador-glass`'s normal ~5% tint reads correctly as frosted glass; `.ador-modal-surface`'s ~88% opacity made these elements look like solid black cards, losing the glassmorphism effect entirely. Reverted: `.ador-glass` stays correct for floating dropdowns/menus/tooltips (background should read through, blurred); `.ador-modal-surface` stays correct for centered modals and slide-in panels (deliberately more opaque since they sit above other glass content, per §pre-existing rationale in `index.css`). **If a future floating element looks wrong, check which of these two problems it actually is before changing the opacity class** — a real transform+blur bug looks like "sharp background, no glass effect at all"; the class being merely too transparent looks like "background reads through clearly but text is hard to read against it."

### 12. Objetivos module (2026-08-15) — quarterly goals board, built same session as several other functional additions

Built after the user asked "at a functional level, what would you add to help run the company day-to-day" — analysis concluded Objetivos was the highest-leverage gap: Finanzas measures money, Clientes measures pipeline, Workspace measures execution, but nothing connected any of them to an actual company goal. Lives at `src/components/objetivos/`, wired into `AppShell.jsx`/`TopBar.jsx`'s `objetivos` slot (the tab already existed as a placeholder).

**Two objetivo types, no nested OKR key-results — deliberately flatter than textbook OKRs.** `type: 'kpi'` (numeric, progress bar, one metric) or `type: 'milestone'` (checkbox, no number). Considered a full Objective→multiple-KeyResults nesting first but cut it: at 3-founder scale a flat list of individually measurable goals is just as useful and ships in a fraction of the UI complexity (no nested-array editor, no per-KR CRUD). If ADOR ever wants a real Objective grouping multiple KRs, that's the trigger to revisit — same "don't build for a hypothetical" rule as elsewhere in this project.

**KPI current values are live-derived, never hand-entered — same rule as every cross-module number in this app (§7/§8/§9/§10).** Four live metrics in `lib/objetivos.js`'s `OBJETIVO_METRICS`: `revenue_quarter` (reuses `useFinanceData()`'s existing `recaudadoTrimestre` rather than recomputing it — calling the Finanzas hook from inside `useObjetivosData.js`), `sp_activos`/`spc_pipeline` (live counts off `clients`), `tasks_completadas` (tasks with `status==='completado'` whose `createdAt` falls in the current quarter — a proxy, since tasks have no dedicated `completedAt` field; acceptable simplification, revisit if it ever misleads). A fifth type, `metric: 'custom'`, is the escape hatch for goals with no matching collection (e.g. "Contratar un cuarto asociado") — its `currentValue` is the *only* manually-edited number anywhere in this module, edited inline via the same pencil-icon pattern as Finanzas' Meta del Trimestre.

**Single quarter at a time, same precedent as Finanzas' `quarterlyTarget`.** `useObjetivosData.js` filters the `objetivos` collection down to `quarter === quarterKey()` (today's real quarter, via `lib/finance.js`'s existing helper) client-side — no way to browse past quarters' objectives yet. That's a real limitation, not an oversight; matches the already-written CLAUDE.md §9 rationale ("no historical target tracking yet — that's the trigger to add one if it's ever needed").

**Redesigned same day, right after first shipping, from a detailed OKR-style spec the user pasted** (North Star metric, team-by-focus grouping, single owner, weekly confidence check-ins, experiment log, Notion/Linear-style visual language). Adapted rather than implemented verbatim — see the response given at the time for the full reasoning; short version of what changed and why:

- **North Star Metric isn't a separate config — it's any existing `type:'kpi'` objetivo promoted via a ★ toggle** (`setNorthStar()` in `lib/firestore.js`, a `writeBatch` that atomically un-stars the previous holder so exactly one or zero objetivos ever have `isNorthStar: true`). Rendered at hero scale by `NorthStarHero.jsx` above the regular grid — deliberately the only hero-scale element on the page, so the board has one unmistakable focal point. Rejected a separate "pick your North Star metric" settings field because it would just duplicate an objetivo that (almost always) already exists.
- **"Foco de Impacto" is free text with `<datalist>` suggestions (`FOCO_SUGGESTIONS` in `lib/objetivos.js`), not the fixed 5-department taxonomy from the user's example spec.** ADOR is 3 founders with no formal departments/org chart — hardcoding "Growth/Product", "Sales", "CTO", etc. would be fiction. The board groups objetivos by whatever string the user actually typed, defaulting to "General."
- **Single owner per objetivo** (`ownerId`, resolved against `subscribeUsers()`, shown as an `Avatar` on the card) — straightforward, matches the existing "Asociado responsable" pattern from Clientes (§8).
- **Weekly check-in is 3 fields (confianza semáforo / bloqueo / progreso), and deliberately only stores the *latest* state on the objetivo doc** (`confidence`, `blocker`, `lastCheckinAt`, `lastCheckinBy` — `submitCheckin()` in `lib/firestore.js`), no per-week history subcollection. Same "one target at a time" simplification as `quarterlyTarget` and the check-in confidence colors reuse Workspace's existing priority traffic-light palette (`#EF5350`/`#FFC107`/`#4CAF50`, §10) rather than inventing a new one. `CheckinModal.jsx` follows the same centered-modal pattern as every other modal in the app.
- **Tasks link to an objetivo via a new optional `tasks/{id}.objetivoId` field**, editable from Task Detail Panel (a plain `<select>` listing the current quarter's objetivos). `ObjetivoCard` shows a live count of open linked tasks — computed by filtering the already-subscribed `tasks` array in `useObjetivosData.js`, not stored on the objetivo doc, so it can never drift out of sync with the real task list. This is the "what am I doing this week that moves this goal" connection from the spec, built on top of Workspace's existing task system instead of a parallel Focus Board.
- **Explicitly deferred: the experiment/hypothesis validation log.** It's a genuinely distinct concept (recording *why* a bet did or didn't work, not tracking progress toward a target) that deserves its own scoped design pass rather than being bolted onto this one. Flagged to the user as a clear next step, not silently dropped.
- **Explicitly skipped: Notion/Linear/Slack integration and monospace-everywhere typography.** The integration doesn't apply — ADOR OS already *is* the single unified tool the spec was asking to integrate with. Typography stays Inter (the established design-system font, §3) except KPI numbers, which got `font-mono` for a "data-driven" feel without a wholesale font change — see `NorthStarHero.jsx`'s and `ObjetivoCard.jsx`'s big numbers.

### 13. Same-session functional additions (2026-08-15) — quick fixes across Clientes/Workspace/Finanzas/top bar

Four smaller items built right after Objetivos, from the same "what would help run the company day-to-day" analysis:

- **Clientes gets sequential IDs and a "Perdido" (lost) status.** `ADR-0001`-style codes assigned atomically via a Firestore transaction on `settings/counters.clientSeq` (see `getNextClientCode()` in `lib/firestore.js`) — separate from the client's Firestore doc id, which stays an opaque random string never shown in the UI. "Perdido" is a `lost`/`lostReason`/`lostAt` flag on top of whatever `stage` the client froze at, *not* an 8th `STAGES` entry — the 7 stages are a forward-only Kanban/next-arrow pipeline (§8) and "lost" isn't a next step from anywhere. Marked/restored from the Ficha panel; lost clients disappear from the active Kanban/List and surface in a separate "Perdidos (N)" toggle view (`LostClientsView.jsx`) so the pipeline views don't have to filter them out visually.
- **Workspace gets a team workload panel.** `computeWorkload()` in `lib/workspace.js` — per-associate count of open tasks, with a red highlight above 5 tasks due this week. Rendered in `WorkspaceSidebar.jsx` below the existing nav. First place in Workspace that shows cross-teammate load; "Mis tareas" (§10) only ever showed your own.
- **Finanzas gets a runway projection.** `RunwayCard.jsx` — the one forward-looking card on an otherwise all-actuals dashboard. `cashBalance` is manually entered (no bank integration exists, so this is unavoidable — same "the one figure a founder has to type in" pattern as `quarterlyTarget`) and combined with the average burn rate of the last 3 *completed* months plus any *dated* pending SP payments to project 30/60-day cash. Undated pending payments can't be placed on the timeline and are excluded from the projection (they still show up elsewhere, e.g. Próximo Cobro).
- **Top bar search is real.** Was a disabled decorative input since Phase 2. `useGlobalSearch.js` filters the same live `clients`/`tasks`/`decisions` subscriptions other modules already hold (no server-side text index — Firestore doesn't support substring search without a third-party add-on anyway, and this is fine at 3-founder scale). Selecting a result navigates to the right module *and* opens that record's detail panel: `AppShell.jsx` grew a `focus` state (`{type, id}`) set alongside `activeModule` by a new `navigateTo()` function, consumed by `ClientesModule`/`WorkspaceModule` via `focusClientId`/`focusTaskId` props and cleared via `onFocusHandled` once the panel opens. `onNavigate` throughout the shell (`TopBar`, `Sidebar`, `HomeScreen`'s card clicks) still works unchanged since `navigateTo(moduleId)` with no second argument just clears focus.
- **Home gets a team-wide birthday banner.** `BirthdayBanner.jsx` + `useTodaysBirthdays.js`, reading `users/{uid}.birthday` — a field `ProfileModal.jsx` already captured but nothing read until now. Compares month/day (not year) against today for every user in the `users` directory; shows on Home for the whole team when it's anyone's birthday, plus a matching bell notification. No new Firestore field needed.

### 14. Objetivos restructured into header + main panel + lateral rail, and the experiment log finally built (2026-08-15, same day, third pass on this module)

The user's follow-up feedback on §12's redesign: the North Star hero needs to be a real, always-visible header section (not conditionally rendered only once one exists — it was disappearing entirely on an empty board, reading as "did this even ship"), and two pieces from the original spec were still missing — the Focus Board (§12 called this "linked tasks," a plain count) and the experiment/validation log (explicitly deferred in §12).

**North Star hero (`NorthStarHero.jsx`) now always renders, with a real empty state** ("Sin Métrica Norte definida — marca cualquier meta numérica con la estrella…") instead of returning `null` when nothing is pinned yet. Structurally it's its own section in `ObjetivosModule.jsx`, above a `grid-cols-[1fr_320px]` split — never folded into the objetivo cards below it. This directly answers "where's the header" by making sure there's always something there.

**Layout is now a 3-part shell** — cabecera (title + countdown + North Star) → panel de Objetivos (foco-grouped grid, main column) → rail lateral (320px, fixed width) — deliberately mirroring Workspace's sidebar/main/Decisiones shell (§10) rather than inventing a new layout primitive. The rail holds two stacked panels:

- **`IniciativasPanel.jsx`** — the Focus Board from the spec, built entirely on real Workspace tasks (via `tasks/{id}.objetivoId`, §13) rather than a parallel task list. `useObjetivosData.js` now returns `openLinkedTasks`: every non-completed task tagged to a current-quarter objetivo, enriched with `objetivoTitle` and resolved `assignees`. One place to mark a task done — the panel is read-only, editing still happens in Workspace.
- **`ExperimentosPanel.jsx`** — the Growth/Validation Log, built as its own top-level collection (`experimentos`, `lib/firestore.js`), not a subcollection of `objetivos`: an experiment's link to a KR is optional (`objetivoId` nullable), so many early bets won't have one yet. Each row has a hypothesis (fixed at creation), an editable result (free text, click "+ Resultado"), and a 3-state status (`pendiente`/`validado`/`invalidado` — `EXPERIMENT_STATUSES` in `lib/objetivos.js`, reusing Workspace's priority traffic-light palette for visual consistency). Deliberately a separate concept from Objetivos' confidence check-ins (§12): a check-in tracks "how is this goal trending," an experiment records "did this specific bet pay off" — conflating them would lose the "prueba y falla rápido" framing the spec asked for.

**Bug found and fixed in the same pass: every Objetivos write was silently swallowing errors.** `NewObjetivoModal.jsx` had no `catch` block — a rejected `createObjetivo()` call (e.g. Firestore rules not yet covering `objetivos`, which turned out to be exactly what was happening on the user's own account) just left the modal sitting there with zero feedback, indistinguishable from a broken button. Fixed by applying the same `withTimeout(...).catch(error => showToast(...))` pattern already established in Workspace (§10's "silent write failures" fix) to **every** write in the Objetivos module: `NewObjetivoModal`, `CheckinModal`, and all of `ObjetivoCard`'s inline actions (pencil-edit save, ★ toggle, milestone checkbox, delete) — not just object creation. This is the same class of bug as §10's original incident; worth checking for on any new create-modal added to this app, since the try/finally-without-catch shape is easy to copy-paste forward without noticing the gap.

**Empty-state redesign, quick-start presets.** The bare "Sin objetivos definidos" text was replaced with a proper empty state (pulsing `TargetIcon` in a glass circle, headline, explanatory subtext, prominent CTA) plus three one-click presets (`QUICK_START_PRESETS` in `ObjetivosModule.jsx`) that seed `NewObjetivoModal` via a new `preset` prop — "Ingresos del trimestre," "SP Activos," "Un hito del trimestre" — so a first-time board doesn't require discovering the type toggle or typing a metric name from scratch. Nothing is created until the user actually saves the form; the preset only pre-fills fields.

### 15. Resumen Semanal on Home (2026-08-15) — built after "que tenga lógica, comprensión" feedback

The user asked what else would help run the company operationally, then specifically asked for a weekly summary with real reasoning behind it, not just numbers restated. Lives at `lib/weeklySummary.js` (pure logic) + `hooks/useWeeklySummary.js` (data aggregation) + `components/home/WeeklySummaryCard.jsx`/`WeeklySummaryPanel.jsx` (UI) — a card on Home, click opens a slide-in panel (same portal/transform-split pattern as ClientDetailPanel).

**The "lógica" is a deterministic rule-based synthesizer, not an LLM call.** `buildWeeklyNarrative()` in `lib/weeklySummary.js` takes the week's raw aggregates and produces two things: (1) per-section prose lines that name specific entities ("Bloqueados: Cerrar el trimestre con caja positiva" — the actual objetivo title, not "1 objetivo bloqueado"), and (2) a prioritized TL;DR that picks the 1–2 most urgent signals (blocked objetivos first, then at-risk/warning signals — a stale client, an overloaded teammate, a revenue drop ≥20%), falling back to "Semana tranquila" when nothing warrants attention. This is genuinely the requested "comprensión" — synthesis and prioritization, not a fancier list.

**The one piece of real quantitative reasoning: North Star pace comparison.** `quarterElapsedPct()` computes what fraction of the current quarter has elapsed by calendar date; the summary compares that against the North Star objetivo's own progress percentage and says "al ritmo esperado" or "por debajo del ritmo esperado" accordingly (flagging a TL;DR warning if it's more than 15 points behind). This is the one place in the app that answers "are we on track," not just "what's the number" — everywhere else (Finanzas' Meta del Trimestre, Objetivos' cards) shows raw progress with no pace context.

**Monday–Sunday week, not a rolling 7 days.** `weekRange()` matches how the team already thinks about "esta semana" elsewhere (the Friday check-in, the workload panel's "due this week" count) — a rolling window would disagree with those by a few days depending on what day you open the summary.

**New field: `tasks/{id}.completedAt`.** Tasks previously only had `createdAt`, so "tasks completed this week" had no real timestamp to filter on. `toggleTaskComplete` and `applyTaskUpdate` (in `lib/firestore.js`) now set `completedAt: serverTimestamp()` when status becomes `completado` and clear it back to `null` on reopen — every write path that can complete a task (Lista's status pill, Task Detail Panel, Kanban drag-and-drop) already routes through one of these two functions, so no call site needed to change. Tasks completed before this shipped won't retroactively show up in past weeks' counts — expected, not a gap to fix.

**Data sources are entirely reused, nothing new subscribed for its own sake.** Finanzas numbers come from `useFinanceData()`'s existing `movements`/`clients`; Objetivos confidence/North Star from `useObjetivosData()`; Workspace overdue/workload from `lib/workspace.js`'s existing `isOverdue()`/`computeWorkload()`. The only genuinely new derived data is the week-boundary filtering itself.

### 16. ADOR IA (2026-08-15) — real Gemini chat, and the project's first backend code

The direction question flagged since §12 ("keyword-matched, zero-cost baseline" vs. "pair with a free-tier LLM for real personality") was resolved by the user: real LLM. Lives at `src/components/adoria/`, wired into `AppShell.jsx`'s existing `ador-ia` placeholder slot.

**This required the project's first server-side code — `api/ador-ia.js`, a Vercel serverless function — and that wasn't optional.** Every other piece of ADOR OS talks to Firebase directly from the browser because Firebase Auth/Firestore are designed for that (client SDK + security rules do the enforcement). A raw API key for a third-party service like Gemini has no equivalent client-side protection: a Vite `VITE_`-prefixed env var compiles straight into the shipped JS bundle, so a `VITE_GEMINI_API_KEY` would be readable by anyone who opens devtools on the deployed site. The serverless function exists specifically to hold `GEMINI_API_KEY` (no `VITE_` prefix, so Vite never bundles it) server-side and proxy the request — the browser posts a message to `/api/ador-ia`, never to Gemini directly.

**Model is `gemini-2.5-flash`** (confirmed via web search at build time — Google's free-tier model lineup changes over the years, so if this ever starts erroring, check the current free-tier model name at ai.google.dev before assuming the code broke).

**Context, not fine-tuning or RAG — same "live-derived, never a stale snapshot" rule as every other cross-module number in this app.** `useAdorIAContext.js` calls the exact same hooks Finanzas/Objetivos/Workspace/Clientes already use (`useFinanceData`, `useObjetivosData`, `subscribeAllTasks`, `subscribeClients`) and `lib/adorIA.js`'s `buildAdorIAContext()` formats the result as a compact, labeled plain-text block (not JSON — reads more naturally as part of a system prompt and costs fewer tokens). This block is re-sent as part of the `systemInstruction` on every single message, so the assistant is always answering from the current state of the business, not a conversation-start snapshot. The system prompt (`ADOR_IA_SYSTEM_PROMPT`) explicitly forbids inventing numbers outside that block — a confidently wrong figure from an "assistant" is worse than "I don't have that data" for a firm handling client-sensitive financials.

**Persona is "trusted chief of staff," not a literal Alfred-from-Batman impression** — the user referenced Alfred as a tone anchor (formal, dry wit, unfailingly competent) during the original scoping conversation, but the system prompt describes those traits rather than claiming to *be* the (trademarked) character.

**Known, accepted limitation: no auth check on the endpoint itself.** `api/ador-ia.js` doesn't verify a Firebase ID token — it trusts that the deployed URL isn't public knowledge, the same posture as the rest of this invite-only tool (no public marketing, no search-indexed pages). If this were ever a real concern, the fix is verifying the client's Firebase ID token server-side (needs `firebase-admin` + a service account, deliberately skipped for v1 scope). The actual exposure today is narrow: someone could burn the free daily quota (1,500 req/day) if they somehow found the URL, not leak data — the endpoint never touches Firestore, it only relays whatever text the client already chose to send.

**Conversation state is ephemeral by design** — messages live in `AdorIAModule.jsx`'s local React state only, no Firestore persistence, resets on page reload. Matches this project's repeated "don't build for a hypothetical" pattern; add a `conversations` collection later if the team actually wants to resume threads.

**Verification note for future sessions:** local `npm run dev` (plain Vite, no `vercel dev`) cannot execute `/api/*` serverless functions at all — a request to `/api/ador-ia` there returns Vite's non-JSON fallback, which `AdorIAModule.jsx` catches and surfaces as an explicit "you're in local dev" error rather than a cryptic parse failure. This is expected, not a bug: real end-to-end testing only happens on the deployed Vercel site, once `GEMINI_API_KEY` is set there.

### 17. ADOR IA pivoted to a local rule-based engine (2026-08-16) — §16 above describes code that exists but isn't what's running

The morning after §16 shipped, the user got as far as opening the Gemini API key screen in Google AI Studio (a key already existed: "Default Gemini API Key", Free tier, project `gen-lang-client-0925730709`) and stopped short of adding it to Vercel — the stated reason was fear of an unexpected charge to their card. This was walked through carefully: a Free-tier key has no billing account attached, and Google structurally cannot charge without one being explicitly linked through a separate, deliberate flow (confirmed the mechanism, offered to have the user verify directly in Cloud Console's Billing screen). The user still said no. **This is a standing preference, not a one-time hesitation to re-litigate** — don't re-pitch connecting Gemini in a future session unless the user brings it up first.

Given that, the user asked for ADOR IA to still feel as close to "real AI" as possible without any external call. What got built instead, entirely in `lib/adorIA.js` and `AdorIAModule.jsx`, without touching `api/ador-ia.js` or the system prompt (both left intact, ready to reactivate):

- **`answerLocally(question, data, userName, lastTopic)` replaced the Gemini fetch call entirely** — `AdorIAModule.jsx`'s `send()` no longer calls `/api/ador-ia`; it calls this function directly and gets a reply in the same render tick (with a small artificial `setTimeout` delay so it doesn't feel suspiciously instant). Zero network calls, zero cost, works identically in local dev and production — which also incidentally fixes the "local dev can't run serverless functions" limitation from §16, since there's no serverless function in the live path anymore.
- **`computeSignals(data)` is the actual "reasoning" layer** — cross-references objetivos bloqueados, North Star pace vs. `quarterElapsedPct()` (imported from `lib/weeklySummary.js` rather than reimplemented, so ADOR IA's opinion of what's urgent can never disagree with the Resumen Semanal card's), team overload, overdue tasks, stale clients, and cash runway into one ranked list. This is what powers two new cross-module questions — "¿cómo estamos?" and "¿qué debería priorizar?" — that synthesize across all four modules instead of answering one topic in isolation, which is the main thing that makes this feel like more than a keyword-matched FAQ bot.
- **Conversational memory, in-session only.** `answerLocally()` returns `{ text, topic }`; `AdorIAModule.jsx` threads `topic` through a `lastTopicRef` so a short/pronoun-only follow-up ("¿por qué?", "y eso", "detalla", "explica") resolves against whatever was just discussed (`isFollowUp()`) instead of falling into the generic "no tengo ese dato" fallback — the thing that most broke the "conversation" illusion before this. Each signal also carries a `reason` string specifically for "¿por qué?" follow-ups, so asking why something matters doesn't just repeat the same fact back.
- **Alfred-toned phrasing pools (`pick()` helper)** on openers/closers so repeated questions don't read as copy-pasted template output — this was a direct, explicit request ("que sea como Alfred, sofisticado"), not an incidental style choice.
- **Message cap at 50** (`MAX_MESSAGES` in `AdorIAModule.jsx`) — raised by the user asking how to prevent the chat from "sobrecargándose" with heavy use. Since there's no external API anymore, there was never a cost/rate-limit risk to address (explained this directly) — the only real concern left was unbounded in-memory growth over a very long session. Oldest messages quietly drop past the cap; the first time it triggers, a one-time subtle gray line appears above the thread explaining what happened, so it never reads as a bug to whoever notices the thread doesn't scroll back further.
- **If the user ever changes their mind on Gemini:** the fastest path back is restoring `AdorIAModule.jsx`'s `send()` to `fetch('/api/ador-ia', ...)` (see git history around 2026-08-15 for the exact previous version) — `api/ador-ia.js` and `ADOR_IA_SYSTEM_PROMPT` were never touched by this pivot.

## "Psychology of software" polish already applied (Phase 2)

These came from an explicit design discussion — worth preserving as a pattern, not just one-off features:

- Esc key closes any open panel (notification center, profile dropdown) — global listener in `TopBar.jsx`.
- Greeting subtext rotates through a few phrases per time-of-day, seeded by day-of-year (stable within a day, varies day to day) — avoids the greeting feeling robotic on day 50. See `GreetingBlock.jsx`.
- Profile dropdown shows a real "Última conexión" line from Firebase's actual `user.metadata.lastSignInTime` — a genuine (not fabricated) trust signal, appropriate for an invite-only tool handling sensitive client data.
- Empty states use pulsing dots / breathing icons / shimmer rather than flat static text — see `InterventionsBlock.jsx` (pulsing live-dot next to the header), `ActivityBlock.jsx` (pulsing dot + "todo al día" reassurance copy), `MeetingDecisionBlock.jsx` (icons breathe slowly).

## Known issues / gotchas

- **Git + GitHub done (2026-08-13).** Local repo initialized, initial commit made, pushed to private GitHub repo `ArcAlphaAS/ADOR---OS-Internal-`, `main` tracks `origin/main`.
- **Firestore enabled and wired (2026-08-13/14).** `nam5` region. `useHomeData.js` subscribes Home to live `clients`/`interventions`/`tasks`/`decisions`/`meetings` data via the hooks in `lib/firestore.js`. Security rules require `request.auth != null` AND an `allowedEmails/{email}` document to exist for the signed-in user's email — access control is enforced at the data layer, not just the login screen. The 3 founder emails are the only entries in `allowedEmails` as of 2026-08-14.
- **Google OAuth removed (2026-08-14).** Self-serve Google sign-in auto-created a Firebase Auth account for any Google account — didn't fit the invite-only model. Login is now email/password only; accounts are created by the admin in Firebase Console → Authentication → Users, one at a time, with a temp password the teammate resets on first login.
- **Deployed to Vercel (2026-08-14) — moved to Cloudflare Workers 2026-09-23, see §36.** Was live at `ador-os-internal.vercel.app`, connected to the GitHub repo, auto-deploys on push to `main`. Firebase Hosting was considered but skipped as redundant — Firebase Auth/Firestore don't care what host serves the static build, and the user already manages other projects on Vercel. The `?preview=1` dev-only auth bypass in `App.jsx` is gated behind `import.meta.env.DEV` so it cannot activate in the production build.
- **"Mi Perfil" and "Configuración" wired (2026-08-14).** `ProfileModal.jsx` edits `displayName` via Firebase `updateProfile` (state merged locally into `useAuth`'s `user` since Firebase mutates `auth.currentUser` in place without triggering a re-render). `SettingsModal.jsx` offers "Cambiar contraseña" via the existing password-reset-email flow. Both are centered glass-card modals, portaled to `document.body`, opened via `TopBar`'s `activeModal` state and `ProfileMenu`'s `onSelect` callback.
- **Clientes module built (2026-08-14).** Full SPC→SP Kanban/List CRM — see §8 above for the data model decisions (payments and intervention progress live on the client doc, not separate collections). Home's metrics, Finanzas block, Intervenciones Activas, and the notification bell all now read live from real Clientes data instead of placeholder/manual collections.
- **Firebase Storage not enabled.** The Clientes → Ficha → Documentos tab only stores file metadata (no actual upload) until this gets set up, same console-enable flow as Firestore.
- **Dev server port varies.** Claude's own preview tooling often occupies 5173, so the user's own `npm run dev` sometimes lands on 5174/5180/etc. Not a bug.
- **Claude's own screenshot preview tool has an intermittent quirk** (unrelated to this codebase): after a resize and/or full-page navigation, screenshots sometimes render at a different pixel scale than `window.innerWidth` reports (e.g. `innerWidth` says 677 but the screenshot image is 800px) — elements are actually positioned correctly per `getBoundingClientRect()`, they just *look* shifted/overlapping in the screenshot. Confirmed again 2026-08-15 while verifying the new global search icon appeared to overlap the "Finanzas" tab — it didn't; `getBoundingClientRect()` showed correct non-overlapping positions the whole time. Workaround: stop and restart the preview server, or trust `getBoundingClientRect()`/computed-style/direct `.click()` calls over the screenshot when this happens — don't conclude something is visually broken from a screenshot alone if the coordinates check out.
- **Firestore rules use a blanket wildcard, not a per-collection allowlist — confirmed 2026-08-15 by reading the actual rules the user pasted.** The full rule set is `allowedEmails/{email}` (read-only, controls who's a valid user) plus `match /{document=**} { allow read, write: if isAllowed(); }` — that second rule matches *any* document path at any depth, so `expenses`, `incomes`, `settings`, `proyectosInternos`, `tasks/{id}/history`, `objetivos`, `experimentos`, and anything added in the future are automatically covered with zero console edits required. Every "Action needed: verify rules cover X" note written earlier in this file (§9, §10, §12/§13) was based on a mistaken assumption that rules were collection-scoped — they aren't, and none of those action items were ever real blockers. If a write ever does fail with `permission-denied` going forward, the real cause is almost certainly one of: (a) testing via the dev-only `?preview=1` mock user, which has no real Firebase Auth session and fails `isAllowed()` unconditionally by design (confirmed as the cause of an earlier false alarm tonight on `objetivos`), or (b) the signed-in email genuinely missing from `allowedEmails`.

### 18. Real logo, Finanzas health signals, a spatial-movement pilot, and Workspace → Notas (2026-09-16)

Five separate pieces of feedback in one session, each worth knowing about individually:

**Real logo replaced every text/icon placeholder.** The user supplied the actual brand files (`icono.svg`, `logo-horizontal.svg`, `logo-vertical.svg` — a white-on-navy "Λ" mark plus a custom "ADOR" wordmark). Two derived white assets now live in `public/`: `logo.svg` (the icon alone, used at 80px with a breathing drop-shadow glow on Splash, 28px no-glow in `Sidebar.jsx`, and inline with "OS" text in `TopBar.jsx`) and `logo-wordmark.svg` (just the "ADOR" letterforms, cropped out of the horizontal lockup's path data — `Logo.jsx`, previously a plain Inter-font span with a comment literally saying "swap in the final mark," now renders this image). **Gotcha hit and fixed:** the wordmark's first crop kept the source lockup's original bottom padding, so the image's bottom edge (what CSS `items-baseline` actually aligns) sat well below the letters' true baseline — "ADOR" floated visibly above "OS" wherever they're paired. Fixed by re-cropping the viewBox to end exactly at the letters' baseline (y≈27.45, not the original 34.658). If this wordmark ever needs re-cropping again, remember: baseline alignment cares about the image's *bottom edge*, not its natural whitespace.

**Finanzas gained a "Salud Financiera" strip — signals, not restated totals.** `FinancialHealthCard.jsx`, a full-width strip above the existing metric cards, computes four things `useFinanceData.js` didn't already surface on their own: runway in months (`cashBalance / monthlyBurnRate`, distinct from RunwayCard's 30/60-day cash projections, §13), net margin %, revenue concentration in the single largest client over the trailing 3 months, and overdue collections (pending SP payments whose own date already passed — distinct from `NextPaymentCard`, which only shows what's still coming). Each tile is traffic-light colored (red/amber/green, Workspace's existing priority palette, §10) and the whole card gets a left-border + header dot colored by the worst of the four, so "does anything here need attention" reads without parsing four numbers. Added after direct visual critique that the strip had no header of its own and blended into the metric-card row below it — the fix was a label + status border, not a redesign.

**A "spatial movement" pilot on Clientes' Ficha panel — grow-from-click, not shared-element morph.** User referenced Linear/Notion/Figma/Apple wanting things to feel less like a CRUD app; asked for panels to visually originate from what was clicked. `ClientCard.jsx` now passes its own `getBoundingClientRect()` up through `onOpen`; `ClientDetailPanel.jsx` computes a translate+scale transform from that rect to the panel's resting position (`originTransform()`) and animates between them — the outer transform-only wrapper only, never the `.ador-modal-surface`/`.ador-grain` element itself (§11's blur-bug rule). **Deliberately not a literal Framer Motion `layoutId` shared-element transition** — the card stays visible in the Kanban board behind the panel (it's an overlay, not a navigation), so two simultaneously-mounted elements can't share one `layoutId` without breaking Framer's shared-layout matching; the manual transform computation sidesteps that entirely. Scoped to the Kanban entry point only for this pilot (List/Perdidos rows and global-search opens still get the plain slide-in — `originRect` is just `null` there).

**Real bug found and fixed in the same pass: dragging a Kanban card opened its Ficha panel mid-drag.** Confirmed by directly logging Framer Motion's actual event sequence — it's `dragStart → click → dragEnd`, not `dragEnd → click` as would be the reasonable assumption. A flag set in `onDragEnd` therefore never had a chance to suppress the click; `ClientCard.jsx` now sets it in `onDragStart` and clears it in `onDragEnd` (deferred one tick via `setTimeout`, since the click for that same release can still be in flight right when `onDragEnd` fires). **If a future card/row anywhere combines Framer `drag` with `onClick`, copy this pattern — don't assume the intuitive event order holds.**

**Quick-capture notes — asked for, redirected mid-build, landed in Workspace as a "Notas" tab.** User wanted a digital replacement for jotting things on paper that "understands and categorizes," ideally with live-as-you-type suggestions like Apple Intelligence. Twice confirmed (again) they're not comfortable connecting Gemini even after the billing mechanism was re-explained (§17 already documents this as a standing preference — held again here). Built as the honest zero-cost equivalent: `lib/notes.js`'s `suggestCategory()` (keyword rules — tarea/gasto/ingreso/objetivo/cliente/nota) never blocks a save, it's just a hint attached after the fact. `GlobalCapture.jsx` is a floating "+" button mounted once in `AppShell.jsx` (not per-module) — reachable from any screen, matching the "always have your notebook open" ask; found and fixed a real placement bug where it visually collided with `AdorIAModule`'s docked "Enviar" button (moved from `bottom-7` to `bottom-24`). **First built as a standalone "Conocimiento" module, then moved the same session** — the user clarified Conocimiento should stay reserved for a real future document/knowledge-base module ("mejor que Notion"), and personal/daily jottings belong next to the team's task views instead. Landed as `NotasView.jsx`, a 4th entry in `WorkspaceModule.jsx`'s `VIEWS` array alongside Lista/Kanban/Timeline — content-only component, same shape as the other three, with the page header/subtitle branching on `view === 'notas'`. "+ Crear tarea" (reusing `ListaView.jsx`'s find-or-create "General" Proyecto Interno pattern, exposed as `findOrCreateGeneralProyecto()` in `lib/firestore.js`) is the only conversion actually wired end-to-end; Gasto/Objetivo/Cliente suggestions are informational-only for now — a deliberate scope cut, not an oversight, per this project's "half-finished is worse than absent" rule. New Firestore collection `notes`, already covered by the existing blanket security rule (no console change needed, §15).

### 19. Workspace redesigned around a "Hoy" landing view (2026-09-16) — research-informed, not a cosmetic pass

The user asked for the whole Workspace section to be rebuilt so it actually functions as a daily workplace, not just a feature-complete task database. Researched how Linear (minimalism, keyboard-first, triage-driven) and Sunsama/Akiflow (daily-planning-first, "do fewer things intentionally") approach this — concluded ADOR's 3-founder scale matches the Sunsama model much better than Linear's backlog-triage one: the daily question isn't "what's in the queue," it's "what's mine, and what's late."

**Note on how this shipped:** a concurrent session working in this same repo tried a different fix for the same complaint first (a `todayOnly` sidebar toggle alongside "Personal"/"Equipo" — committed and pushed as `6a160d5`/`237e4d0`). The user was shown both directions and picked this one; the sidebar-toggle version was superseded in the working tree before ever landing as the final design, though its `DecisionesPanel.jsx` copy changes (see the last paragraph below) were independent and kept.

**Workspace now opens on `HoyView.jsx`, not "Todo."** New `hoy` view, first in `VIEWS` in `WorkspaceModule.jsx` and the new default `view` state. Shows exactly two buckets for the signed-in user — Vencidas (overdue) and Para hoy (due today) — nothing else, deliberately no "upcoming this week" noise, matching the same "fewer things, clearly" instinct already used in Resumen Semanal's prioritized TL;DR (§15). Reuses `TaskRow`/`TaskCells` as-is for inline editing consistency; a small workstream tag was added above each row (via a thin wrapper, `HoyTaskRow`) since Hoy mixes tasks from every Intervención/Proyecto and a bare title loses that context. Empty state is deliberately calm/positive (pulsing green dot, "buen momento para adelantar"), not the generic gray "sin datos" pattern — this is the one screen every session starts on, so it needed its own tone.

**"Hoy" used to be a sidebar filter toggle sitting next to "Personal" — promoted to a real tab because a filter is something you have to know to look for, and a landing screen is where you land.** There is no `todayOnly` state in `WorkspaceModule.jsx`/`WorkspaceSidebar.jsx`; "Personal" (everything assigned to me, any workstream, any date) stays as a sidebar filter scoped to the team views (Lista/Kanban/Timeline) since that's a genuinely different question ("what's mine" vs. "what's mine and due"). `WorkspaceSidebar` is now hidden entirely on Hoy (already a fully personal, cross-workstream screen — a team-scoping sidebar next to it was dead space).

**View switcher gained text labels, not just icons.** `WorkspaceModule.jsx`'s pill switcher previously showed four unlabeled icon circles (Lista/Kanban/Timeline/Notas) with only a hover tooltip — not self-explanatory to a non-technical founder on first use. Now every tab shows icon + label; the Hoy tab also carries a small red count badge (overdue + due-today count) when a different tab is active, same visual language as the top-bar notification bell.

**Removed the disabled "+ Nueva Intervención" ghost button from the sidebar** — it read as a broken button to a first-time user with no way to know Intervenciones are auto-created from Clientes (§8). Replaced with a plain explanatory line, shown only when there are currently zero Intervenciones: "Las Intervenciones aparecen aquí solas cuando un SPC pasa a Intervención Activa en Clientes."

**Notas folded into Hoy the same day, right after shipping (direct follow-up: "lo de notas no es igual que hoy? sino que solo quede uno").** The user was right — a standalone "Notas" tab and a "Hoy" tab were both, in practice, "your personal daily space," just split across two clicks for no real reason. `NotasView.jsx` was deleted; `VIEWS` in `WorkspaceModule.jsx` dropped back to four tabs (Hoy/Lista/Kanban/Timeline). `HoyView.jsx` now opens with an always-visible single-line `QuickCapture` input (same `createNote()` + `suggestCategory()` call as `GlobalCapture.jsx`'s floating "+", just in-place instead of a modal) followed by any pending (unreviewed) notes, then Vencidas/Para hoy — capture and triage on one screen instead of two. `GlobalCapture.jsx` (the floating "+" reachable from anywhere in the app, not just Workspace) was deliberately kept as-is — it's still the fastest capture point when you're not already on Hoy — its toast copy was just updated to point at "Workspace → Hoy" instead of the now-gone "Workspace → Notas". Reviewed/archived notes are intentionally not shown on Hoy at all (they're done; Hoy is only about what still needs you), unlike the old `NotasView.jsx` which kept a permanent "Revisadas" section — a real, deliberate scope narrowing, not an oversight. A stray `workspaceView: 'notas'` value saved in an existing user's profile from before this change now falls back to `'hoy'` on load (`WorkspaceModule.jsx` checks the saved id against the current `VIEWS` list) rather than rendering nothing.

**Sidebar gained a "Mi trabajo" section header above "Personal", matching "Equipo" below it (same follow-up conversation).** The direct complaint was that the whole Personal/Equipo/Proyectos structure "no es intuitivo" — on inspection the concrete issue was asymmetry: "Equipo" had a proper section label above its items, "Personal" was a lone unlabeled toggle floating above a divider with no equivalent treatment. Purely a labeling/grouping fix in `WorkspaceSidebar.jsx`, no new filter logic.

**Deliberately did not touch:** Lista's grid-based inline editing, Kanban's drag-and-drop, Timeline's zoom levels, or Notas — all already solid per §10/§14/§18's iteration history. This pass was scoped to navigation/entry-point clarity, not the underlying task-editing mechanics.

**Decisiones panel was separately reframed the same day (kept as-is, not part of the Hoy redesign).** The user's complaint ("debería apuntarse en un software para todos, no tanto de manera independiente") was about *perception*, not the data model — `decisions` was already a single shared Firestore collection with no per-user scoping, visible to everyone the moment `AppShell`'s hierarchy renders `DecisionesPanel`. Header renamed to "Decisiones de Dirección" with an explicit "Registro compartido — visible para todo el equipo" subtitle, and the visible-count cap raised from 3 to 6. **Not done:** it's still a side-panel showing only the newest few, not a full searchable log/history view — a possible real follow-up if the team ever wants to browse further back, not silently dropped.

### 20. Assignment confirmation gate + "Mis Pendientes" in Hoy (2026-09-16, follow-up conversation)

The user asked for two things together: (1) a real place to see/add personal pendientes directly from Hoy, not just notes, and (2) assigning a task to a teammate should never just silently drop it on them — the assignee should have to confirm, and be pointed toward talking to whoever assigned it. Two design questions were resolved with the user before building: confirmation is **blocking** (the task doesn't count as theirs anywhere until they respond), and "comunícate con esta persona" is **just a text hint** naming the assigner, not a real messaging feature — Chat is still a placeholder module, so there's nothing to wire a button to yet.

**Data model: `pendingConfirmations` on `tasks/{id}`, a subset of `assignedTo`.** Adding anyone *other than yourself* to a task's `assignedTo` — at creation (`createTask`) or later (`applyTaskUpdate`) — puts their uid in `pendingConfirmations` too. `applyTaskUpdate` in `lib/firestore.js` changed signature to take the **full task** (not just its id) plus `actorUserId`, because computing "who's newly added" requires diffing against the task's current `assignedTo` — every call site (`TaskRow`, `TaskDetailPanel`, `KanbanView`'s drag-and-drop) had `task` in scope already, so this was a signature change, not new plumbing. Self-assignment never generates a pending entry — accepting your own work needs no confirmation. A `lastAssignedBy` string field (whoever's actorName triggered the most recent pending addition) rides alongside, purely so the confirm popup and history can name a person without a second lookup.

**`isPendingFor(task, uid)` in `lib/workspace.js` is the single source of truth for "doesn't count as theirs yet."** Applied everywhere a task is filtered down to "mine": `WorkspaceModule.jsx`'s `isMine`, `HoyView.jsx`'s `mine`, `useTaskNotifications.js`'s bell filter, `useHomeData.js`'s "Tareas Hoy" block, and `computeWorkload()`'s per-associate counts. A task pending your confirmation is invisible to your Personal filter, Hoy, the bell, and the workload panel — it only becomes "yours" once you accept it. It's still visible on the team's Lista/Kanban (with a visual marker, see below), since the team should be able to see the assignment was attempted.

**`AssignmentConfirmGate.jsx`** — new, mounted once in `AppShell.jsx` alongside `GlobalCapture` (fires regardless of which module is open). Subscribes to `subscribeAssignedPending(uid)` (`where('pendingConfirmations','array-contains', uid)`, new in `lib/firestore.js`) and shows a centered modal for the oldest pending item: who assigned it, which workstream, the task title, a "habla con {nombre}" hint, and two buttons — Aceptar/Rechazar. **Deliberately no close button and no backdrop-dismiss** — this is the "bloqueante" the user asked for; the only way past it is a decision. `respondToAssignment()` (new in `lib/firestore.js`) either just clears the uid from `pendingConfirmations` (accept — stays in `assignedTo`) or clears it from both (reject — task goes back to not being theirs at all), logging either outcome to the task's existing history subcollection. If more than one assignment is pending, a small "+N más esperando" line hints there's a queue, one modal at a time.

**Visual marker for teammates: a small amber ring + dot on the pending avatar in `AvatarStack.jsx`**, plus a "pendiente" tag next to their name in the Asignado picker (`TaskCells.jsx`'s `AssigneeCell`, `TaskDetailPanel.jsx`'s checklist) — so a teammate scanning Lista/Kanban can tell "assigned but not yet confirmed" from "assigned and on it" without opening the task.

**"Mis Pendientes" — a third bucket in `HoyView.jsx`, alongside Vencidas/Para hoy.** Vencidas/Para hoy are strictly date-driven; this new section is everything else open and assigned to you (no date, or a future date) — the place that answers "what do I have on my plate" beyond what's urgent today. It always renders with its own inline "+ Agregar pendiente" row (title, prioridad, fecha opcional, asignados — reusing the same `TaskCells` components as Lista, landing in the "General" Proyecto Interno via `findOrCreateGeneralProyecto`), even when the list is empty, so there's always an obvious way to add something — the page is never a dead end. The old full-page "Nada vencido, nada para hoy" empty state was demoted to a small one-line reassurance (`NadaUrgente`) shown only above Vencidas/Para hoy when both are empty, since Mis Pendientes below it always has something actionable.

**Restored the "+ Crear tarea" note→task conversion that got dropped in the Notas-merge (§19).** `NoteCard` in `HoyView.jsx` re-gained the button (calls the same `findOrCreateGeneralProyecto` + `createTask` pair `NotasView.jsx` used) — an oversight from simplifying the card during the merge, not an intentional cut.

**Verification note for future sessions — the `?preview=1` mock user swaps `AppShell`'s `user` prop for good, but doesn't touch the underlying Firebase Auth session.** While testing this live, the browser tab had a real, already-authenticated session (`angelsamillang@gmail.com`, confirmed via `firebaseLocalStorageDb` in IndexedDB) — so every Firestore read/write succeeded for real even while `?preview=1` was in the URL, because `App.jsx` only swaps the *React* `user` object passed down to `AppShell` (see `PREVIEW_MOCK_USER`, §5), it doesn't sign out or replace `auth.currentUser`. `AssignmentConfirmGate` correctly never fired under `?preview=1` — it explicitly skips `user.uid === 'preview'`, same rule as every other write-gated feature in this app — which looked like a bug until re-loading **without** `?preview=1` (letting the real `user` flow through) showed the popup working exactly as built. If a future session sees a real-data-but-broken-feature mismatch while testing under `?preview=1`, check this first before assuming the code is wrong.

### 21. Personal's empty state and a one-tap reschedule for overdue tasks (2026-09-16, same-day follow-up)

Two more concrete asks from the same conversation, both scoped and shipped together. First: comparing a screenshot of "Todo"'s rich empty-state table (the `GENERAL_WORKSTREAM` fallback from §10) against "Personal" with zero tasks (a bare centered sentence, no table, no way to add a task from there) — the user wanted Personal to "también tenga lo siguiente," i.e. the same real table treatment. Second: for Hoy, "que se vean los pendientes del día anterior... y alguna forma de reorganizarlos" — Vencidas already surfaces yesterday's (and older) overdue tasks, so the missing piece was a *reorganize* action, not visibility.

**Personal's empty state now reuses the exact same `GENERAL_WORKSTREAM` fallback `ListaView.jsx` already had for "Todo," just relabeled.** `ListaView` gained an `emptyLabel` prop (default `'General'`); `WorkspaceModule.jsx` passes `emptyLabel={onlyMine ? 'Personal' : 'General'}`. The dedicated `onlyMine && visibleWorkstreams.length === 0` branch in `WorkspaceModule.jsx` that rendered a bare "Sin tareas asignadas a ti" sentence was deleted entirely — Kanban and Timeline already handled zero workstreams/tasks gracefully on their own (empty status columns, an always-rendered date axis), so the only view that needed the fix was Lista, and it already had the exact right fallback built for a different case. Tasks added through Personal's empty table still land in the real "General" Proyecto Interno underneath (same storage as "Todo"'s fallback) — only the label changes, an accepted, deliberate simplification rather than inventing a genuinely separate "Personal" storage concept.

**One-tap reschedule for Vencidas, Things 3/Apple Reminders-style — a small "→ Hoy" pill next to each overdue task's workstream tag, plus a "Mover todas a hoy" bulk button in the Vencidas section header (shown once there's more than one).** Both call the same `applyTaskUpdate(task, { dueDate: today }, actorUserId, actorName)` used everywhere else — no new write path, just a one-click shortcut around the existing `EstimationCell` date popover. `HoyTaskRow`/`Section` in `HoyView.jsx` gained optional `onReschedule`/`headerAction` props to carry this without forking the shared row/section components. This was the concrete answer to "alguna forma de reorganizarlos" — the alternative (a full drag-and-drop day-planner, or a dedicated "reschedule sheet" like Things 3's) would have been real over-scope for what the ask actually needed.

### 22. HIG-style design review of Workspace (2026-09-16, same day) — two real layout bugs fixed, one flagged finding retracted

The user asked for an honest review "como un Apple software and UI/UX engineer." Reviewed Hoy/Lista live against the user's own real account (read-only — no test writes this time) and found three suspected issues; one didn't hold up under measurement, two did.

**Retracted: the two floating "+" buttons (`GlobalCapture`'s and the collapsed `DecisionesPanel`'s) looked like they overlapped in a screenshot, but `getBoundingClientRect()` showed a real 40px vertical gap between them at the same viewport size.** This is the exact screenshot-vs-`innerWidth` scaling quirk already documented in "Known issues / gotchas" above — re-confirmed here as a live example of why that note exists. No code changed for this one; flagging a real-looking screenshot artifact as a bug would have wasted a fix on something that wasn't broken.

**Real bug #1, fixed: `WorkstreamGroup`'s header row in `ListaView.jsx` let the "Proyecto Interno"/"Intervención" badge wrap mid-word** (confirmed via `getBoundingClientRect()`: badge height jumped from 19px to ~38px, i.e. two lines) whenever the workstream also had a completion-percentage stat (`0/1 · 0%`) competing for the same row width at moderate viewport widths — flexbox's default `min-width: auto` on the un-truncated title span was forcing the *other* siblings to shrink/wrap instead of the title, backwards from what should happen. Fixed by giving the title `min-w-0 flex-1 truncate` (the one element that's fine to ellipsize) and pinning the badge/stat to `flex-shrink-0 whitespace-nowrap` (the two elements that should never wrap). Apple platforms never let a short fixed label wrap mid-word; this was a real, reproducible violation of that, not a subjective style nit.

**Real bug #2, fixed: the collapsed `DecisionesPanel` rail was two bare icons with no visible container** — functional (each had a title/tooltip) but read as orphaned/half-broken rather than deliberately minimal, since nothing signaled "this is the Decisiones module, just collapsed" without hovering first. Wrapped the icon+count in the same `.ador-glass ador-grain` card treatment every other grouped surface in this app already uses, plus a small vertical "DECISIONES" label (`writingMode: vertical-rl`) so it's identifiable at a glance even at 56px wide.

**Methodology note for future reviews:** when a screenshot suggests two elements overlap or a layout looks broken, measure with `getBoundingClientRect()`/`getComputedStyle()` before treating it as a confirmed finding — this project's own "Known issues" section already documented the screenshot-scaling quirk once, and this review is the second time trusting the screenshot alone would have produced a false positive.

**Follow-up the same day: polished `WorkspaceSidebar.jsx` after the user pointed at it specifically ("esta parte se puede elevar").** Ran it through `taste-skill` for a second opinion, adapted for a dense internal tool rather than the marketing pages that skill is written for (Section 13 of that skill explicitly says dashboards are out of scope for its full checklist — only the general consistency/hierarchy principles apply here, not its hero/marquee/bento rules). Three concrete fixes:

- **`NavItem`'s active state now shares `FilterToggle`'s visual grammar** — a colored tint (`${accentColor}20`) + colored, semibold label — instead of a flat white highlight. Before this, "Personal" selected and an Intervención/Proyecto selected read as two different kinds of "selected," even though they answer the same question (what's the current scope). "Todo" (no `accentColor` of its own) still falls back to the neutral white tint.
- **The "Las Intervenciones aparecen aquí solas…" explanatory line got a dashed-border box** instead of floating as bare muted text — reads as an intentional callout now, not leftover filler copy.
- **Workload bars went from a 4px flat-color track to a 3px track tinted to the row's own state color** (faint red for an overloaded associate, faint white otherwise) with a `transition-all` on the fill — a small, deliberate refinement rather than a redesign, since the bar-as-comparison-visual itself is legitimate for a real internal dashboard (the taste-skill's objection to filled-track bars is a marketing-page rule, not a blanket ban).

### 23. iOS-productivity pass: sliding segmented control, Reminders-style icon badges, checkbox pop (2026-09-16, same day)

Direct follow-up ("elevalo, apple, producitivdad IOS") asking specifically for the feel of a native iOS productivity app (Reminders, Things 3), not just general polish. Three changes, all additive motion/visual work — no data model or behavior changes:

- **The Hoy/Lista/Kanban/Timeline view switcher is now a real sliding segmented control**, not just per-button recoloring. `WorkspaceModule.jsx`'s active-tab background is a single `motion.div` with `layoutId="workspace-view-indicator"` — Motion animates it moving between buttons with a spring (`stiffness: 500, damping: 34`) instead of the old instant background swap. This is the single most recognizable "feels native" signature of an iOS segmented control.
- **Reminders-style circular icon badges**, reserved for "smart list" entries only (the same restraint Apple itself uses: Today/Scheduled/All/Flagged get badges, a user's own custom lists just get a colored dot). Added to Hoy's three sections (`SectionIcon` in `HoyView.jsx` — Vencidas/Para hoy get `CalendarIcon`, Mis Pendientes gets `ListViewIcon`, each in a colored circle) and to the sidebar's two "smart" nav rows (`ListIcon` in `WorkspaceSidebar.jsx` — Personal gets `UsersIcon`, Todo gets `BriefcaseIcon`). Regular Intervención/Proyecto rows keep their existing plain dot — giving every row a badge would be noise, not hierarchy.
- **The task checkbox now has the Reminders/Things 3 completion "pop"** — `TaskRow.jsx`'s checkbox is a `motion.button` with `whileTap={{ scale: 0.82 }}` for tactile feedback, and the checkmark/empty-circle swap is wrapped in `AnimatePresence` with a spring scale-in (`stiffness: 500, damping: 22`) instead of an instant icon swap. Checking something off is now a small, deliberate physical-feeling event, which is genuinely the detail that makes those apps feel "alive" rather than a color change.

**Verification note:** a stale `ReferenceError: UsersIcon is not defined` kept appearing in the browser console's error log across multiple full page reloads while verifying this, even though the UI rendered correctly every time (icon badges visible, no crash) and the Vite dev server itself reported zero compile errors. Treated as a leftover console log-history entry from mid-edit HMR, not a live bug — confirmed by the app never actually breaking. If this resurfaces and the UI *does* look broken next time, don't assume it's the same false alarm — recheck the actual import.

### 24. "Salud" pill and a scrubber-dot progress bar on Lista's workstream headers (2026-09-17)

The user shared two reference images — a dark widget with a clear "X of Y tasks done" progress bar with a scrubber dot, and a light project-pipeline table with colored "Schedule Health"/"Budget Health" pill columns — and asked which pieces were worth pulling in, explicitly flagging to be careful about it at the design level rather than copying either literally. Per this project's established pattern (§9's "adopted the reference's shapes, not its literal colors"), extracted the underlying ideas rather than the light theme or the orange accent.

**New `workstreamHealth(tasks)` in `lib/workspace.js`** — a colored pill ("En tiempo" green / "Atrasado" red) shown in `ListaView.jsx`'s `WorkstreamGroup` header, next to the existing Intervención/Proyecto Interno badge. Computed the same way every other cross-module status in this app is (§7 and everywhere since): live, from real data, never a manually-set field. It's `true` the moment the group has any open task past its due date, `null` when the group has zero tasks (no badge shown at all, rather than a meaningless "on track" default).

**Replaced the old "0/1 · 0%" progress text with "1 de 1 completadas"** — plainer, matches the widget reference's clarity ("1 OF 5 TASKS DONE") better than the compact-but-cryptic fraction-and-percent format.

**Added a scrubber dot to the workstream progress bar** (a small circle riding at the fill's edge, ringed in the page's near-black to read as "cut into" the bar) — the one purely visual borrow from the widget reference, since it's what makes a flat fill bar read as "a position on a track" instead of just a percentage.

**Removed a real, previously-unnoticed bug found while doing this: the "En curso" badge under an expanded Intervención was a hardcoded string, not a real status** — it always said "En curso" regardless of whether the Intervención was actually on track or badly behind. Deleted it; the new Salud pill in the header carries the real, live status now, so there's no longer a fake status sitting a few pixels below a true one.

**Verification note:** no real overdue tasks existed in the signed-in account at review time, so the "Atrasado" (red) branch couldn't be visually confirmed against live data — verified instead by calling `workstreamHealth()` directly from the browser console with constructed task objects (overdue, future-dated, and completed-but-overdue combinations), confirming all four branches return the expected label/color/null. If this is touched again, prefer visual confirmation with a real overdue task when one exists.

### 25. Sidebar's "Todo" renamed three times — landed on "Grupo" (2026-09-17)

Direct feedback: "Todo" (Spanish for "everything") reads too easily as the English "to-do" in a task-management app — genuinely ambiguous, not a style nit. Went through "Panorama" (didn't read as "this is the team's work" clearly enough) and "Trabajo" (still not quite it) before landing on **"Grupo"** — the user's own suggestion. Works well for two reasons: it pairs cleanly with "Personal" right above it (Personal vs. Grupo — yours vs. the team's, same one-word register, same sentence pattern), and it sits naturally under the existing "EQUIPO" section header without repeating it. Deliberately never "Proyectos" through any of these attempts — an Intervención is never called a "proyecto" in ADOR's own vocabulary (§8: SPC/SP terminology), so that label would have mislabeled half of what this view actually shows (Intervenciones + Proyectos Internos combined), not just been imprecise. No icon-library glyph change needed (kept `BriefcaseIcon`) across any of the three renames; no other copy in the app referenced any of the old labels.

**Same-day follow-up: the page title next to these views said "Workspace" for the team scope, which the user flagged as its own confusion** — "Workspace" is the whole section's name (covers Hoy/Personal/Grupo alike), so reusing it as a page title *inside* the section, only for one specific scope, said nothing about what you were actually looking at. `WorkspaceModule.jsx`'s `title` now reads **"Proyectos Individuales"** for the Personal scope and **"Proyectos Colaborativos"** for the team/Grupo scope, replacing the old `'Personal' : 'Workspace'` fallback. The sidebar's short labels ("Personal"/"Grupo") were left as-is — this was a page-header clarity fix, not another round of the sidebar renames above.

### 26. Hoy redesigned from a reference image — header, "Enfoque actual," compact rows, right rail (2026-09-17)

The user shared a reference screenshot of a Sunsama/Linear-style "Today" screen and asked for Hoy to look like it. Before building, four scope questions were asked and answered directly (not assumed):

- **Navigation stayed as-is.** The reference's own sidebar (Workspace/Proyectos/Clientes/Conocimiento/Comunicación/Finanzas/Equipo) was *not* adopted — Clientes/Finanzas stay on the top bar per §2's already-deliberate split. Only Hoy's own content was redesigned.
- **"Enfoque actual" is a highlight card, not a timer.** `pickFocusTask(vencidas, hoy, pendientes)` in `lib/workspace.js` picks the single most urgent open task (oldest overdue first, then today's highest-priority, then backlog highest-priority) and "Iniciar enfoque" just opens its Task Detail Panel — no pomodoro/session-tracking state was built, confirmed out of scope before starting.
- **No per-task duration-in-minutes field.** The reference showed "45 min"/"20 min" per row; skipped per the project's own "don't build for a hypothetical" rule — there's no existing estimate concept to derive it from, and no editing UI was requested.
- **The right rail was built in full, minus keyboard shortcuts** (explicitly dropped per feedback): `HoyRightRail.jsx` holds `MiniCalendar` (a navigable month widget, no real events since Calendario is still a placeholder), `ProgressDonut` (same completed/total numbers as the header's stats line, just a ring), `ObjetivoSemanaCard` (reframes the existing North Star Objetivo — same `quarterElapsedPct()` pace comparison Resumen Semanal already does, not a separate hand-tracked weekly goal; real empty state when no North Star is pinned), and `QuickActionsCard` (functional, no shortcut badges: "Nueva tarea" opens Mis Pendientes' inline add and scrolls to it, "Nueva nota" focuses the in-page capture input, "Ver calendario" navigates via a new `onNavigate` prop threaded from `AppShell.jsx` → `WorkspaceModule.jsx` → `HoyView.jsx`).

**Rows became compact checklist items instead of Lista's full editable grid.** `CompactTaskRow` (replacing the old `HoyTaskRow` wrapper around `TaskRow`) shows a checkbox, title, a small workstream/reschedule line, and either a priority flag (`FlagIcon`, new in `icons.jsx`) or — for the new **"Completado" section** — the completion time. Editing beyond checking a task off now happens only in the Task Detail Panel (opened by clicking the row), a deliberate reduction from Hoy's previous per-cell inline editing: the reference's appeal was scannability, and Lista still has full inline editing for anyone who needs it. This is intentionally the one Hoy-specific divergence from "reuse TaskRow as-is" (§19) — a judgment call, not an oversight.

**The existing Vencidas/Para hoy/Mis Pendientes split was kept, not collapsed into one generic list**, even though the reference shows a single "Hoy" group — that three-way split was itself a deliberate, debated decision (§19–21) answering three different questions, and the reference's flat list didn't have an equivalent reason to unify them. Each section gained a collapse chevron (local state, doesn't persist) matching the reference's fold/unfold headers.

**New in `lib/workspace.js`:** `isCompletedToday()` (same date-comparison shape as `isDueToday()`, checked against `completedAt` instead of `dueDate` — powers "Completado"), `pickFocusTask()`, and `dailyQuote()` (day-of-year-seeded rotating quote line in the header, same stable-per-day pattern as `GreetingBlock.jsx`'s subtext).

**`WorkspaceModule.jsx`'s generic page title/subtitle is now hidden specifically on the Hoy view** — it would otherwise render a second, redundant "Hoy" heading directly above `HoyView`'s own richer header (date, quote, stats). Every other view (Lista/Kanban/Timeline) still uses it.

**Verification note:** tested via `?preview=1` — layout, header, calendar, sections, and all three Quick Actions (including the "→ Ver calendario" navigation and the scroll-to-and-open behaviors for "Nueva tarea"/"Nueva nota") confirmed working visually. Writing an actual task failed with `permission-denied`, which is the already-documented `?preview=1` limitation (no real Firebase Auth session, not a code bug — see §20's verification note and "Known issues" in `PROJECT_STATE.md`) — not re-tested against a real signed-in account in this session.

**Same-day follow-up (four more changes, all direct feedback on the first pass above):**

- **The mini calendar was narrowed from a full month to a single week row.** The reference image's own calendar widget only showed one visible week — `MiniCalendar` in `HoyRightRail.jsx` now renders exactly that (7 days, Monday-first via `weekRange()`, prev/next shifts by a week instead of a month) rather than a full month grid.
- **Decisiones moved out of Workspace entirely, into Objetivos.** It was a fixed, always-on 56/280px side rail in `WorkspaceModule.jsx` that every Workspace view had to account for (and that Hoy's new right rail made especially cramped). Direct feedback: "las decisiones grandes van con las metas grandes." `DecisionesPanel.jsx` (the collapsible side-rail component) was deleted; a new `DecisionesCard.jsx` — a plain `.ador-glass` card, same shape as `IniciativasPanel`/`ExperimentosPanel` — now lives in Objetivos' own lateral rail (`ObjetivosModule.jsx`). `RegisterDecisionModal.jsx` moved from `components/workspace/` to `components/objetivos/` alongside it (it only ever needed `workstreams`/`actorName`/`onClose`, nothing Workspace-specific — Objetivos passes `workstreams={[]}` since it has no client/proyecto context of its own, so the modal's optional "Vinculada a" field just shows "Ninguna" there). No data-model change — `decisions` was already one shared, unscoped collection.
- **"Objetivo de la semana" was rebuilt as a genuinely personal card, not a relabeled North Star.** Direct correction: the North Star Objetivo is a company-wide quarterly goal (Objetivos module); Hoy's version needed to be about the individual signed-in person instead — a real, distinct concept, not two names for the same thing. Rebuilt in `HoyRightRail.jsx`: you write your own one-line weekly intention (stored on `users/{uid}.weeklyGoal = {title, weekKey}`, `weekKey` = that week's Monday — so an unset-this-week check is just "does the stored weekKey match today's," same "one target at a time" pattern as Finanzas' `quarterlyTarget`), edited inline via a pencil icon. The progress bar underneath is never hand-ticked — it's `completedThisWeek / (completedThisWeek + openTasksDueThisWeek)` computed live from the signed-in user's own tasks (`weekRange()` from `lib/weeklySummary.js`), matching this app's blanket rule against manually-tracked progress wherever a real number can be derived instead. Shows a neutral "sin tareas con fecha esta semana" state rather than a fake 0% when there's nothing to compute from.
- **"Enfoque actual" now always renders, even with nothing urgent.** It used to return `null` and disappear entirely when `pickFocusTask()` found nothing. Given the same "a landing screen shouldn't just vanish" precedent already established for `NorthStarHero.jsx` (§14), it now shows a calm positive state instead ("Nada urgente en este momento — buen momento para avanzar algo de Mis Pendientes").

**Second same-day follow-up: priority (urgencia) became editable directly from every open row.** Direct ask — the compact rows (Vencidas/Para hoy/Mis Pendientes) previously only *displayed* priority as static text (an intentional simplification made when Hoy's rows were redesigned to be lighter than Lista's full grid, see above); the user wanted to be able to set it themselves without opening the Task Detail Panel. `CompactTaskRow`'s static priority flag was swapped for the exact same `PillCell` popover Lista/`TaskRow.jsx` already use — no new component, just reused. `Section`/`CompactTaskRow` gained an `actorUserId` prop (threaded down from `HoyView`) since editing now writes through `applyTaskUpdate(task, { priority }, actorUserId, actorName)`, same pattern as every other inline edit in the app. "Completado" keeps showing a completion time in that slot instead, since a finished task's priority isn't something you'd change from the list.

### 27. Calendario module (2026-09-17) — read-only Google Calendar reflection, second server-side integration

The 2026-08-14 "no urgency, team uses Google Calendar directly" deferral (§ next-steps below) was revisited by direct user request — not to replace Google Calendar, but to mirror it inside ADOR OS. Two scope questions were resolved with the user before building, same as every other multi-decision feature in this project:

- **Personal only, not a combined team view.** Each founder connects their own Google account independently; there's no merged "everyone's schedule in one place" view. This isn't a limitation to revisit later — the user pointed out Google Calendar itself already merges invited events into each person's own calendar (if Mateo invites Ángel, it already shows up on Ángel's calendar), so a per-person connection is genuinely sufficient, not a corner cut.
- **Read-only, not read/write — and for a real technical reason, not just less to build.** The user initially wanted read/write ("ver, agendar, escribir, borrar"), but full calendar access is a Google-classified **restricted** scope: getting a restricted-scope app out of "Testing" publishing status (needed for tokens to stop expiring every 7 days) requires Google's heavier verification process (security assessment included) — realistically never worth it for a 3-person internal tool. Read-only (`calendar.readonly`) is classified **sensitive**, not restricted, which keeps open a real, much lighter path to a persistent connection later. This was explained directly and the user chose read-only on that basis, not just to save build time.

**Second server-side integration in the project, same minimal-footprint pattern as ADOR IA/Gemini (§16).** A raw OAuth client secret can never reach the browser, so exactly two things route through new Vercel serverless functions — `api/google-calendar/exchange.js` (trades an authorization code for tokens) and `api/google-calendar/refresh.js` (trades a stored refresh token for a fresh access token). Everything else — listing events — happens directly from the browser against `googleapis.com/calendar/v3/...` with the resulting access token in an `Authorization: Bearer` header; Google's Calendar API accepts that cross-origin, so no proxy is needed for reads. New env vars: `VITE_GOOGLE_CLIENT_ID` (not secret, fine in the bundle) and `GOOGLE_CLIENT_SECRET` (server-only, same `VITE_`-prefix discipline as `GEMINI_API_KEY`).

**Deliberately no Firebase Admin/service account added for this.** The exchange/refresh functions don't touch Firestore at all — they just return tokens. The client (already signed in, already satisfying the existing `allowedEmails` Firestore rule) writes its own refresh token to its own `users/{uid}.googleCalendar` doc directly via the normal client SDK. This sidesteps needing new server-side Firestore infrastructure just for one field, at the cost of the same accepted tradeoff already documented for ador-ia.js (§16): the refresh token sits under this app's one blanket rule, so any of the 3 founders could technically read another's stored token via the Firestore console. Same trust posture already accepted elsewhere in this 3-person tool — not a new category of risk.

**The redirect URI is the app's own bare origin, not a dedicated callback route.** Google redirects back to `https://ador-os-internal.vercel.app/?code=...`; `useGoogleCalendar.js` picks the `code` param off `window.location` on mount, exchanges it, then strips it from the URL via `history.replaceState`. Simpler than standing up a dedicated `/auth/callback` page for one query param.

**Access tokens live in memory only** (a hook-level ref, never persisted) and are refreshed on demand — only the long-lived refresh token is stored. `prompt=consent` is forced on every connect so Google reliably re-issues a refresh token even on a second connect from the same account (Google only issues one on the *first* consent by default, which would otherwise silently break reconnecting).

**The 7-day Testing-mode token expiry is surfaced as an honest, calm UI state, not a generic error.** `isReconnectError()` in `lib/googleCalendar.js` detects Google's `invalid_grant` response specifically; `CalendarioModule.jsx`'s `needsReconnect` state says "tu conexión venció... es normal, no un error" rather than a scary failure message, and clears the stale token from Firestore so the UI doesn't get stuck claiming to be connected.

**Cloud Console setup completed and confirmed working live (2026-09-17), same session.** Walked through end-to-end with the user: enabled the Calendar API on the `ador-os-internal` Google Cloud project (same project backing Firebase — already existed, wasn't obvious from its Firebase display name), added the `calendar.readonly` scope in the new "Google Auth Platform" UI (Google's consent-screen tooling was redesigned since §16/§17 — it's now Branding/Audience/Clients/Data Access as separate pages, not one form), and created a Web application OAuth Client ID with `https://ador-os-internal.vercel.app/` as the authorized redirect URI. Real discoveries made during this walkthrough, worth knowing for next time:

- **The project's OAuth consent screen was already "In production," not "Testing"** — a leftover from the since-removed Google Sign-In login provider (§"Google OAuth removed"). This is good news, not a problem: the 7-day Testing-mode token expiry this section originally worried about **does not apply here** — tokens issued under this app should behave like a normal production app's, not expire weekly. Confirm this holds in practice over the coming weeks; if reconnects start happening unexpectedly often, that's the thing to revisit.
- **`VITE_GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` are set in both `.env` (local, gitignored) and Vercel's Production environment.**
- **Real bug hit and fixed: the Client Secret got corrupted by one dropped character when pasted into Vercel** (`GOCSPX-...` became `OCSPX-...`, missing the leading `G`) — Google's token endpoint correctly rejected it with "The provided client secret is invalid," which was the right signal to go re-check the value character-by-character rather than assume the integration itself was broken. Caught and fixed live; worth remembering if a future secret-paste-into-a-dashboard step produces the same class of error — check for a dropped/mangled character before assuming deeper problems.
- **A Vercel env var change requires a fresh deployment to take effect** — editing the value alone doesn't touch already-built deployments. A `git push` (even an empty commit) triggers one automatically via the GitHub integration, same as clicking "Redeploy" in the dashboard.
- **Confirmed working end-to-end** with a real founder account: the Google consent screen showed the expected "Google hasn't verified this app" warning (click "Advanced" → "Go to ADOR OS (unsafe)" — expected, not a bug, per this section's own earlier note), and after accepting the read-only permission, Calendario correctly displayed real upcoming events.

**Redesigned from a reference image into a real Día/Semana/Mes/Agenda calendar, same session, right after the connection was confirmed live.** The first pass (a plain agenda list) was always meant to be a placeholder — see the "design pass deferred on purpose" note originally on this section. New files: `CalendarioGrid.jsx` (hour-row × day-column grid, shared by Día and Semana — just a different `days` array; a simple greedy column-packing handles overlapping events, not pixel-perfect interval scheduling but visually honest), `MonthGrid.jsx`, and `CalendarioRightRail.jsx` (a navigable mini month — a separate component from Hoy's week-strip `MiniCalendar` in `HoyRightRail.jsx`, since Calendario's own page has room and reason for a full month; a "Hoy" events card; and a real "Tareas de hoy" card pulling the signed-in user's actual Workspace tasks via the already-existing `subscribeTasksForUser`).

**Two pieces from the reference were deliberately not built as real actions, decided with the user before starting:**
- **"Nuevo evento" stays visible but disabled**, with a tooltip explaining why — creating events needs Google's read/write scope, which this app intentionally doesn't have (see the read-only-vs-restricted-scope reasoning above). Matches this project's existing "Documentos download button, present but disabled" pattern (§8) rather than hiding the control entirely.
- **The reference's "Enlaces rápidos" quick-link grid (Programar reunión / Bloque de tiempo / Calendarios) was dropped**, not built as dead buttons — same "nothing decorative, everything earns its place" rule as everywhere else in this app (§10).

**Events are colored by their real Google Calendar color** (`GOOGLE_EVENT_COLORS` in `lib/googleCalendar.js`, Google's own stable 11-color `colorId` palette, hardcoded rather than fetched from `colors().get()` to avoid an extra round-trip) — falling back to ADOR's blue for events with no per-event color override. This is what makes the grid an honest reflection rather than an invented category system.

**`useGoogleCalendar.js` changed from a fixed "next 14 days" fetch to a `loadRange({start, end})` the module calls whenever the visible view or date changes** (switching Día/Semana/Mes, paging forward/back, or jumping to a day from the mini calendar/month grid) — the hook still owns the whole OAuth/token lifecycle, `loadRange` just replaced the old hardcoded window with whatever range the UI is currently showing.

**Verification note:** passes a clean build and was checked visually via `?preview=1` (header, disabled "Nuevo evento" with its tooltip, the connect empty-state) — the grid/month/agenda views themselves render real events only against a real Google connection, which `?preview=1` can't provide (no real Firebase Auth session, same limitation noted elsewhere in this file). Confirmed with real events shortly after by the user — grid, colors, and layout all looked right.

**A generic "typical energy" curve was added to the Día view, then removed the same session.** After confirming the grid worked, the user asked for something like a circadian-rhythm-aware layer (referencing an app called "Exo" — an energy curve, buffer/rest zones between meetings, an AI-suggestions panel). Scoped down before building, given this app's own rule against ever showing a number that looks measured/personal when it isn't (no biometric/sleep data source exists here): built as `lib/energyCurve.js`, a generic, clearly-labeled "typical pattern" curve (common chronobiology — mid-morning peak, post-lunch trough, smaller afternoon rise) with two highlighted "Pico de energía" windows, explicitly *not* claimed as personal data. Buffer/rest zones and AI suggestions were dropped before building (the user wasn't sure about the former, and asked to drop the latter outright). Shipped, then pulled back out entirely on direct follow-up ("mejor por ahora quitamos lo de los picos") — `energyCurve.js` deleted, `CalendarioGrid.jsx` reverted to its plain hour-grid. Worth remembering if this comes back up: the generic-curve approach (not fabricating personal data) was the right call and got no pushback — the feature itself just didn't earn its place yet, not a data-honesty objection.

### 28. "Personal" redesigned into a real dashboard, `PersonalOverview.jsx` (2026-09-17)

The user shared a reference image (stats row, "Mis proyectos" cards, a tabbed task table, a right rail with Enfoque actual/Progreso semanal/Calendario de hoy/Acciones rápidas) asking for Workspace's Personal scope to look like it, instead of the plain filtered `ListaView` table. Wired in as a swap-in: `WorkspaceModule.jsx` now renders `PersonalOverview` instead of `ListaView` specifically when `view === 'lista' && onlyMine` — Kanban/Timeline still just filter as before, unchanged.

**Two trims held, one reversed on same-day follow-up feedback:**
- **No per-task duration field** — same rule as everywhere else in this app; Estimación is a date range, not minutes, and there's nothing to derive a fabricated duration from. Held.
- **"Plantillas" dropped from Acciones rápidas** — no templates feature exists anywhere in this app. Held.
- **"Calendario de hoy" and the rest of "Acciones rápidas" (Nueva tarea/Nueva nota) were cut in the first pass, then added back on direct feedback.** "Calendario de hoy" runs its own `useGoogleCalendar(user.uid)` call — a second independent connection alongside Calendario's own page's instance. The first-pass concern (two instances separately refreshing tokens) was real but overweighted — accepted as fine on reflection since it's read-only and Google's quota is generous, not the fragility risk it first looked like. "Nueva nota" navigates to Workspace → Hoy (a new `onGoToHoy` callback threaded from `WorkspaceModule.jsx`) since that's where `QuickCapture` actually lives — there's no external API to open `GlobalCapture.jsx`'s floating "+" from outside itself, and building a second capture UI here would be redundant, not a real addition. "Nueva tarea" switches to the Hoy tab of the task table and opens the inline add row (`quickNewTask()`).

**Everything shown is real, live-derived data, same blanket rule as every cross-module number in this app:** stats (active projects, pending tasks, completed this week), "Mis proyectos" cards (only workstreams where you have ≥1 task, progress computed from *your* tasks in that workstream, not team-wide), the 4-tab task table (Hoy = overdue + due today, Próximas = future-dated, Sin fecha, Completadas), "Enfoque actual" (reuses the exact same `pickFocusTask()` HoyView.jsx already uses — deliberately the same signal in both places, not a second competing concept), and "Progreso semanal" (a week-scoped donut, distinct metric from Hoy's day-scoped stats, not a duplicate).

**Confirmed with the user: tasks with no real project (landing in the synthetic "General" workstream, same fallback `ListaView.jsx` already used) are expected and already handled** — "Mis proyectos" only lists workstreams with ≥1 of your tasks, so a task with no real project just doesn't get a project card; it still shows up correctly in "Mis tareas" with "General" as its project column.

**"Ver todos →" on Mis proyectos calls the existing `onToggleOnlyMine`** — switches to Grupo scope, showing every workstream — a real navigation action, not a dead link.

**Verification note:** checked via `?preview=1` — layout, stats, empty Enfoque actual/Progreso semanal states, the "Calendario de hoy" not-connected state, the "Acciones rápidas" quick actions (confirmed "Nueva tarea" correctly opens the inline add row on the Hoy tab), and the inline "+ Agregar tarea" row all render/function correctly with zero data. Not yet tested with real tasks/projects/a connected calendar populated.

### 29. Unified task table across Hoy, Personal, and Grupo — `ProjectTaskRow.jsx` (2026-09-17, same-day follow-up)

Direct follow-up on §28: the user asked for Personal's table to match Grupo's exact column set (Tarea/Descripción/Asignado/Prioridad/Estimación/Estado) plus a Proyecto column (since Personal, unlike Grupo, isn't grouped by workstream), and — after a clarifying question about whether that should also apply to Hoy — confirmed "mismo formato para todos... pero para hoy que tenga matices" (same format for everyone, but Hoy keeps its own nuances). This reverses part of §26's "Hoy is lighter than Lista" simplification: full inline-editable columns are now the standard everywhere a task appears, not just Grupo/Lista.

**New shared component, `ProjectTaskRow.jsx`** — the same shape as `TaskRow.jsx` (Grupo/Lista's row: checkbox, Tarea, Descripción, Asignado, Prioridad, Estimación, Estado) plus a Proyecto column, via a new `PROJECT_TASK_ROW_GRID` constant in `lib/workspace.js`. Used by both `PersonalOverview.jsx` (replacing the bespoke `PersonalTaskRow` from §28) and `HoyView.jsx` (replacing `CompactTaskRow`). `TaskRow.jsx`/`TASK_ROW_GRID` stay as-is for Grupo/Lista — a project column there would just repeat the `WorkstreamGroup` header already showing it.

**Hoy's "matices" — what stayed different, not reverted:** the Vencidas/Para hoy/Mis Pendientes/Completado section split (§19-21's deliberate IA, answering four different questions) is unchanged; each section still gets its own collapse chevron and column-header row. Vencidas' "→ Hoy" reschedule pill survived as an optional `onReschedule` prop on `ProjectTaskRow`, rendered inline next to the Proyecto badge instead of stacked under the title (no room for that in a fixed-width Proyecto cell). The "Completado" section's old `timeLabelFor` (showing a completion time instead of the priority pill) was dropped — the Estado column already says "Completado" now that every section uses the same full row, so a second completion indicator would be redundant, matching how Grupo's completed tasks already work.

**"Mis Pendientes"/Vencidas/Para hoy's inline add row (`AddPendiente`) and Personal's (`AddTaskRow`) both grew to match** — title, a workstream `<select>` (new for Hoy; Personal already had one), `DescriptionCell`, `AssigneeCell`, priority, and (Personal only, unchanged) due date. `HoyView.jsx` gained a `workstreams` prop (threaded from `WorkspaceModule.jsx`) since it previously only received `workstreamById`, not the full array a `<select>` needs.

**Analyzed and expanded "Acciones rápidas" on Personal per the user's own request to make it genuinely useful for this page**, not just port the reference's two buttons: added "Nuevo Proyecto Interno" (reuses `WorkspaceModule.jsx`'s existing `NewProyectoModal` via a new `onNewProyecto` prop) — a real, directly relevant action for a page organized around "my projects" that the reference didn't include but the page's own content suggested.

**Verification note:** checked via `?preview=1` — Hoy's "+ Agregar pendiente" now shows all 7 fields (título/proyecto/descripción/asignado/prioridad/fecha) and saves correctly against the write path; Personal's table shows the new Proyecto/Descripción/Asignado columns with real headers; Grupo/Lista confirmed unchanged. Not yet tested with real populated data in either table.

### 30. Directorio module (2026-09-18) — the sixth real module, first built from two reference images at once

Built from two reference screenshots the user shared, with explicit instructions on how to combine them rather than replicating either literally: base layout from the first image (Dirección big-cards + Equipo table + right-column Áreas/Equipos/Cultura sidebar), the stats row (Personas/Áreas/Equipos/Roles asignados) from the second, and the second image's click-to-select person detail panel — but deliberately truncated at "Áreas de responsabilidad," cutting "Reporta a"/"Trabaja con"/"Proyectos activos"/"Objetivos" entirely per direct instruction. Lives at `src/components/directorio/`, wired into `AppShell.jsx`'s existing `directorio` sidebar slot (previously a placeholder).

**New collections, deliberately decoupled from `/users/{uid}`.** `directoryPeople` and `directoryTeams` (`lib/firestore.js`) are informational profiles for anyone at ADOR, not tied to a real Firebase Auth account — `/users/{uid}` is one row per actual login (self-registered on sign-in, §8), while the reference images show ~12 people for a firm with 3 founders' worth of real accounts. Conflating the two would have forced either fabricating fake Auth accounts for non-users or silently capping the directory at 3 people. Both new collections are automatically covered by the existing blanket Firestore rule (§Infrastructure notes) — no console change needed, confirmed by the same reasoning already established for every prior collection.

**Photos reuse ProfileModal's existing `photoDataUrl` pattern, not Firebase Storage.** `resizeImageToDataUrl` (`lib/image.js`) was already built for profile photos specifically to avoid Storage's Blaze-plan requirement (see that file's own header comment) — `AddPersonModal.jsx` calls the exact same function, no new infrastructure needed.

**"Dirección" is a person-level flag (`isDirectivo`), not a value of `area`.** A directivo's focus usually spans more than one area (Ángel's tags: Estrategia, Growth, Operaciones) — modeling Dirección as one more `area` value would force picking a single one arbitrarily. Regular Equipo members get a single free-text `area` (same "no fixed department taxonomy" rule as Objetivos' Foco de Impacto, §12) shown as its own table column instead.

**The right sidebar's "Áreas" list and stats row are entirely live-derived** (`areaCounts()`/`groupByArea()` in `lib/directorio.js`), same blanket rule as every cross-module number in this app — Dirección's count comes from `isDirectivo` people, every other area's count from grouping the rest by their real `area` string. "Roles asignados" on the stats row is computed as the % of people with a non-empty `role`, not a fabricated number.

**Person detail panel stops at "Áreas de responsabilidad" — the sections below it in the reference image were cut, not deferred as a TODO.** "Reporta a," "Trabaja con," "Proyectos activos," and "Objetivos" all need a real data model this build doesn't have (a reporting-line graph, cross-project assignment, per-person OKR tracking) — showing them would mean fabricating the exact kind of number this project has a standing rule against. Revisit only once one of those has a genuine source, not by inventing placeholder data to fill the space.

**Organigrama/Equipos/Roles are real, not "en construcción" stubs — but intentionally lighter than Personas.** Organigrama is a static visual hierarchy (Dirección row → área groups below, connector lines, no drag/drop) built entirely from the same `directoryPeople` data, no new collection. Equipos is genuine CRUD (`directoryTeams`: name + member picker, avatar-stack cards) — this is also what powers the sidebar's "Equipos" count on the Personas tab, so it was worth building for real rather than stubbing. Roles is read-only, just `people` grouped by their existing `role` field — no separate roles collection, since a role is already just a string on a person.

**"Filtros"/"Ordenar por" from the reference images were deliberately dropped, kept to just a search input.** Same "nothing decorative, everything earns its place" rule as everywhere else in this app (§10) — a non-functional Filtros button would be exactly the kind of dead UI this project has repeatedly removed elsewhere (e.g. the old disabled "+ Nueva Intervención" button, §19).

**Every write routes through `withTimeout(...).catch(showToast)`**, same defensive pattern established in Workspace/Objetivos (§10/§14) — applied from the start this time rather than being a bug fixed after the fact.

**Verification note:** build is clean; UI verified with a temporary in-memory data seed (`?preview=1` has no real Firebase Auth session, so real writes to the new collections correctly fail with `permission-denied` — same documented limitation as everywhere else) — confirmed Dirección cards, Equipo table, stats, the sliding tab indicator, the person detail panel (via DOM inspection — this session hit the known screenshot/viewport pixel-scale mismatch already documented in "Known issues," trusted `getBoundingClientRect()`/`elementFromPoint()` over the screenshot per that precedent), Organigrama's hierarchy, Equipos' team cards, and Roles' grouping. Not yet tested against a real signed-in account with real photo uploads.

**Same-day follow-up: management restricted to administrators.** Direct concern raised right after shipping: nothing stopped anyone with an ADOR OS login from adding themselves to Directorio and marking themselves "Dirección." This app has no existing RBAC concept anywhere — every account in `allowedEmails` has identical full read/write via the one blanket Firestore rule (§Infrastructure notes) — so a real admin/non-admin distinction didn't exist to reuse. Added one, scoped to this exact problem: `users/{uid}.isAdmin`, checked via `isDirectorioAdmin()` in `lib/directorio.js`. **Fails open on a missing field** (`profile?.isAdmin !== false`) rather than defaulting new/unset profiles to non-admin — this was the deliberate choice, not an oversight: the 3 founders' profiles all predate this field, and failing closed would have locked all 3 out of managing Directorio until someone manually flagged them admin in the Firestore console, a chicken-and-egg problem with no clean bootstrap. Failing open means they're already admins today with zero setup step, and the trigger to actually use this is the day a non-founder gets an ADOR OS login and needs `isAdmin: false` set by hand — same console-editing pattern already established for `allowedEmails` and `settings/finanzas`. Gated behind this: "+ Añadir persona," every person's edit/delete menu (`EquipoRow`, `PersonDetailPanel`'s edit icon), and team create/delete. Viewing the whole directory stays open to everyone with app access — only mutation is restricted.

**Same-day follow-up: link an existing ADOR OS account instead of retyping everything.** A founder with a real login still had to fill out "+ Añadir persona" as if they were a stranger — no connection between `users/{uid}` (real accounts) and a Directorio entry existed. `AddPersonModal.jsx` gained a "Vincular con cuenta de ADOR OS" dropdown, populated from `subscribeUsers()`, that only appears when there's at least one real account not already linked to some other Directorio entry; picking one copies its `displayName`/`email` straight into the form and stores the account's uid as `linkedUserId` on save. Kept intentionally simple — no bidirectional "claim your profile" flow from the account's own side (that was the original proposal, replaced per direct feedback: only admins decide who's who, so the linking happens from the admin-only creation form, not a self-service claim).

### 31. Clientes' Pipeline (Kanban) redesigned from a reference image (2026-09-18)

Two reference images shared, with explicit scoping instructions: List view (`ListView.jsx`) stays completely untouched; the actual work is the Kanban board (relabeled "Pipeline" on `CLIENTES_VIEWS`) plus a small stats row pulled from the second image ("2 Oportunidades / S/ 30,000 Potencial / 0 Activas / 0 Cerradas"). This is the same "adapt shapes, not literal content" pattern already used repeatedly in this project (Finanzas §9, Lista's Salud pill §24) — the reference's own 5-stage pipeline labels ("Prospecto," "Descubrimiento," "Propuesta," "Negociación," "Ganado") were never adopted; ADOR's real 7-stage SPC→SP vocabulary (`STAGES` in `clientStages.js`) stayed exactly as-is, since that's core business logic, not styling.

**Every new number and field on the redesigned card/stats row is a real, already-existing field — nothing invented.** `STAGES` gained a short `description` per stage (shown under the stage name, next to `stageColor()`'s dot and the live count) — new copy, but no new data. `ClientCard.jsx` now shows `industria`, a `currencyPEN`-formatted `montoAcordado`, an associate avatar, "Último contacto Nd" (reusing `ListView.jsx`'s own `lastContactAt`-or-`createdAt` fallback pattern verbatim, not a new one), and the client's own `nextStep` as a quick-action line. The stats row (Oportunidades/Potencial/Activas/Cerradas YTD) is entirely `clients`-derived — Potencial sums `montoAcordado` across open SPC, Cerradas YTD counts SP conversions whose `stageEnteredAt` falls in the current calendar year.

**Two pieces from the reference's bottom row were deliberately not built, for real scope reasons, not laziness.** "Actividad reciente" would need aggregating every client's own `clients/{id}/history` subcollection into one cross-client feed — a real feature (Firestore doesn't make this free: a bare `collectionGroup('history')` query would also sweep in `tasks/{id}/history`, since both use the same subcollection name, so it'd need a real disambiguation strategy first). "Notas rápidas" was skipped because the app already has exactly one quick-capture point (`GlobalCapture.jsx`, reachable from anywhere) — adding a second, Clientes-scoped one would fragment note-taking across modules, the opposite of that component's whole reason for existing. Built instead: "Próximas acciones," a real panel listing every open SPC's own `nextStep` (already a live, inline-editable field — no new concept), sorted by days since last contact.

**Verification note — a real gotcha worth flagging for future sessions.** This session's browser preview turned out to have a live, authenticated Firestore session cached from earlier in the conversation (confirmed via `firebaseLocalStorageDb` in IndexedDB — same class of issue CLAUDE.md §20 already documents for `?preview=1`, except here the *real* session was serving real data through a `?preview=1` tab that should have had none). The redesigned board/cards/stats/Próximas acciones were confirmed against real client records (QANLLA, gaa) as a result — a genuine, if accidental, real-data check — but no write action (Nuevo SPC, Añadir oportunidad, drag-to-move) was triggered during that verification specifically to avoid mutating production data from a dev session. If a future session's `?preview=1` unexpectedly shows real data instead of "Sin datos"/permission-denied errors, this is why — check `firebaseLocalStorageDb` before assuming `?preview=1` is safely sandboxed.

**Same-day follow-up: "Próximas acciones" can create a real Workspace task.** Direct follow-up question ("¿qué sería 'Próximas acciones'?") led to the honest answer that it was read-only — useful to see, but not actually *actionable* from where you're looking at it. Each row gained a "→ Crear tarea" button that creates a completely normal Workspace task (no new field, no live link back to the client — the client's name is just folded into the task title at creation time, e.g. "QANLLA: Preparar diagnóstico," so the task still makes sense once it's living on its own in Hoy/Personal) via the same `createTask`/`findOrCreateGeneralProyecto` path every other "General"-bucket task creation in this app already uses. Assigned to the SPC's own associate, falling back to whoever clicked it. A local per-row "✓ Creada" state (not a durable Firestore-backed dedupe check) stops an easy accidental double-click — accepted as a minor, low-cost gap rather than building real duplicate-prevention for a founder clicking twice in one sitting. **Caught and avoided a real risk while verifying this:** given the live cached session documented above, actually clicking "Crear tarea" during testing would have created a genuine task in production — verified the button's rendering/layout by temporarily overriding one real client's `nextStep` in local state for a screenshot, and deliberately never clicked it.

### 32. Clientes as a real CRM — Requiere atención, Por cobrar, Salud del pipeline (2026-09-18)

Direct framing from the user: "la idea es que ahí, en la sección Clientes, nos ayude a gestionar lo de los clientes como un CRM tal cual potente" — a nicely-styled contact list isn't the bar, actual CRM-grade utility is. Walked through what a real CRM has that Clientes didn't (risk/money visible at a glance, funnel health, forecasting) and agreed a prioritized order: risk + pending payments first, funnel health second, forecasting explicitly deferred since Finanzas already owns cash projection (`RunwayCard.jsx`, §13) and a second, Clientes-scoped forecast would compete with it rather than add anything.

**Two new pure helpers in `clientStages.js`, both fully live-derived — no new manually-tracked field anywhere in this pass.** `pendingPaymentAmount(client)` reads the exact same `pago1`/`pago2`/`montoAcordado` fields Finanzas and the Ficha's Pagos tab already read — there's structurally only one amount ever owed at a time, since `pago2` stays locked until `pago1` is `Recibido` (`PagosTab.jsx`), so this never double-counts. `pipelineHealth(clients)` computes conversion rate, average time in the current stage, and a `lostReason` breakdown in one pass.

**Conversion rate only counts clients that reached a terminal state (became SP or were marked lost).** An SPC still mid-pipeline hasn't failed — counting it against the rate would understate real performance and make the number swing wildly early in a quarter before enough deals have resolved either way. This is the same "don't report on things still in motion as if they were resolved" instinct behind Objetivos' `quarterElapsedPct()`-based pacing comparison (§15) — a number is only honest once it's actually comparing like-to-like.

**"Promedio en etapa actual" is explicitly a live proxy, not a true historical average — said so directly in the code comment, not left implicit.** This app doesn't keep a full stage-by-stage transition log (only the *current* stage's `stageEnteredAt`), so a genuine "average time spent in each of the 7 stages historically" isn't computable from what's stored. Showing the honest, computable number (current stage age, averaged across active SPC) beats either fabricating precision the data doesn't support or not shipping the metric at all — same "trigger to revisit" pattern used throughout this file whenever a simplification is accepted deliberately (e.g. Objetivos' `tasks_completadas` proxy, §12).

**Three new panels, one new layout row, no new module/tab.** `RequiereAtencion` (14+ days since contact, red-bordered, empty state reads "Nada estancado" in green rather than just being absent) and `PorCobrar` (SP clients with money still owed, running total) sit in a 2-column row right under the board; `ProximasAcciones` (already built earlier this session) and the new `SaludPipeline` card sit in a second 2-column row below that. All four read from data the rest of Clientes already subscribes to — nothing added a new Firestore listener.

**Verification note:** the real client data available in this session's (accidentally live) cached Firestore session had none of the states these panels are built to surface — no 14+-day-stale client, no pending SP payment, no lost clients with varied reasons. Rather than either skip verification or risk writing real test data, temporarily replaced the `subscribeClients` call with an in-memory seed covering all four states, confirmed every panel rendered correctly, then reverted — confirmed by diffing the post-revert build's output hash against the pre-debug build, which matched exactly.

### 33. Finanzas redesigned from a detailed functional spec (2026-09-18)

The user sent a full written functional spec (not just a reference image) explicitly framing the problem: "la propuesta es funcional si cada bloque está conectado a acciones y datos reales... no quiero que terminemos haciendo un 'dashboard bonito' que solo muestra números." Every section below maps directly to a line in that spec's own table (Salud financiera / Caja disponible / Runway / Margen / Por cobrar / Ingresos-gastos / Flujo de caja / Atención / Proyección / Movimientos), and the spec's own two worked examples ("S/7,200 por cobrar → clic → clientes → Ficha → registrar pago" and "Runway → clic → qué está consumiendo caja") became the literal spec for the two drill-down panels.

**`FinancialHealthCard.jsx` rewritten to the spec's exact four metrics** — Caja disponible, Runway, Margen, Por cobrar — replacing the previous set (Runway/Margen neto/Concentración/Cobros vencidos, §9's original build). Client concentration risk wasn't dropped as a concept, just moved: it's still computed in `useFinanceData.js` (`topClientConcentrationPct`) and available for `RequiereAtencion` to use later if it ever needs a fifth rule, just not one of the four headline tiles anymore since the user's own spec didn't ask for it there.

**"Por cobrar" and "Runway" are real clickable tiles, opening `FinanceDetailPanel.jsx`** — a slide-in drill-down using the same portal + transform-split pattern as every other detail panel in this app (CLAUDE.md §11). Deliberately does **not** rebuild payment registration inside Finanzas: each "Por cobrar" row links via `onNavigate('clientes', {type:'client', id})` straight into that client's real Ficha → Pagos tab, where "Registrar pago" already exists for real (§8) — duplicating that UI here would create a second, divergent payment-editing surface. `onNavigate` is threaded in fresh from `AppShell.jsx` (FinanzasModule previously received only `user`), reusing the exact `focus`/`navigateTo` mechanism global search already established.

**`RequiereAtencion.jsx` is real, rule-based, not decorative — the section the user singled out as "MUY importante" and "mucho más valioso que simplemente mostrar números."** Three live rules: runway < 3 months (red under 2, amber under 4), any pending "por cobrar" amount (red if 2+ payments are actually overdue, amber otherwise), and expense-category spend spikes ≥15% vs. last month (a new threshold, chosen in the same spirit as the existing 7/14-day urgency thresholds elsewhere in this app, not copied from anywhere). Shows a calm green "Sin alertas financieras" when nothing's wrong — same "a landing screen shouldn't just show an empty gap" rule established for `NorthStarHero.jsx` (§14) and Hoy's `NadaUrgente` (§21).

**New `SituacionActualCard.jsx` replaces `MetricCards.jsx`'s three separate cards with one unit** ("Resultados del mes" in the spec), Ingresos/Gastos/Resultado read together with a single delta on the result itself rather than three isolated numbers with their own separate deltas — directly matching the spec's own mockup. `RunwayCard.jsx` gained a 90-day row and a final "Caja proyectada" total, since the spec's proyección explicitly wanted 30/60/90, not just 30/60.

**Every new number in `useFinanceData.js` is live-derived from data the hook already subscribed to — nothing manually re-entered.** `totalPorCobrar`/`porCobrarClientCount` sum *all* pending payments (not just overdue — that's the pre-existing, distinct `overdueAmount`); `categoryTotalsPrevMonth`/`categorySpikes` compare this month's category totals against last month's, category-by-category; `resultDeltaPct` compares net result month-over-month; `estadoSalud` (Estable/Atención/Crítico) is the same worst-signal logic the card's border color already used, just also exposed as the one-word label the spec's mockup shows next to "SALUD FINANCIERA."

**Retired `MetricCards.jsx` and `NextPaymentCard.jsx` (deleted, not left as dead unused files)** — superseded by `SituacionActualCard.jsx` and the new "Por cobrar" panel respectively. `QuarterlyGoalCard.jsx` (Meta del Trimestre) was kept as-is at the bottom of the right column — it's a real, distinct, already-working feature (§9) the new spec's own architecture list didn't mention removing, so it stayed rather than being silently dropped.

**Verification note — same live-session gotcha as §31/§32, worth reading once more for the specific near-miss.** This session's cached real Firestore session meant the health bar, Situación actual, Flujo de caja, Requiere atención, and both drill-down panels were all confirmed against real financial numbers directly — genuinely useful, but mid-verification the `RunwayCard`'s inline cash-balance edit field got left open by a stray click, with a real `settings/finanzas.cashBalance` write one "Guardar" click away. Caught it and reloaded without saving instead. If a future session hits this same cached-session condition, treat every editable field as live — confirm nothing is mid-edit before navigating away or reloading, not just before submitting a form.

**Same-day follow-up: Proyección de Caja gained the stacked bar visual.** The first redesign pass left this card text-only; a direct follow-up pointed out the reference image's own version has a small vertical stacked bar next to the numbers. `RunwayCard.jsx` gained `ProjectionBar` — three segments (caja actual/blue, 30-day inflow/green, 60-90-day inflow/lighter blue) stacked bottom-to-top, heights proportional to the same real `cashBalance`/`inflowIn30`/`inflowIn90` values the rows already show, not a decorative illustration. Negative deltas clamp to 0 height rather than rendering broken negative segments — the real number still shows in the row text even when the bar itself can't visually represent it. Rows were also simplified to match the reference exactly: dropped the 60-day row (the reference only ever shows 30 and 90) and switched from computing deltas via subtraction (`projectedIn30 - cashBalance`) to reading the hook's own `inflowIn30`/`inflowIn90` directly, avoiding a redundant reconstruction of a value the hook already computed. Real `cashBalance` is 0 in production right now, so this was verified with a temporary prop override on the `RunwayCard` call itself (not a Firestore write) rather than the in-memory-seed-plus-revert pattern used elsewhere in this session — simpler since only one component's props needed faking, not a whole subscribed collection.

**Same-day follow-up: Proyección de Caja's manual edit flow reworked.** Direct feedback that the bare number-input-plus-Guardar edit state "se ve muy bien ni funcional" — no way to back out short of reloading, Escape did nothing, no validation feedback on a bad value. `RunwayCard.jsx`'s edit state gained: a "Cancelar" button and a real Escape-key handler that both call the same `cancelEdit()` (revert the draft, close without writing — no divergent code paths to keep in sync), inline validation that rejects empty/negative/non-numeric input with a visible error instead of the previous silent no-op (`if (amount >= 0) setCashBalance(amount)` — an invalid draft used to just do nothing, no different from a broken button), a loading state on save (`withTimeout(...).catch(showToast)`, the same defensive pattern retrofitted onto Workspace/Objetivos after their own silent-failure bugs, applied here from the start), and an "S/" prefix on the input so it doesn't read as a bare unlabeled number field. The empty state (`!cashBalance`) changed from a plain sentence pointing at a small pencil icon to a real dashed "+ Registrar caja disponible" button, matching the add-affordance style already established elsewhere (Workspace's "+ Nuevo equipo," Directorio's team creation).

**Verification hit a real, worth-noting automation limitation, not a bug in the app.** This input is a React-controlled number field, and the browser tool's coordinate-based clicks in this session landed inconsistently — confirmed via `getBoundingClientRect()`/`elementFromPoint()` checks that a click intended for the input was actually landing elsewhere, the same screenshot/viewport pixel-scale mismatch already documented in "Known issues / gotchas." Rather than fight that further, verified the shared `cancelEdit()` code path by triggering it through the "Cancelar" button instead (same function Escape calls) — confirmed it reverted the draft and closed without writing (`cashBalance` stayed empty afterward), which validates the Escape path too since there's only one function, not two to separately get right.

**Same-day follow-up: Meta del Trimestre and a new Meta Anual merged into one "Metas" card.** The user asked for an annual revenue goal, then specifically asked where it should live — walked through the options directly rather than guessing: not a replacement for Proyección de Caja (a distinct, genuinely load-bearing concept — cash/runway vs. a revenue target), and not a fifth separate card either, since the right column already had four. Landed on merging it with the existing quarterly goal into one `MetasCard.jsx` (replacing `QuarterlyGoalCard.jsx`, deleted) with a Trimestre/Año sliding toggle — same underlying concept (target vs. recaudado) at two different time scopes, one UI instead of two near-duplicate cards.

**Gained a real radial-progress ring per direct request that "la meta también sea gráfico."** `GoalRing` is a hand-drawn SVG `stroke-dashoffset` circle, deliberately reusing the exact technique Workspace's `ProgressDonut` (`HoyRightRail.jsx`) already established rather than introducing a third way to draw a progress indicator in this codebase (Finanzas already has its own no-library convention for `FinanceChart.jsx` and the `ProjectionBar` stacked bar — this keeps a fourth chart-ish element visually and technically consistent with all of them). Ring color shifts gold→blue→green as progress crosses 60%/100%, same traffic-light-adjacent logic used elsewhere in this app, just applied to a continuous rather than discrete signal.

**New `recaudadoAnual` in `useFinanceData.js` is exactly the same kind of live derivation `recaudadoTrimestre` already was** — sums every real income entry (client payments + manual) whose date falls in the current calendar year, nothing new tracked. `setAnnualTarget()` in `lib/firestore.js` mirrors `setQuarterlyTarget`/`setCashBalance` exactly (one shared `settings/finanzas` doc, `merge: true`). The card's edit flow reuses the exact Cancelar/Escape/validation/loading pattern `RunwayCard.jsx` got in the immediately preceding change — applying a UX fix to the second editable field in the same session it was built for the first, not leaving Finanzas with two different quality bars for what's functionally the same kind of control.

**Verification note:** real `quarterlyTarget`/`annualTarget` are both 0 in production, so this was verified with a temporary prop override on the `MetasCard` call (not a Firestore write) — confirmed the toggle switches correctly and the ring renders proportionally in both periods. One genuinely useful incidental check: the annual "recaudado" figure computed against real data (S/35,000) matched the real transaction total visible in Movimientos exactly (QANLLA S/20,000 + Venta de Servicio S/15,000) — real confirmation that `recaudadoAnual`'s date-filtering logic is correct, not just that the UI renders.

### 34. Chat → "Comunicación": DMs / Grupos / Canales with real permissions, Google Meet hand-off, conversation files vs. Drive (2026-09-23)

The user defined Communication's architecture explicitly before any more UI work ("si no terminaremos copiando Slack sin tener claro qué significa cada cosa"). The nav label changed from "Chat" to "Comunicación" (`Sidebar.jsx`, `AppShell.jsx`); the module id stays `chat`.

**Three kinds of conversation, each meaning something different** — pure rules in `lib/chat.js`:
- **Mensajes directos** — private between exactly two people. Unchanged storage (`chatDms/{dmIdFor(a,b)}`). Not a channel.
- **Canales** — a permanent space for an area/function (`#general`, `#estrategia`...). `chatChannels/{id}` with `kind: 'channel'` and `visibility: 'public' | 'private'`. Public = everyone in ADOR, automatically a member, no member list tracked. Private = `memberUids`, invitation only, **not listed at all** to non-members (can't find it, can't join it).
- **Grupos** — an ad-hoc private conversation between a few people, not a permanent area. Same collection, `kind: 'group'` (always private). "Convertir en canal" in its Detalles panel promotes it in place (messages kept) once it turns recurring — the user's own rule.
Channels and groups share one collection deliberately: one messages subcollection shape, one unread mechanism, one composer. Channels created before this change have no `kind`/`visibility` field and are read as public channels (`conversationKind()`/`isPrivate()`), so nothing needed migrating.

**`#direccion` is a governance level, not an area** — it's a separate, private-by-default suggestion from `#estrategia` (`SUGGESTED_CHANNELS`). Area channels also default private; flip any to "Toda la empresa" in Detalles. Suggestions only prefill the create modal — nothing is auto-created.

**Detalles panel (`ConversationInfoPanel.jsx`) makes permissions visible:** Visibilidad / ¿Quién lo encuentra? / ¿Quién entra? / Miembros, plus invite, remove, leave, and a visibility toggle. Rendered as a normal-flow column beside the thread, not a floating panel (no portal needed). Going private keeps existing members plus you, so a channel never ends up with nobody who can see it.

**Real limitation, stated plainly: private visibility is enforced in the UI, not yet in Firestore rules.** The blanket `match /{document=**}` rule still lets any `allowedEmails` account read any document, so a private channel is hidden, not locked — someone could still read it through the Firestore console/SDK. Fine at today's 3-founder scale (all three would be members of `#direccion` anyway), but **before any non-founder gets an ADOR OS login and sensitive topics land in a private channel, rules must be tightened** — and because rules are OR'd, that means narrowing the blanket rule to exclude `chatChannels`, then adding a chat-specific rule (read if `visibility != 'private'` or `request.auth.uid in memberUids`, same check on `/messages` via `get()`). That also requires `subscribeChatChannels` to split into two queries (public, and `memberUids array-contains uid`), since Firestore rejects list queries a rule might deny. Don't claim `#direccion` is "secure" until that ships.

**Llamar / Videollamada hand off to Google Meet — no in-app calling.** Shown in DMs and groups. ADOR OS only has read-only Google access (§27), so it can't create a Meet room and read the link back itself. `MeetPopover.jsx` is a two-step flow instead: "Abrir Google Meet" (opens `meet.google.com/new` in a new tab) → paste the link → it's posted as a call card with "Unirse". Meet has no audio-only mode; "Llamar" just labels the card as a call and hints to join with the camera off. The upgrade path, if the paste step ever annoys the team: the Meet REST API (`spaces.create`, scope `meetings.space.created`) would let the button create the room and post the link in one click — needs a new OAuth consent, so ask first.

**Files: conversation files vs. official documents — ADOR OS is not another Google Drive.** The composer's attach tray (normal flow above the input, no popover) offers three deliberately different options: **Imagen** — a *conversation file*, compressed (1400px JPEG) into the message doc itself as a data URL (same Storage-free trick as profile photos, `lib/image.js`; capped at ~900KB for Firestore's 1MB doc limit), labeled "Archivo de conversación". **Documento de Drive** — the *official* document stays in Drive; you paste its link and it renders as a "Documento oficial · abrir en Drive" card (any Drive/Docs/Sheets/Slides URL in any message gets the card, and a link-only message shows just the card). **Otro archivo** (PDFs etc.) — visible but disabled until Firebase Storage is enabled, same pattern as Clientes → Documentos (§8).

**Message shape grew, backwards compatible:** `{text, attachment?, call?}` via `messageFields()` in `lib/firestore.js`; `sendChannelMessage`/`sendDmMessage` still accept a bare string. URLs in text render as real links via `splitLinks()` (no `dangerouslySetInnerHTML`).

**Not built yet, from the user's own "ADOR OS owns" list:** threads, mentions, reactions, and chat notifications in the top-bar bell. Architecture first was the explicit ask; these are the natural next layer.

**Verification note:** no real session was active in the preview, so everything was checked with a temporary in-memory seed in `ChatModule.jsx` (reverted afterward, confirmed no `SEED` left): sidebar grouping (DMs → Grupos → Canales split into Empresa/Privados), a private channel the viewer isn't in correctly hidden, Detalles permissions panel, Drive card, call card, attach tray, Meet popover, and the new-channel modal with suggestions. No Firestore writes were made.

**Same-day follow-up (2026-09-23): profile panel on tap, like every messaging app.** From a reference image the user shared: clicking a person's name opens a right-side profile (`ProfilePanel.jsx`) — from the DM header (name + avatar is now a button) or from any author name in a channel/group thread. Identity comes from Directorio when the account is linked (`directoryPeople.linkedUserId`, §30: name, photo, role · area, "sobre mí"), else the account's own name/email. Actions: Llamar / Video (same Meet hand-off; from a channel it first jumps into the DM so the call card lands there), Buscar (filters the open DM's thread, scoped to that conversation), and "Mensaje" when opened from a channel. In a DM it also lists **Archivos compartidos** (image attachments) and **Documentos y enlaces** (Drive links marked "Documento oficial", other links by domain), both derived live from the loaded messages via `sharedInConversation()` — no stored index. **Silenciar** writes `users/{uid}.chatMuted.{conversationId}` (same one-map-on-the-profile pattern as `chatLastRead`) and suppresses that conversation's unread dot. Only one right panel at a time (`panel` state: Detalles or a profile); an open profile follows you into another DM. The Meet popover's open state was lifted into `ChatModule.jsx` so it can be anchored to either the header buttons or the panel's. Header call buttons became icon-only (they crowded the name once the panel was open). **Deliberately left out of the reference:** the green online dot (no presence tracking exists — a fake "online" would be worse than none) and the "Más" button (nothing real to put behind it yet). Verified with a temporary in-memory seed, reverted.

**Incoming-call alerting, layers 1 + 2 (2026-09-23, same day, user chose "1 y 2").** Before this, a Meet call was only a card in the conversation — nobody got alerted. Now sending the Meet link from the popover also writes a short-lived `chatCalls/{id}` doc (`createChatCall` in `lib/firestore.js`: `type`, `url`, `toUids` — the DM partner or the group's members minus the caller — `fromUid/fromName`, `conversationLabel`, `responses: {}`). The card in the thread stays the permanent record; this doc is only the "ring" signal. `IncomingCallGate.jsx` (mounted once in `AppShell.jsx`, beside `AssignmentConfirmGate`, so it rings in any module; skipped for the `?preview=1` mock user) subscribes to `toUids array-contains me` (no orderBy → no composite index needed), and shows the newest call that's under 45s old and that I haven't answered:
- **Layer 1 — in-app:** a full-screen "Videollamada / Llamada entrante" overlay (caller photo from `users`, pulsing ring) with Rechazar / Unirse, plus a two-tone ringtone synthesized with Web Audio (no audio file). Unirse opens the Meet link; both write `responses.{uid}` so it stops ringing for that person only (group calls keep ringing for others). Dismissed locally first, so a slow/failed write never leaves it ringing. After 45s it simply stops — the card in the chat remains as the "missed call".
- **Layer 2 — background tab:** a real OS notification (Notification API, `requireInteraction`, click focuses the tab) when ADOR OS is open but not focused, plus a flashing "📞 … te está llamando" tab title that works even without notification permission. Permission can only be requested from a click, so Comunicación's sidebar shows a one-time "Activar avisos de llamada" prompt (`CallNotificationsPrompt`); if the browser has it blocked, it says to re-enable it from site settings.
- **Known limits:** browsers block sound until the person has clicked somewhere in the page at least once since loading it (the overlay and notification still appear). `chatCalls` docs are never cleaned up — fine at 3-founder volume, the client filters by age; add a cleanup if it ever grows. Clock skew between two computers shifts the 45s window slightly.
- **Layer 3 (not built): ringing when ADOR OS is closed or the computer is asleep** needs Web Push (VAPID keys) + a service worker + a Vercel function to send the push — the same infrastructure as the parked PWA phase 3 in `PROJECT_STATE.md`; build it together with that.
- **Verification:** the overlay was checked with a temporary fake call injected into the gate (reverted): renders on top of Home (app-wide), Rechazar dismisses it. The notification prompt renders in Comunicación (the preview browser reports permission "denied", so it showed the blocked-state message). A real two-account ring test hasn't been possible here — first real test should be two founders on two computers.

**Menciones, campana, Inbox/Guardados/Archivos, composer completo y optimización (2026-09-23, same day).** Built from the user's reference image (sidebar top section: Inbox · Menciones · Mensajes guardados · Archivos; composer: Aa · emoji · clip · imagen · micrófono).
- **Index collections instead of collection-group queries.** `chatMentions` (one doc per person mentioned: `toUid`, `fromName`, `convType/convId/conversationKey`, `messageId`, text snippet), `chatSaved` (doc id `${uid}_${messageId}`, a text snapshot + pointer, only the saver reads it), `chatFiles` (pointer per image / voice note / Drive link shared). A collection-group query over every `messages` subcollection would need a hand-made console index and would scan every message ever sent — exactly the "que no cargue tanto" concern. Deleting a message runs `cleanupMessageIndexes(messageId)` so these never point at a ghost. Files older than this change aren't in the index (no backfill).
- **"Unread" is never stored anywhere new.** A mention is unread when `createdAt > users/{uid}.chatLastRead[conversationKey]` — the same timestamp the sidebar dots already used, so opening the conversation clears the dot, the Menciones count and the bell item at once, with zero extra writes.
- **Mentions:** typing `@` in a channel/group shows member suggestions (arrows + Enter/Tab); the message stores `mentions: [{uid,name}]` + `mentionUids`, and only those exact `@Nombre` tokens are highlighted (gold for you, blue for others). No mentions in DMs — every DM message already reaches the person. Edits don't create new mentions.
- **Bell (`hooks/useChatNotifications.js`, first in TopBar's list):** unread @mentions, unread DMs, and unread private groups — deliberately *not* ordinary channel messages (that would train everyone to ignore the bell; Inbox and the dots cover those). Muted conversations excluded. Bell items can now carry an `onClick`: `NotificationCenter` renders those as buttons that navigate via `navigateTo('chat', {type:'chat', convType, convId, participantUids, messageId})`; `ChatModule` consumes the `focus` prop, opens the conversation and scrolls to + flashes the message (`jumpToRef`). Other bell items stay informational as before.
- **Sidebar views:** Inbox (every conversation with activity, newest first, built from a new `lastMessage` preview written onto the channel/DM doc on send — never loads histories), Menciones, Mensajes guardados (bookmark on message hover), Archivos (Drive "documentos oficiales" vs. "archivos de conversación" grid; only from conversations you can currently see; its listener only runs while the view is open). While a view is open no conversation streams or gets marked read.
- **Composer:** textarea with Shift+Enter newlines and auto-grow; **Aa** = Slack-style formatting (`**negrita**`, `_cursiva_`, `~tachado~`, `` `código` ``, rendered by `splitFormatting()` — no Markdown library, no innerHTML); **emoji** = curated 48-emoji grid (no library); **clip** = Documento de Drive / Otro archivo (still disabled until Storage); **imagen** = direct image pick; **micrófono** = voice notes up to 60s (MediaRecorder, opus ~32kbps). The reference's separate "+" was dropped as a duplicate of the clip. Every panel sits in normal flow above the input — no floating popovers.
- **Reactions:** hover → 6 quick reactions; `reactions.{emoji}: [uids]` toggled per emoji (several per person allowed), tooltip lists who reacted.
- **Optimización:** (1) threads subscribe with `limitToLast(50)` + "Cargar mensajes anteriores" (keeps scroll position), instead of streaming whole histories; (2) images and voice notes store their heavy payload in `chatBlobs/{id}`, fetched only when opened/played (cached per session) — the message carries just a ~480px thumbnail or a duration; (3) every module except Home is now `React.lazy` in `AppShell.jsx` and prefetched on idle after login — initial JS went from one 1.38MB bundle to ~1.03MB (the remaining bulk is the Firestore SDK itself, which is needed at login), with each module a separate 15–86KB chunk. Old image messages with an inline `dataUrl` still render. Data-URL images now open in an in-app `ImageLightbox` (Chrome blocks opening `data:` URLs in a new tab — the earlier `href={dataUrl}` links were silently broken).
- **Bug caught in verification:** the "clear the thread on conversation switch" effect was declared after the subscription effect, so it could wipe freshly loaded messages; moved above it.
- **Verification:** temporary in-memory seed (reverted): nav counts, Inbox list, Archivos grid, mention → opens #general and flashes the message, @ suggestions, emoji panel, formatting/reaction/mention rendering. The bell hook skips the `?preview=1` user, so the bell itself and all writes still need a real two-account test.

**Presencia, "escribiendo…", ✓/✓✓ leído, pegar imágenes, hilos (2026-09-23, same day).**
- **Presence = heartbeat, not a guess.** `usePresenceHeartbeat` (mounted in `AppShell.jsx`) writes `presence/{uid}` `{state, lastActiveAt}`: "online" on open and every 60s while the tab is visible, "away" when it goes to the background, "offline" on `pagehide` (best-effort — a crash or sleeping laptop can't send it, so readers also expire anything older than ~2.5 min, `presenceOf()` in `lib/chat.js`). Green dot = en línea, amber = ausente, no dot + "Activo hace X" = desconectado. Shown on DM avatars in the sidebar, the DM header, and the profile panel (replacing the earlier "no presence exists" note). ~60 writes/hour per open person. The more precise alternative (Realtime Database `onDisconnect`) needs enabling a second database in the console — not worth it at this scale.
- **Escribiendo…:** `chatTyping/{conversationKey}` (and `{convId}_thread_{parentId}` for threads) holds `{[uid]: {name, at}}`; the composer writes at most once every 3s while typing and clears its entry on send; readers ignore entries older than 7s.
- **Leído (✓ / ✓✓):** only on your own messages in DMs and private groups (Slack doesn't show receipts in channels; "read by the whole company" means nothing). Computed from the other participants' existing `users/{uid}.chatLastRead[conversationKey]` — no new data. ◷ = not confirmed by the server yet, ✓ grey = sent, ✓✓ blue = every other participant has opened the conversation since; hover in a group lists who has read it. To make ✓✓ honest, `markChatRead` now only fires while the window is visible *and* focused (plus again on focus/visibility return) — before, a background tab would have marked messages read.
- **Pegar imágenes:** Ctrl/⌘+V of a screenshot or copied image and drag-and-drop onto the composer both go through the same compression path as the image button (pasted screenshots get a "Captura HH:MM.png" name). The lightbox gained "Copiar imagen" (re-encoded to PNG, the only image type the async clipboard accepts) so an image from the chat can be Ctrl+V'd elsewhere.
- **Hilos (Slack's model):** "Responder en hilo" on message hover opens `ThreadPanel.jsx` (parent + replies + its own composer, a normal-flow right column like Detalles/profile, tied to the conversation it was opened in). Replies live in `.../messages/{parentId}/replies` — a subcollection, because keeping replies out of the main timeline via a field would need a hand-made composite index. The parent carries `replyCount` / `lastReplyAt` / `replyUids` (written in the same batch as the reply), so the timeline shows "N respuestas · Última hace X" with avatars without loading replies. Following follows Slack: whoever wrote the parent + anyone who replied gets a `chatMentions` doc with `kind: 'reply'` and `threadParentId` (people @mentioned in the reply get the mention instead, never both); read state for anything in a thread uses `chatLastRead['thread_{parentId}']`. New sidebar view **Hilos** (one row per thread, bold when new); the bell collapses several replies in one thread into one item; Menciones/Guardados/bell entries inside a thread open the thread and flash the reply. Not built: Slack's "también enviar al canal" checkbox.
- **Layout:** with any side panel open the page tightens its gaps, and the composer's tool buttons wrap under the text box when the column is narrow (always, in the thread composer) instead of crushing it.
- **Verification:** temporary in-memory seed in `ChatModule.jsx` + `ThreadPanel.jsx` (reverted): presence dots and "En línea", ✓✓ blue on a message read after it was sent vs. ✓ on a newer one, "Leonardo está escribiendo…", thread summary, thread panel at 1100px. Needs a real two-account test: presence, typing, receipts and thread notifications all depend on another person's writes.

**Inbox como email + llamadas estilo Teams (2026-09-23, same day).**
- **Inbox = unread-first, like an email client.** Opens on "No leídos" (only conversations with something new), with a "Todos" tab; rows grouped Hoy / Ayer / Esta semana / Anteriores (`dayBucket()`), gold left bar + gold count pill on unread, sender + where + preview + time; hover → "Leído" / "No leído"; "Marcar todo como leído" in one write (`markManyChatRead`). The count ("3") comes from a new `messageCount` counter bumped on every channel/DM send, copied into `users/{uid}.chatReadCount[key]` whenever `markChatRead` runs — unread = messageCount − chatReadCount (`unreadCountOf`). Conversations whose last message predates the counter show "•" instead of a number. "Marcar como no leído" moves `chatLastRead[key]` to 1ms before the last message (`markChatUnread`). Thread replies don't bump the counter. Also fixed a latent issue: the open conversation's doc (counter/lastMessageAt) can land a moment after the message itself, which could leave the conversation you're reading flagged unread in Inbox/bell — `markReadIfVisible` now also re-runs when that doc's `messageCount` changes.
- **Llamadas estilo Teams, all on the `chatCalls` doc:** created *before* the card is posted so the message's `call.callId` links them; new fields `joinedUids` (caller included from the start), `status` ('ringing' | 'cancelled' | 'ended', + `statusBy/statusAt`), `convType/convId/participantUids`. `callState()` in `lib/chat.js` derives what every surface shows — llamando… (first 45s), en curso (someone besides the caller joined, with who), perdida / sin respuesta, rechazada (all invitees declined), cancelada, finalizada · N min (someone pressed Finalizar, or >3h stale). The **call card** in the thread follows that live (listener only for calls < 6h old): Unirse (counts you in) / Volver a la llamada, Cancelar for the caller while ringing, Finalizar for anyone in the call. The caller gets an app-wide **OutgoingCallBanner** (top-right under the bell, `IncomingCallGate.jsx`): "Llamando a Leonardo… · Cancelar" → "se unió" / "rechazó la llamada" / "no respondió". Cancelling stops the callee's ringing screen. Honest limit: ADOR OS can't see inside Meet, so "en curso" means "someone clicked Unirse", and "finalizada" needs someone to press Finalizar (or goes stale after 3h). Old call cards without a `callId` keep working as before.
- **Still manual:** creating the Meet link (open Meet → paste). One-click would need the Google Meet REST API (`spaces.create`, scope `meetings.space.created`) — enable the API + add the scope in Cloud Console + reconnect Google once; offered to the user, not built.
- **Verification:** temporary seeds in `ChatModule.jsx`, `ChatThread.jsx` and `IncomingCallGate.jsx` (all reverted): Inbox unread list with counts, the three call-card states, the caller banner. Needs a real two-account test.

**Grafito y oro, llamar en un clic con Meet, fijar, recordármelo, mensaje → tarea (2026-09-23, same day).**
- **Color (user's choice among three options shown side by side):** the chat no longer uses ADOR's bright blue. Your messages are warm graphite (`#1C1A16`) with a thin gold edge (`rgba(184,134,11,0.42)`) and ivory text; gold (`#E8C15A` / `#B8860B`) marks what's yours or new — unread dots and counts, active sidebar item, ✓✓ read, send button, mentions of you, pinned. Scoped to `components/chat/*` and the chat items in the bell; the rest of the app keeps its blue accent.
- **Llamar en un clic (Teams-like, on Google Meet):** the one Google connection (`users/{uid}.googleCalendar`, shared with Calendario) now also requests `meetings.space.created` (lets ADOR OS create Meet rooms and manage only the ones it created). `useGoogleMeet` → status `ready` / `needsConnect`; with `ready`, pressing Llamar/Videollamada opens a tab inside the click (so it isn't popup-blocked), creates the room via `api/google-meet/space.js` (a proxy that forwards the user's short-lived access token to `meet.googleapis.com/v2/spaces`, `accessType: OPEN`), points the tab at it, posts the live call card and rings the other side. Any failure closes the tab and falls back to the manual popover, which now leads with "Llama en un clic — Conectar Google". OAuth now carries `state` (`calendario` / `chat`) so the right hook spends the code: `AppShell` opens Comunicación when returning with `state=chat`, and `useGoogleCalendar` ignores those. `exchange.js` returns the granted `scope`, stored as `googleCalendar.scopes`; old connections without the Meet scope show "Conectar Google" once. **Needs, before it works:** Google Meet REST API enabled in Cloud Console (project `ador-os-internal`) + the `meetings.space.created` scope added under Google Auth Platform → Data Access; then each founder reconnects once from the chat. Not verifiable from the dev preview (OAuth redirect is the production URL).
- **Fijar:** ⋯ on a message → Fijar/Desfijar. Stored as `pinned.{messageId}` (text snapshot, who pinned) on the channel/DM doc; a gold "📌 N fijados" bar under the header expands in place to jump to or unpin each.
- **Recordármelo:** ⋯ → Recordármelo → 20 min / 1 h / hoy 17:00 / mañana 9:00 / el lunes 9:00. `chatReminders` docs (owner-only). `ReminderGate` (app-wide, top-right) shows it when due — Ver mensaje (opens + marks done), Posponer 1 h, Listo — plus an OS notification if allowed; the bell lists due reminders first; pending ones are listed (and cancellable) at the top of Mensajes guardados. Delivery is client-side: if ADOR OS is closed at that time, it appears on next open.
- **Mensaje → tarea, always with confirmation** (user's rule): ⋯ → Crear tarea opens `TaskFromMessageModal` (title, description with the quoted message and where it came from, para, fecha límite, prioridad, proyecto — General by default). Nothing is created until "Crear tarea". Uses Workspace's own `createTask` (so assigning someone else still goes through their accept/reject gate, and the modal says so), then `linkTaskToMessage` stamps `task: {id, title}` on the message → a green "✓ Tarea: …" chip that opens the task in Workspace (and the option disappears, preventing duplicates). Works on thread replies too.
- **Verification:** temporary seeds (reverted): colors, pinned bar, task chip, ⋯ menu options, task modal, reminder card. The one-click Meet path needs the console steps + a real Google account to test.

**Solo la cuenta de Google de la empresa + enlace de Meet detectado solo (2026-09-23, same day).**
- **Company-account rule (explicit user requirement: no personal Google accounts).** `assertCompanyGoogleAccount()` in `lib/googleCalendar.js` runs right after every OAuth exchange (Calendario and chat) *before* anything is saved: the connected Google email must equal the person's ADOR OS login email (`users/{uid}.email`), or be on a domain listed in `settings/google.allowedDomains` (hand-edited in the Firestore console, same as `allowedEmails`; empty/missing = login email only). A non-matching account is refused with a clear message, and its token is revoked at Google. Connections saved before this rule are re-checked on load and disconnected if they don't comply. `login_hint` pre-selects the company account on Google's picker. Calendario's return from Google now also reopens Calendario (OAuth `state=calendario`), not just Home.
- **Manual call path, one step shorter:** after "Abrir Google Meet", when the ADOR OS window regains focus the popover reads the clipboard; if it holds a Meet link (bare URL or Meet's whole "Copiar información de la reunión" block — `findMeetLink()`), it fills it in and sends the call on its own ("Enlace detectado — llamando…"). The browser asks once for clipboard permission; refused → pasting by hand still works. Not visually re-verified in the preview (needs a DM with a real second user).

**Cómo se enteran de mensajes nuevos fuera del chat (2026-09-23, same day).** Before this, only the bell's red dot. Now, Slack-style, all mounted app-wide from `AppShell.jsx`:
- **Gold unread badge on the Comunicación sidebar icon** — number of conversations with unread messages (channels you're in, groups, DMs; muted excluded), `useChatUnreadCount()` in `hooks/useChatNotifications.js`. The badge sits inside the 40px button's own box so it can't widen the shrink-wrapped capsule (§1); the tooltip adds "· N sin leer".
- **"(N) ADOR OS" in the browser tab title**, same count.
- **`ChatMessageToaster.jsx`** — for each *new* item addressed to you (DM, @mention, private-group message, reply in your thread; the same set as the bell, whose items now carry a stable `key`): a toast bottom-left with a soft two-note chime (Web Audio), click opens the conversation at that message, auto-hides after 6s, max 3 stacked, opaque `ador-modal-surface` (it floats over busy content). Not shown while you're in Comunicación. When ADOR OS isn't the window in front, an OS notification is sent too (needs the "Activar avisos de llamada" permission). Backlog present at load never toasts — only what arrives afterwards. Ordinary channel messages don't toast (they only raise the badge/unread dots), same rule as the bell.
- **Verification:** badge and tab title checked with a temporary fake count; the toast with a fake incoming DM (visibility check bypassed in the throwaway copy only — the preview pane reports itself as hidden). All reverted.

**Orden interno, estados de disponibilidad y búsqueda global (2026-09-23, same day).**
- **Refactor, no behavior change.** `ChatModule.jsx` (1,230 lines) and `ChatThread.jsx` (1,170) were split by responsibility: `ChatSidebar.jsx` (left column, status picker, view nav, `PresenceAvatar`), `ConversationHeader.jsx` (header, call buttons, `PinnedBar`), `MessageBubble.jsx` (one message: rich text, attachments, call card, reactions, receipts, hover menu), `Composer.jsx` (input, format/emoji/attach, paste/drop, voice), `ChatThread.jsx` (message list + lightbox), `hooks/useChatData.js` (every app-level chat listener) and `lib/chatIndexes.js` (pure derived state: unread map, Inbox, Menciones, Hilos, Guardados, Archivos, counters, `makeLabelFor`). `ChatModule.jsx` is now the orchestrator (~750 lines: selection, send/react/pin/remind/task/call actions). Verified by a no-undefined-names lint pass (ESLint + react plugin, run from a throwaway config) and a visual regression check.
- **Estados (Slack/Teams-style), on `presence/{uid}`:** `dnd: {until}` set by hand from "Mi estado" in the sidebar (Disponible / No molestar 30 min, 1 h, 2 h, hasta mañana 9:00), and `calendar: {until, meeting}` written by the person's *own* `usePresenceHeartbeat` every 5 minutes from their Google Calendar (`fetchCurrentBusy`: a timed, non-"free", non-declined event happening now; only the end time and whether it has other guests are published — never title or attendees). `presenceOf()` precedence: No molestar (red) → En reunión / Ocupado hasta HH:MM (violet `#A78BDA`) → en línea / ausente / activo hace X. Effects: **No molestar** — incoming calls don't ring or pop up (they end as missed-call cards), message toasts/chime/OS notifications are suppressed; badge, tab count and bell still collect everything. **En reunión** — the call screen still appears but silently ("Sin sonido — estás en una reunión"), toasts appear without chime, OS notifications are `silent`. Callers get a heads-up toast before calling someone in DND/meeting, and the caller banner says "X está en No molestar — verá la llamada perdida". Calendar-based status only works for people who connected Google, and only in production (token refresh goes through the Vercel function).
- **Búsqueda en todos los mensajes:** the sidebar box (Enter, or the gold "Buscar “…” en todos los mensajes" under it) opens a `search` view. Firestore has no full-text search and a hosted search service is overkill, so `useMessageSearch` reads the last 400 messages of every conversation you can see — one-off `getDocs` (`fetchRecentMessages`), no listeners, cached 60s per conversation — and matches on the device: case- and accent-insensitive, every word must appear (text, attachment name, author). Results: author, where, time, snippet centred on the match with gold highlights; filters "De:" and "En:". Clicking opens the conversation with 400 messages loaded (`deepJumpRef` / `SEARCH_DEPTH`) so the hit can be scrolled to and flashed. Limits, stated in the empty state: older than the last 400 per conversation isn't searched; thread replies aren't included.
- **Verification:** status picker, red/violet dots and "No molestar hasta…" header, search results with highlights and filters — all with temporary seeds (reverted). Needs real accounts for: calendar-driven "En reunión", DND actually silencing a call from another person.

**Pase visual del chat (2026-09-23, same day)** — the user picked items 1–4 of a design review plus right-panel spacing:
- **Mensajes agrupados (iMessage/Slack):** consecutive messages from the same person, same day, < 5 min apart form one block (`groupFlags()` in `ChatThread.jsx`): in channels/groups the author's face (`PersonAvatar`, 28px) and name appear once at the top (a spacer keeps the block aligned; DMs skip both), time + ✓/✓✓ only on the last bubble, other bubbles show their time on hover inside the action toolbar, 3px between bubbles vs 14px between blocks, and only the last bubble keeps the "tail" corner.
- **"Nuevos mensajes":** a gold divider before the first unread message from someone else. `newSince` is the read marker captured the moment the conversation opens (before opening marks it read); the conversation now opens scrolled to that divider instead of the bottom. Only messages that arrive after the thread opened get the `ador-msg-in` entrance (220 ms fade/rise, keyframes in `index.css`) — history never animates.
- **Hover toolbar floats** over the bubble's top edge (absolute, Slack-style) instead of sitting in the row, where it took width even while invisible and crushed bubbles to one word per line when a side panel was open. Thread list got `overflow-x-hidden`.
- **Sidebar:** "Comunicación" header with your status under it and **your face on the right** — click either to change status (replaces the "Mi estado" card). Inbox / Hilos / Menciones / Guardados / Archivos became **one row of icons** with count badges and tooltips. The notifications prompt now has "Ahora no" and never shows again once dismissed or answered (localStorage `ador_call_prompt_dismissed`).
- **Contrast + type scale (chat UI only):** greys `#333/#3A3A3A/#444/#555/#666` → `#5A5A5A/#767676/#767676/#7A7A7A/#858585` (small text now meets ~4.5:1 on `#0A0A0A`); font sizes collapsed from ~12 values to four — 11 (meta), 12.5 (secondary), 13.5 (body), 15 (titles) — plus 17 for the two display moments and 10 for badge digits.
- **Real faces:** `PersonAvatar.jsx` + `ChatPeopleContext` (provided once by `ChatModule`) resolve any uid to its Directorio photo, else the account photo, else initials on a warm graphite disc (`#26231E` / `#D9CFBF`) instead of the shell's blue. Used everywhere in the chat (lists, headers, bubbles, threads, mentions, search, pickers, modals); saved messages now also store `authorUid` for this.
- **Right panels share one frame:** `SidePanel.jsx` — 320 px, same header (title, subtitle, close), padding and scrolling body — for Perfil, Detalles and Hilo (they had drifted to 280/290/340 px with different paddings).
- **Verification:** grouped channel with photo/initials, the "Nuevos mensajes" landing, icon row, status header, Detalles panel at 1100 px with readable bubbles, hover toolbar — temporary seeds, reverted. Lint: no undefined names.

**Retención: cómo el chat se queda gratis (2026-09-23, same day).** User asked how to avoid filling storage, Slack-style, staying free. Decisions (the user's): conversation files 90 days; text kept forever (recommended: tiny, and it's the company's memory — a per-channel limit, e.g. #direccion 1 year, can come later).
- **Policy (`lib/chatRetention.js`):** images and voice notes expire at 90 days — the `chatBlobs` payload and the `chatFiles` pointer are deleted and the message's `attachment` becomes a stub `{kind, name, expired: true}` rendered as "Imagen expirada · nombre" / "Nota de voz expirada" (`ExpiredAttachment` in `MessageBubble.jsx`); Drive links never expire (they cost nothing); `chatCalls` > 7 days, done `chatReminders`, and `chatMentions` > 90 days are deleted. Text messages are never deleted.
- **Runs free, no server:** `useChatRetention` (AppShell) calls `runChatRetention()` 20 s after ADOR OS opens; `claimDailyCleanup()` is a transaction on `settings/maintenance.chatCleanupAt` so only one founder's app runs it per ~day. Batches of 150 per kind keep each day far below the 20k-deletes quota; any backlog continues the next day. Only files indexed in `chatFiles` expire — images from before the index existed (a handful of early test messages with inline `dataUrl`) are left alone.
- **Lighter media:** images now 1280 px @ 0.78 (thumbnail 400 px @ 0.7), voice at 24 kbps — roughly half the bytes. The pending-image hint says "se borra a los 90 días — lo importante, súbelo a Drive".
- **Search stays inside the free read quota:** it now reads the last 150 messages per conversation (was 400) and reuses them for 15 minutes (was 1). At ~10 conversations that's ~1,500 reads per fresh search instead of ~4,000, against a 50k/day limit.
- **Also fixed:** the composer's auto-height is re-measured when its width changes (`ResizeObserver`), so it no longer stays tall after a window resize or a side panel opening.
- **Not verifiable here:** the cleanup itself only runs against real Firestore (the preview has no auth). The expired stubs were checked with a seed (reverted).

**Borradores, citar, reenviar, avisos por conversación, descripción, enviar más tarde (2026-09-23, same day).** The user picked these from a review; keyboard shortcuts were explicitly left out.
- **Borradores:** `lib/chatDrafts.js` keeps half-written text per conversation (`thread_{parentId}` for threads) in this browser's localStorage — a per-person convenience, no Firestore writes per keystroke. Composer takes `draftKey`; the sidebar shows a gold pencil (next to the unread dot if both), Inbox shows "Borrador: …" instead of the last message. Drafts don't follow you to another computer.
- **Responder citando:** hover → reply arrow. The message carries `replyTo: {id, authorUid, authorName, text}` (a snapshot, not a live link); `QuoteBlock` in `MessageBubble.jsx` draws it and jumps to the original. Threads stay for long discussions. Quote replies don't send an extra notification.
- **Reenviar:** ⋯ → Reenviar → `ForwardModal.jsx` (person / group / channel + optional note sent right after). The copy carries `forwarded: {authorUid, authorName, from}` ("Reenviado · de X en #canal"; DMs say "un mensaje directo", never the DM's participants). Images/voice reuse the same `blobId` and are indexed in `chatFiles` again, so they expire too. Mentions aren't copied (no re-notifying).
- **One send path:** `lib/chatSend.js deliverMessage()` writes the message + its mention/file/Drive index docs; used by the composer, forwards and scheduled messages.
- **Avisos por conversación:** `users/{uid}.chatNotify[key]` = `all` | `mentions` | `none`, set in Detalles → "Avisos para ti" (`setChatNotify` also writes `chatMuted` so everything that already read mute keeps working). Defaults = the old behavior: DMs/groups `all`, channels `mentions` (`notifyLevel()` in `lib/chat.js`). A channel on `all` now reaches the bell/toast for every message; a group on `mentions` stops doing so. DMs keep just the Silenciar toggle.
- **Descripción del canal/grupo:** editable in Detalles (Enter saves, max 160); the header shows it first, then "Canal de toda la empresa" etc.
- **Enviar más tarde:** clock button in the composer (text only — images/voice go now) → En 1 hora / Hoy 17:00 / Mañana 9:00 / El lunes 9:00 / custom date. `chatScheduled` docs; no server — `hooks/useScheduledSender.js` (mounted in AppShell) listens to messages you author or receive (`audienceUids array-contains`, plus `isPublic` for public channels; two single-field queries, no composite index) and sends due ones from whichever involved person's ADOR OS is open, claiming each with a transaction (`claimScheduledMessage`; a claim older than 5 min can be retaken). Author sees "Programado · hora" above the composer with Enviar ahora / Cancelar. Limit: if nobody involved has ADOR OS open at that time, it goes out when one of them opens it.
- **Interfaz:** no "Sin actividad reciente" when there's no presence data (nothing is shown); a "Buscar en esta conversación" icon in every conversation header; the paperclip now goes straight to "Documento de Drive" (the disabled "Otro archivo" button is gone until Storage exists); a floating "↓ N mensajes nuevos" button when a message arrives while you're reading further up (the thread no longer jumps to the bottom then; your own messages still scroll down).
- **Verification:** temporary in-memory seed (removed): quote block, "Reenviado" label, scheduled bar, schedule panel, forward picker, Detalles (description + avisos), new-messages button (appears, counts 2, scrolls down, disappears), draft saved/restored across conversations, DM header without presence text. Writes (forward, schedule, notify level, description) need a real login to test.

**Encuestas, aviso al citar, editar programados, mensajes importantes (2026-09-23, same day).** User picked these; "registrar decisión desde el chat" and a free-text status were explicitly declined.
- **Encuestas:** composer "Encuesta" button (main conversation only, not threads) → inline form, Sí/No prefilled, up to 6 options, optional "varias respuestas". Message carries `poll: {question, options:[{id,label}], multi, closed}` and `pollVotes: {optionId: [uids]}`; `votePoll()` is one small update (single-choice moves your vote atomically; clicking your choice again removes it). Votes are visible (faces + %); the author can close/reopen. Polls can't be forwarded. Preview/snippet: "📊 Encuesta: …".
- **Aviso al citar:** "Responder citando" in a channel or group now creates a `chatMentions` doc `kind: 'quote'` for the quoted person (skipped if they're also @mentioned; DMs don't need it). All of this lives in `deliverMessage()` (lib/chatSend.js), so scheduled messages do it too. Bell and Menciones word each kind via `mentionVerb()` in lib/chat.js ('mention' | 'quote' | 'important' | 'reply').
- **Editar programados:** "Editar" on the "Programado" row → text + date/time inline. `updateScheduledMessage()` is a transaction that only applies while the doc is still `pending`, so an edit can't be silently lost to a send in progress.
- **Importante:** composer toggle (not schedulable). Message gets `important: true, acks: []`; everyone else in the conversation (`membersOf`, or the DM partner) sees "Confirmar lectura" (`ackImportantMessage` → arrayUnion); the author sees "Confirmado por N de M · falta X". In channels/groups every recipient also gets a `kind: 'important'` notification regardless of their "Solo menciones" setting (unless the conversation is set to "Nada"). Preview gets a "❗" prefix.
- **Verification:** temporary seed (removed): poll with votes/percentages/faces, both sides of an important message, the scheduled "Editar" form, the poll builder. Votes, acks and edits are writes — need a real login.

**Pase de diseño del chat (2026-09-23, same day).** From a design review; the user chose these and deferred per-person avatar colors and narrow-screen layout ("cuando optimicemos todo").
- **Composer:** only emoji / imagen / voz / enviar stay visible. Everything else is behind a "+" (Más opciones) panel, in normal flow above the box, each with a name and a one-line description: Formato, Documento de Drive, Encuesta, Marcar importante, Enviar más tarde. A small gold dot on "+" means something from it is on (Importante, Drive mode, or one of its panels open). Threads' compact composer shows only the options that apply there.
- **Bubbles:** max width `min(75%, 600px)` so long messages wrap at a readable line length on wide screens. "Importante" is now a 3px gold left edge on the bubble (plus a tooltip) instead of an uppercase label above it; the label only remains when the message has no text bubble.
- **Header:** "Detalles" became an ⓘ icon button, matching buscar/llamar.
- **Sidebar:** the open view in the Inbox/Hilos/Menciones/Guardados/Archivos row widens to show its name in gold. The "avisos del sistema bloqueados" note is shown once and then remembered as seen (same localStorage key as "Ahora no").
- The hover toolbar was left as is — editing and deleting are already one click, and ⋯ holds the rest (user's call).

### 35. Platform round (2026-09-23): Inicio fixes, Google Drive for files and backups

The user asked "what's missing across the whole platform" and approved, in this order (my call): (1) Inicio fixes, (2–4) Google Drive — connection, "Exportar todo" backup, files from Drive, (5) global search, (6) error log, (7) permissions + Administración, (8) phone/iPad. Items 1–4 shipped here; 5–8 are next.

- **Inicio → Próxima reunión** now reads the person's Google Calendar (a `useGoogleCalendar` instance with `initialRange` = next 14 days; `pickNextMeeting()` in `HomeScreen.jsx` prefers events with other guests, skips all-day events, shows "Ahora — en curso"). The old `meetings` collection was never written by anything — no longer read. Not connected → "Conecta tu Google Calendar para verla aquí →" (click opens Calendario).
- **Inicio → Resumen financiero** now uses `useFinanceData()` — the same numbers as Finanzas (client payments + manual incomes; "Ingresos del mes", % vs last month, 6-month sparkline). Before, it counted client payments only and could say "Sin datos" while Finanzas showed income. Click opens Finanzas.
- **Google Drive, one connection:** `drive.file` was added to the shared Google OAuth scope (`DRIVE_SCOPE` in `lib/googleCalendar.js`, alongside calendar.readonly + meetings.space.created). `drive.file` is Google's non-sensitive "only files the user picks or the app creates" scope. OAuth `state` for Drive is `drive:<module>`; `AppShell` reopens that module and finishes the connection itself (`finishDriveConnect` in `lib/googleDrive.js`); `useGoogleCalendar` ignores `drive:` states. Old connections lack the scope → `DriveNeedsConnectError` → `useDrivePicker` shows a one-time "Conecta Google Drive" modal.
- **Archivos desde Drive** (`hooks/useDrivePicker.jsx` → Google Picker, loaded from apis.google.com on first use; has a "Subir" tab so a file on the computer goes into Drive first). ADOR OS stores only `{fileId, name, mimeType, url, iconUrl}` — files never live in Firestore, so no Firebase Storage/billing needed. Wired into: Clientes → Ficha → Documentos (old metadata-only rows still list as "Registro antiguo"), Finanzas → Nuevo gasto → Comprobante (+ "comprobante ↗" link in Movimientos), Comunicación "+" → "Archivo de Google Drive" (message `attachment: {kind:'drive', …}`, `DriveFileCard`, indexed in `chatFiles`, never expires; pasting a Drive link still renders the old card; the paste-only "Documento de Drive" mode was removed), Conocimiento editor → "📎 Adjuntar de Google Drive" (inserts Markdown links).
- **Exportar todo** (Configuración, admins only via `isAdmin`): `lib/backup.js exportAllData()` reads every collection + subcollections (client history/documents, task and knowledge history, channel/DM messages and thread replies) into one JSON (timestamps → ISO), leaving out `chatBlobs`, `chatTyping`, `presence` and any `refreshToken`/`accessToken`. `uploadBackupToDrive()` puts it in a "ADOR OS — Respaldos" folder in the admin's Drive (created once; drive.file can see folders it created). `settings/maintenance.lastBackup {at, by, link, documentCount}` shows "Último: …". Costs one Firestore read per document; manual, not scheduled.
- **Needs, before any of the Drive parts work (console, one time):** in Google Cloud project `ador-os-internal` (610980815690): enable **Google Drive API** and **Google Picker API**; Google Auth Platform → Data Access → add `…/auth/drive.file`; create an **API key** restricted to those two APIs and to referrers `https://ador-os-internal.vercel.app/*` and `http://localhost:*/*`, set as `VITE_GOOGLE_API_KEY` in Vercel (Production) and local `.env`, then redeploy. Each person then presses "Conectar Google" once.

### 36. Hosting moved from Vercel to Cloudflare Workers (2026-09-23)

**Why:** Vercel's Hobby (free) plan is "non-commercial personal use only" and counts any deployment used for a business's financial gain as commercial — an internal tool of a for-profit firm falls under that (checked against vercel.com/docs/limits/fair-use-guidelines). The user wants everything free and independent of any paid domain. Cloudflare's free plan allows commercial use. Workers (not the older Pages) was chosen as Cloudflare's long-term product.

- **Live URL: `https://ador-os.adorfirm.workers.dev`** — Cloudflare account subdomain `adorfirm` (renamed from the default), Worker name `ador-os`. Deliberately NOT on `adorfirm.com`: if the domain lapses, ADOR OS keeps working. A custom subdomain (e.g. `os.adorfirm.com`) can be added later as an extra alias only.
- **Deploys:** Workers Builds connected to GitHub `ArcAlphaAS/ADOR---OS-Internal-`, branch `main`: build `npm run build`, deploy `npx wrangler deploy`. Every push to main publishes in ~1–2 min (Cloudflare → ador-os → Deployments). Preview URLs per branch are on.
- **Config: `wrangler.jsonc`** — `main: server/worker.js`, `assets.directory: ./dist` with `not_found_handling: single-page-application` and `run_worker_first: ["/api/*"]`, `keep_vars: true` (so dashboard variables survive deploys), `workers_dev` + `preview_urls` true, logs on. The dashboard project name must stay `ador-os` (must match `name`). `.node-version` pins Node 22.
- **Server code: `server/handlers.js`** — the four host-independent handlers (`googleExchange`, `googleRefresh`, `meetSpace`, `adorIA`), each `({method, headers, body}, env) → {status, json}`. `server/worker.js` routes `/api/google-calendar/exchange|refresh`, `/api/google-meet/space`, `/api/ador-ia` to them via `server/cloudflare.js`; everything else is `env.ASSETS`. The client still calls the same `/api/...` paths. `api/*.js` + `server/vercel.js` are thin Vercel adapters kept only until Vercel is switched off — delete them then.
- **Env, two places in Cloudflare (ador-os → Settings):** *Builds → Variables and secrets* (baked into the app by Vite, plain text): `VITE_FIREBASE_*` (6), `VITE_GOOGLE_CLIENT_ID`, `VITE_GOOGLE_API_KEY`. *Runtime variables and secrets* (read by the /api handlers, type Secret): `GOOGLE_CLIENT_SECRET`, `VITE_GOOGLE_CLIENT_ID`. Local dev keeps using `.env`; `npx wrangler dev` also reads it.
- **Authorized:** Firebase Auth → Authorized domains has `ador-os.adorfirm.workers.dev`; the Google OAuth client "ADOR OS" has it as JS origin and `https://ador-os.adorfirm.workers.dev/` as redirect URI. The Vercel entries are still there until Vercel is removed.
- **Google Cloud (project `ador-os-internal`, 610980815690) finished the same day:** Drive API + Picker API enabled; Data Access declares `drive.file` (non-sensitive) plus `calendar.readonly` and `meetings.space.created` (sensitive). The app stays unverified on purpose ("Google hasn't verified this app" → Advanced → continue; 100-user cap) — don't submit verification unless the user asks. API key "ADOR OS – Drive Picker" restricted to Drive + Picker APIs and to `https://ador-os.adorfirm.workers.dev/*` and `http://localhost:5173/*`. The Firebase "Browser key (auto created by Firebase)" must not be touched.
- **Confirmed working live on Cloudflare (user):** login, existing Google connection (stored in Firestore, so it carried over), Drive picker. **Vercel paused by the user on 2026-09-23** (the old URL returns 503). Paused, not deleted, so `api/` + `server/vercel.js` and the Vercel URLs in Firebase/Google are kept as an instant fallback; remove them only when the user deletes the Vercel project.

### 37. Company Drive folder, full search, error log, roles + Administración, stricter rules (drafted) (2026-09-23/24)

Continuing the platform round (§35) after the Cloudflare move (§36).

- **Carpeta de ADOR en Drive:** the founders use personal Gmail accounts (no Workspace/domain), so "the company's Drive" = a folder made in one partner's Drive and shared with the others as Editor. An admin chooses it once (Administración → Datos → "Carpeta de ADOR en Drive", Google picker in folder mode) → `settings/google.driveFolder {fileId, name, url}`. The picker then opens on it first and its "Subir" tab uploads straight into it (`DocsUploadView.setParent`); backups go to a "Respaldos" subfolder inside it (falls back to "ADOR OS — Respaldos" in the admin's own Drive if that connection can't write there).
- **Búsqueda global** (`hooks/useGlobalSearch.js` + `SearchResults.jsx`): clientes, tareas, **mensajes** (via `useMessageSearch`, from 3 characters, cached 15 min), personas, conocimiento, objetivos, decisiones, proyectos, anuncios, comunidad. Accent/case-insensitive, all words must match; ↑/↓ + Enter. Each result has `target = [moduleId, focus]`; Directorio (`{type:'person'}`) and News (`{type:'news'|'community'}`) gained `focus` props; decisions now open Objetivos (they moved there in §26 — the old link went to Workspace). Members never see results from modules they can't open.
- **Registro de errores** (`lib/errorLog.js`): window errors, unhandled rejections and render crashes go to `errorLogs` (max 15 per tab, deduped, known noise ignored, no form data/tokens), listed in Administración → Errores; deleted after 30 days by the daily cleanup. `ModuleErrorBoundary` wraps the module area: a crashing module shows "Algo falló en esta sección · Reintentar" instead of a blank screen; the rest of the shell keeps working.
- **Roles** (`lib/access.js`, `hooks/useAccess.js`): Administrador (partners, everything) and Miembro (modules in `settings/access.memberModules`, default all but Finanzas; never Administración). Role = `users/{uid}.isAdmin` (missing = admin, so the founders need nothing). TopBar/Sidebar hide modules, AppShell shows "Esta sección no está disponible para tu rol", ProfileMenu shows "Administración" only to admins. Daily chat cleanup now runs only from an admin's app.
- **Administración** (`components/admin/AdminModule.jsx`, route `admin`, from the profile menu): Personas (list from `allowedEmails` + `users`, role select, "Quitar acceso" = delete `allowedEmails/{email}`, can't demote/remove yourself or the last admin), **Invitar** (`lib/invite.js`: allowedEmails with role → Auth account via a second throwaway Firebase app so the admin isn't signed out → `users/{uid}` with role → Firebase password-reset email so they pick their password; if the email already had an account, role applies on first login via `allowedEmails.role` in App.jsx), Accesos (member module checkboxes), Errores, Datos (Drive folder + Exportar todo, moved here from Configuración).
- **Chat channels query split** for rule compatibility: `subscribeChatChannels(uid, …)` = `visibility == 'public'` ∪ `memberUids array-contains uid`. Channels from before `visibility` existed are fixed by `backfillChannelVisibility()` (runs automatically from an admin's app). `cleanupMessageIndexes` is best-effort.
- **`firestore.rules` (in the repo) — DRAFTED, NOT LIVE.** Admins: everything. Members: shared work data; Finanzas (expenses, incomes, settings/finanzas) only if allowed; can't change their own `isAdmin`; private channels/groups only if member (messages checked via the parent channel); DMs only if their uid is in the DM id; mentions/saved/reminders/calls/scheduled only their own; error logs create-only. Known limitation: `chatFiles`/`chatBlobs` readable by any allowed account. **Before pasting it into Firebase Console → Firestore → Rules, test it** (Rules Playground in the console, or the Firestore emulator — needs Java, not installed here). Only needed before the first Miembro is invited; today all 3 accounts are admins.

### 38. Phone and iPad (2026-09-24)

- **Installable (PWA):** `public/manifest.webmanifest` (name ADOR OS, standalone, dark theme, `icon-192.png` / `icon-512.png` from the gold-Λ icon, also as maskable) + Apple meta tags in `index.html` (`apple-mobile-web-app-capable`, black-translucent status bar, title) and `viewport-fit=cover`. "Añadir a pantalla de inicio" on iPhone/iPad (Safari → Compartir) or Android (Chrome → Instalar app) opens it full-screen with its own icon. No service worker yet — that comes with push notifications (the parked "layer 3": calls/messages with the app closed).
- **Layout below 1024px (phones + iPad portrait):** the floating side capsule and the top pill tabs are hidden (`hidden lg:…`); `components/shell/BottomNav.jsx` is an iOS-style bottom tab bar — Inicio · Workspace · Chat (gold unread badge) · Clientes · **Más** (a bottom sheet with the rest: Objetivos, Finanzas, Calendario, Conocimiento, News, Directorio, ADOR IA, Administración) — filtered by role. `main` gets bottom padding (64px + safe area); message toasts move above the bar. iPad landscape and desktop are unchanged.
- **Modules:** root paddings are `px-4 md:px-8 lg:px-12`; two-column layouts stack (`grid-cols-1 lg:/xl:grid-cols-[…]`: Inicio, Objetivos, Conocimiento, Finanzas 66/34); wide tables (Conocimiento, Directorio) scroll sideways (`overflow-x-auto` + `min-w`); Workspace hides its filter column below md and shows a compact scope `<select>` (Personal / Grupo / each workstream), view tabs icon-only on phones; **Comunicación** is one pane at a time below md (list → conversation full-screen with "← Conversaciones"), and its side panels (Perfil/Detalles/Hilo) cover the screen on phones.
- **Verified** in the preview at 375×812 and 768×1024: every module, no horizontal overflow, no console errors. Not yet tried on a real phone.

### 39. Push notifications with ADOR OS closed (2026-09-24)

Standard Web Push, free, no Firebase Cloud Messaging: the system notification bar on iPhone (only with ADOR OS installed on the home screen, iOS 16.4+), Android and desktop.

- **`public/sw.js`** (service worker, registered in production from `main.jsx`; not in `npm run dev`): shows each push; skips it when ADOR OS is open and in front (the in-app toast/call screen covers it), except on Safari/iOS, which revokes push permission from sites that receive a push without showing one. Tap → focuses the open window and `postMessage`s the link, or opens `/?open=chat&ct=…&cid=…&p=…&m=…&t=…`. No fetch handler, no offline caching.
- **Devices** (`lib/push.js`): "Activar" (a tap is required for the permission prompt) → `pushManager.subscribe` with the VAPID public key from `GET /api/push/config` → `pushSubscriptions/{sha256(endpoint)}` `{uid, endpoint, keys, device}`. Re-saved under the signed-in person on every open (`refreshPushSubscription` in AppShell); deleted + unsubscribed on sign-out (`useAuth.signOut`). UI: `PushNotificationsCard.jsx`: always in Configuración (with "Enviar notificación de prueba"), and as a one-time prompt in Comunicación's sidebar (replaced the old "Activar avisos de llamada" prompt; permission now comes with push).
- **Sending** (`server/push.js`, `POST /api/push/send`): the sender's app posts the event with its Firebase ID token. The Worker reads Firestore **through the REST API with that token** (so the normal rules apply and an invalid token gets nothing, which means no separate JWT check is needed): every `pushSubscriptions` doc, the channel doc, and each recipient's `users` + `presence`. Then it applies the bell's rules (`notifyLevel`: DM/group default all, channel default mentions; silenced = none; No molestar = nothing), encrypts per device (RFC 8291 aes128gcm) and signs with VAPID (RFC 8292), all on Web Crypto. 404/410 from the push service deletes that subscription. Encryption was checked by decrypting with the reference `http_ece` library, and the VAPID signature with a verify.
- **What triggers a push:** `deliverMessage()` (composer, forwards, scheduled) sends `{kind:'message', targets:[{uid, reason: mention|important|quote|reply}]}`. The server adds the rest of the conversation as reason `message` (only reaches people whose level is `all`); thread replies (`onlyTargets`) only reach followers + mentions. Call cards don't push as messages. `startCall` sends `{kind:'call'}` (skips DND only, TTL 60s, `requireInteraction`).
- **No duplicates:** while this device has push on (`isPushOnHere()`), IncomingCallGate/ChatMessageToaster skip their in-page `new Notification()` fallback.
- **Config:** `wrangler.jsonc` `vars`: `FIREBASE_PROJECT_ID`, `VAPID_PUBLIC_KEY`, `VAPID_SUBJECT`. **`VAPID_PRIVATE_KEY` is a Cloudflare runtime Secret** (it was given to the user once in chat and is not stored in the repo). Losing it means generating a new pair, updating the public key in wrangler.jsonc, and everyone pressing Activar again.
- **Delivery delay (2026-09-24):** a partner on Android got a message about 4 minutes late. The server already sends immediately with `Urgency: high` and there's no queue. Likely cause, now fixed: if the sender locks the phone or switches app right after sending, the phone can suspend the `/api/push/send` request until the page comes back. `sendPush` now uses `fetch(…, {keepalive:true})`. The other possible cause is on the recipient's device: Android battery optimization for Chrome. Ask them to set Chrome's battery usage to Unrestricted and allow notifications for ADOR OS.
- **Status:** live 2026-09-24. The secret is set and the test notification was confirmed on the user's iPhone (installed PWA).
- **Known limits:** `senderUid`/`senderName` are taken from the request body, not from the token, so an allowed account could technically spoof the sender's name on a notification. Acceptable among the founders; verify the token's `sub` if members ever join. The `pushSubscriptions` rule is in the drafted `firestore.rules`; the live blanket rule already covers it.

### 40. Feeling like an iPhone app: floating tab bar, Satoshi, calls from the phone (2026-09-24)

- **Typeface = Apple's SF Pro through the system font stack** (`-apple-system, BlinkMacSystemFont, …`). Satoshi was used first the same day, then the user switched to SF. On Apple devices it's the real SF Pro. Apple's license forbids serving SF as a web font, so Android/Windows fall back to Inter (Google Fonts, only downloaded where used). Defined in `body` and Tailwind's `--font-sans` in `index.css`. `font-mono` is unchanged.
- **Mobile tab bar** (`BottomNav.jsx`, below 1024px): a floating capsule (`inset-x-3`, 64px, `#1C1C1E`/90 + blur, just above the home indicator) with icons only; the active tab is a filled gold circle (from the user's reference image). z-[46], so it stays above the "Más" sheet (tap again to close) and the chat's side panels. Bottom spacing was updated to match: main `pb-[calc(92px+safe)]`, SidePanel, message toasts and GlobalCapture's "+" (smaller on phones).
- **Calls from a phone** (`ChatModule.jsx` `quickCallTouch`/`ReadyCallSheet`): on touch devices below 1024px there's no pre-opened "Creando la reunión…" tab (in the installed app it was a dead end with no way back). The room is created, the card is posted and the other person starts ringing, then a bottom sheet offers "Unirse a Meet" as a real link the person taps, so iOS/Android open the Meet app. ADOR OS stays on the chat underneath. Desktop is unchanged.
- **Native touch feel, touch screens only** (`@media (hover:none) and (pointer:coarse)` inside `@layer base`, so Tailwind utilities still win): no tap highlight, no double-tap zoom, no text selection/callout on buttons/links/nav, UIKit-like fade on press. The viewport has `maximum-scale=1`, so iOS doesn't zoom into inputs on focus (pinch zoom still works on iOS).

- **Motion + gestures (same day, `lib/motion.js`):** `SPRING` (centered modals: Settings/Profile, the New… modals, Forward, TaskFromMessage, call and assignment gates, GlobalCapture) and `SHEET` (the "Más" sheet, Task/Client/Finance detail panels, ReadyCallSheet) replaced fixed-duration easings. `swipeToClose(axis, onClose)` only switches on for touch below 1024px: swipe down closes the "Más" sheet and ReadyCallSheet; swipe right closes the Task/Client/Finance detail panels and the chat's Perfil/Detalles/Hilo panel (below 768px only, where it's full-screen). GlobalCapture has no swipe on purpose, because it holds a textarea. Tapping the active bottom tab scrolls `main` and any scrolled list inside it to the top; message threads carry `data-keep-scroll` and are left alone.

- **Press and hold a message (phones, same day):** on touch below 1024px the hover toolbar is hidden (`touch` in `MessageBubble.jsx`), and holding a message for 420ms (cancelled if the finger moves >8px) opens `MessageActionSheet.jsx`, an iOS action sheet with a preview, the quick reactions, then Responder, Responder en hilo, Copiar texto, Guardar, Reenviar, Fijar, Recordármelo… (sub-list), Crear tarea, Editar and Eliminar (asks to confirm), plus Cancelar. Only options that apply appear. Swipe down closes it. The bubble area is `select-none` with no iOS callout on touch, so holding doesn't select text ("Copiar texto" covers copying). Editing got visible Cancelar/Guardar buttons, since phones have no Enter/Esc.

### 41. Installed-app polish: update notice, keyboard, icon badge, shortcuts, desktop install (2026-09-24)

- **"Nueva versión · Actualizar":** `vite.config.js` gives each build an id (`__BUILD_ID__`) and emits `dist/version.json`. `hooks/useAppUpdate.js` compares the two on open, every 5 min, and when the app comes back to the front. `UpdateBanner.jsx` (AppShell) shows a pill at the top that reloads. Production only.
- **Keyboard (phones, `lib/keyboard.js`, from main.jsx):** sets `--app-h` from `visualViewport` and toggles `html.kb-open` when the keyboard is up. CSS in `index.css` then sizes `.h-screen` and full-screen phone panels (`.ador-kb-fit`: chat SidePanel) to the visible height, hides `.ador-hide-on-kb` (the bottom tab bar and GlobalCapture's "+"), and drops main's bottom padding, so the composer sits right on the keyboard. It also undoes iOS's own slide-up (`scrollTo(0,0)`). Not verifiable in the desktop preview; needs a real phone.
- **Icon badge:** AppShell calls `navigator.setAppBadge(chatUnread)` and posts `ador-badge` to the service worker. `sw.js` stores the number in the Cache API (`ador-meta`) and adds 1 per message push that arrives while the app isn't in front. Installed app only (iOS 16.4+, Android, desktop PWAs).
- **Icon shortcuts:** manifest `shortcuts` (Comunicación, Hoy, Calendario, Clientes) → `/?open=<module>`, handled by `parseOpenLink`. **Android and desktop Chrome/Edge only; iOS doesn't support web app shortcuts.**
- **Desktop "always on":** push already works on computers (Configuración → Activar). Configuración now also offers "Instalar en esta computadora" (Chrome/Edge `beforeinstallprompt`, `installApp()` in lib/push.js). Installed, it gets its own Dock/taskbar icon and badge, and notifications keep arriving while the browser is running, even with no window open.

- **Offline (same day):** (1) Firestore uses `persistentLocalCache` + `persistentMultipleTabManager` (`createDb()` in lib/firestore.js, falls back to the memory cache if IndexedDB isn't allowed). Data shows with no connection, and offline writes survive closing the app and go out on reconnect. (2) `withTimeout` (lib/workspace.js, used by every write) no longer fails while `navigator.onLine === false`: it waits for `online` and restarts the clock. Before this, an offline message showed "Tardó demasiado" and an offline image send was aborted, even though Firestore would have delivered it. (3) `OfflineBanner.jsx`: a "Sin conexión — lo que envíes saldrá al volver la señal" pill, then "Conectado de nuevo" for 2.5s. Messages sent offline keep showing ◷ until confirmed. Checked in the preview by faking `navigator.onLine` + the events.

### 42. Interface pass: logo, card hierarchy, one title style (2026-09-24)

- **"ADOR OS" in the top bar:** the "OS" had `letterSpacing: 0.3em` and read as "O S". Now 0.04em, muted grey `#9A9A9A`, 5px gap, baseline-aligned with the wordmark (TopBar.jsx).
- **Card hierarchy (index.css, unlayered after `.ador-glass`):** `.ador-card-attention` (gold `#E8C15A` inset left edge + warm tint) and `.ador-card-urgent` (same in red), via a `--attn` color-mix. On Inicio: `WeeklySummaryCard` follows the new `level` from `buildWeeklyNarrative()` ('urgent' | 'warn' | 'calm', from the worst highlight), and its dot and text weight follow it too. `TasksTodayBlock` is attention when there are open tasks today and urgent when one is blocked. Informational cards stay plain. Inicio's card labels went from `#444` (barely readable) to `#7A7A7A`. Reuse these two classes elsewhere when a card holds something to act on.
- **One page title everywhere:** `.ador-title` (`@layer components`): 26px on phones, 30px from md, semibold, -0.02em. It replaced 18/22/28/30/34px h1s in every module (Calendario, Conocimiento, Finanzas, Hoy, Directorio, Workspace, Administración, Objetivos, News, ADOR IA, Clientes). Top spacing is unified to `pt-6 … lg:pt-10`, `pb-16`, `lg:px-12` (Calendario, ADOR IA, Objetivos, Finanzas and Workspace were off). Inicio keeps its own larger greeting.

## Next recommended steps (in priority order, as discussed with the user)

Keep this list in sync with "Next steps" in `PROJECT_STATE.md`.

1. **Real-world checks by the user:** install ADOR OS on a phone (Safari → Compartir → Añadir a pantalla de inicio / Chrome → Instalar app); create the shared "ADOR" Drive folder and pick it in Administración → Datos; set Firebase Auth email templates to Spanish (Authentication → Templates → Template language); tell Leonardo and Mateo the new URL.
2. **Two-account test** (two founders, two computers): Comunicación (mentions, bell, threads, ✓/✓✓, "escribiendo…", polls, important messages, scheduled messages, forwarding, calls) and an invite from Administración (e.g. a spare email as Miembro).
3. **Test and activate `firestore.rules`** (§37) — required before the first Miembro is invited. Test with the Firestore emulator (needs Java — ask before installing a portable JDK in `~/.local`, same approach as Node) or the Rules Playground in the Firebase console; only then paste into Firebase → Firestore → Rules.
4. **Push notifications (§39): live.** `VAPID_PRIVATE_KEY` is set in Cloudflare, and the test notification arrived on the user's iPhone on 2026-09-24. Still to do: Leonardo and Mateo turn them on per device, then a real message/call test between two people.
5. **Existing clients created before 2026-08-15 have no `code` field** (shown as "—"). Ask the user before writing a backfill script — see §13.
6. If Vercel is ever deleted (it's paused): remove its URLs from Firebase Authorized domains and the Google OAuth client, and delete `api/` + `server/vercel.js` (§36).

Note: files no longer need Firebase Storage — they live in Google Drive (§35/§37). Don't push Storage unless the user asks.

Note: Calendario (§27) is done and confirmed working live as of 2026-09-17 — not on this list anymore.

Note: connecting `GEMINI_API_KEY` (now a Cloudflare runtime secret, §36) is **not** on this list anymore — ADOR IA runs on a local rule-based engine by explicit user choice (see §17). Don't propose adding the key unless the user raises it first.

## Other context that only exists in conversation history (not in any file)

- The user also has a broader Claude memory system at `/Users/angelsamillan/.claude/projects/-Users-angelsamillan-Claude/memory/` with product-vision-level context (`project_ador_mvp_master_prompt.md`, `project_ador_living_enterprise_vision.md`, `project_ador_digital_twin_design.md`, `project_ador_chatbot_tone.md`) that predates and informed this build. Worth reading if a future session needs product rationale beyond what's in this repo.
- Node.js is a portable, non-system install at `~/.local/node` (added to `~/.zshrc` `PATH`) — this was a deliberate choice over Homebrew/system install to avoid touching the user's system; don't "fix" this by reinstalling Node system-wide without asking.
- The Firebase project (`ador-os`) was created and configured live, step-by-step, with the user driving the console themselves while being walked through it — they now know how to navigate Firebase console basics (Authentication, sign-in providers, adding a web app). Don't over-explain those basics again; do walk them through Firestore setup the same way when that becomes the next step, since it's a new area for them.
